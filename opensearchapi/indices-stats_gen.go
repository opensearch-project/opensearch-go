// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"io"
	"net/http"
	"strconv"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/errmask"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
)

// IndicesStatsReq represents the request for the indices.stats operation.
//
// Provides statistics on operations happening in an index.
//
// GET /_stats
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesStatsReq struct {
	// Metric specifies the list of path segments for the request URL.
	Metric []string

	// Indices specifies the list of path segments for the request URL.
	Indices []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *IndicesStatsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesStatsReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.IndicesStatsPath{
		Metric:  r.Metric,
		Indices: r.Indices,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// IndicesStatsParams represents query parameters for the IndicesStatsReq.
type IndicesStatsParams struct {
	TimeoutParams
	DebugParams
	// A comma-separated list or wildcard expressions of fields to include in
	// field data and suggest statistics.
	CompletionFields []string

	// Type of index that wildcard patterns can match. If the request can
	// target data streams, this argument determines whether wildcard
	// expressions match hidden data streams. Supports comma-separated values,
	// such as `open,hidden`.
	ExpandWildcards []string

	// A comma-separated list or wildcard expressions of fields to include in
	// field data statistics.
	FielddataFields []string

	// A comma-separated list or wildcard expressions of fields to include in
	// the statistics.
	Fields []string

	// If `true`, statistics are not collected from closed indexes.
	//
	// Default: true.
	ForbidClosedIndices *bool

	// A comma-separated list of search groups to include in the search
	// statistics.
	Groups []string

	// If `true`, the call reports the aggregated disk usage of each one of the
	// Lucene index files (only applies if segment stats are requested).
	//
	// Default: false.
	IncludeSegmentFileSizes *bool

	// If `true`, the response includes information from segments that are not
	// loaded into memory.
	//
	// Default: false.
	IncludeUnloadedSegments *bool

	// Indicates whether statistics are aggregated at the cluster, index, or
	// shard level.
	Level string
}

func (r IndicesStatsParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if len(r.CompletionFields) > 0 {
		set("completion_fields", strings.Join(r.CompletionFields, ","))
	}

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	if len(r.FielddataFields) > 0 {
		set("fielddata_fields", strings.Join(r.FielddataFields, ","))
	}

	if len(r.Fields) > 0 {
		set("fields", strings.Join(r.Fields, ","))
	}

	if r.ForbidClosedIndices != nil {
		set("forbid_closed_indices", strconv.FormatBool(*r.ForbidClosedIndices))
	}

	if len(r.Groups) > 0 {
		set("groups", strings.Join(r.Groups, ","))
	}

	if r.IncludeSegmentFileSizes != nil {
		set("include_segment_file_sizes", strconv.FormatBool(*r.IncludeSegmentFileSizes))
	}

	if r.IncludeUnloadedSegments != nil {
		set("include_unloaded_segments", strconv.FormatBool(*r.IncludeUnloadedSegments))
	}

	if r.Level != "" {
		set("level", r.Level)
	}

	return params
}

// IndicesStatsResp represents the response for the indices.stats operation.
//
// Provides statistics on operations happening in an index.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesStatsResp struct {
	// The statistics for all indexes.
	All IndicesStatsAllIndices `json:"_all"`

	Shards  ShardStatistics                `json:"_shards"`
	Indices map[string]IndicesStatsIndices `json:"indices,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesStatsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r IndicesStatsResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// IndicesStatsAllIndices is a typed component of the indices.stats operation.
//
// The statistics for all indexes.
type IndicesStatsAllIndices struct {
	Primaries IndicesStatsIndex `json:"primaries"`
	Total     IndicesStatsIndex `json:"total"`
}

// IndicesStatsIndex is a typed component of the indices.stats operation.
type IndicesStatsIndex struct {
	IndicesStatsIndexBase
}

// IndicesStatsIndexBase is a typed component of the indices.stats operation.
//
// The base statistics for an index.
type IndicesStatsIndexBase struct {
	Completion *CompletionStats `json:"completion,omitempty"`

	// The document-level statistics.
	Docs *DocStats `json:"docs,omitempty"`

	// The statistics about field data memory usage.
	Fielddata *FielddataStats `json:"fielddata,omitempty"`

	// The statistics about flush operations.
	Flush *FlushStats `json:"flush,omitempty"`

	// The statistics about get operations.
	Get *GetStats `json:"get,omitempty"`

	// The statistics about indexing operations.
	Indexing *IndexingStats `json:"indexing,omitempty"`

	// The statistics about merge operations.
	Merges *MergesStats `json:"merges,omitempty"`

	// The statistics about query cache usage.
	QueryCache *QueryCacheStats `json:"query_cache,omitempty"`

	// The statistics about recovery operations.
	Recovery *RecoveryStats `json:"recovery,omitempty"`

	// The statistics about refresh operations.
	Refresh *RefreshStats `json:"refresh,omitempty"`

	// The statistics about request cache operations.
	RequestCache *RequestCacheStats `json:"request_cache,omitempty"`

	// The statistics about search operations.
	Search *SearchStats `json:"search,omitempty"`

	// The statistics about segments.
	Segments *SegmentsStats `json:"segments,omitempty"`

	Store *StoreStats `json:"store,omitempty"`

	// The statistics about translog operations.
	Translog *TranslogStats `json:"translog,omitempty"`

	// The statistics about index warmer operations.
	Warmer *WarmerStats `json:"warmer,omitempty"`
}

// IndicesStatsIndices is a typed component of the indices.stats operation.
//
// The statistics for specific indexes.
type IndicesStatsIndices struct {
	Primaries IndicesStatsIndex `json:"primaries"`

	// The statistics for individual shards.
	Shards map[string][]IndicesStatsIndexShard `json:"shards,omitempty"`

	Total IndicesStatsIndex `json:"total"`

	// The universally unique identifier.
	UUID string `json:"uuid"`
}

// IndicesStatsIndexShard is a typed component of the indices.stats operation.
type IndicesStatsIndexShard struct {
	IndicesStatsIndexShardBase
}

// IndicesStatsIndexShardBase is a typed component of the indices.stats operation.
type IndicesStatsIndexShardBase struct {
	IndicesStatsIndexBase

	// The commit information for a shard.
	Commit *IndicesStatsShardCommit `json:"commit,omitempty"`

	// The retention lease information for a shard.
	RetentionLeases *IndicesStatsShardRetentionLeases `json:"retention_leases,omitempty"`

	// The routing information for a shard.
	Routing *IndicesStatsShardRouting `json:"routing,omitempty"`

	// The sequence number information for a shard.
	SeqNo *IndicesStatsShardSequenceNumber `json:"seq_no,omitempty"`

	// The path information for a shard.
	ShardPath *IndicesStatsShardPath `json:"shard_path,omitempty"`
}

// IndicesStatsShardCommit is a typed component of the indices.stats operation.
//
// The commit information for a shard.
type IndicesStatsShardCommit struct {
	// The generation number of the commit.
	Generation int `json:"generation"`

	// The unique identifier for a resource.
	ID string `json:"id"`

	// The number of documents in the commit.
	NumDocs int64 `json:"num_docs"`

	// The user-defined data associated with the commit.
	UserData map[string]string `json:"user_data"`
}

// IndicesStatsShardRetentionLeases is a typed component of the indices.stats operation.
//
// The retention lease information for a shard.
type IndicesStatsShardRetentionLeases struct {
	// The list of retention leases.
	Leases []IndicesStatsShardLease `json:"leases"`

	// The primary term of the retention leases.
	PrimaryTerm int64 `json:"primary_term"`

	Version int64 `json:"version"`
}

// IndicesStatsShardLease is a typed component of the indices.stats operation.
//
// The retention lease details.
type IndicesStatsShardLease struct {
	// The unique identifier for a resource.
	ID string `json:"id"`

	// The sequence number of the document.
	RetainingSeqNo int64 `json:"retaining_seq_no"`

	Source    string `json:"source"`
	Timestamp int64  `json:"timestamp"`
}

// IndicesStatsShardRouting is a typed component of the indices.stats operation.
//
// The routing information for a shard.
type IndicesStatsShardRouting struct {
	// The node containing the shard.
	Node string `json:"node"`

	// Whether this is a primary shard.
	Primary bool `json:"primary"`

	// The node where the shard is relocating to.
	RelocatingNode *string `json:"relocating_node"`

	// The state of shard routing.
	State string `json:"state"`
}

// IndicesStatsShardSequenceNumber is a typed component of the indices.stats operation.
//
// The sequence number information for a shard.
type IndicesStatsShardSequenceNumber struct {
	// The global checkpoint for the shard.
	GlobalCheckpoint int64 `json:"global_checkpoint"`

	// The local checkpoint for the shard.
	LocalCheckpoint int64 `json:"local_checkpoint"`

	// The sequence number of the document.
	MaxSeqNo int64 `json:"max_seq_no"`
}

// IndicesStatsShardPath is a typed component of the indices.stats operation.
//
// The path information for a shard.
type IndicesStatsShardPath struct {
	// The path to the shard's data.
	DataPath string `json:"data_path"`

	// Whether the shard uses a custom data path.
	IsCustomDataPath bool `json:"is_custom_data_path"`

	// The path to the shard's state.
	StatePath string `json:"state_path"`
}

// BroadcastShardFailures detects partial failures on a broadcast-shape
// response (one envelope, all-shards aggregate). Returns nil when no
// shards failed.
func (r *IndicesStatsResp) BroadcastShardFailures() *PartialSearchError {
	if r == nil {
		return nil
	}
	if r.Shards.Failed == 0 {
		return nil
	}
	return &PartialSearchError{
		FailedShards: r.Shards.Failed,
		TotalShards:  r.Shards.Total,
		Failures:     r.Shards.Failures,
	}
}

// PartialFailures returns the partial-failure sub-errors detected on the
// IndicesStatsResp, gated by mask. Mask bits suppress their corresponding
// wrapper category.
func (r *IndicesStatsResp) PartialFailures(mask errmask.ErrorMask) []error {
	var errs []error
	if !mask.Has(errmask.BroadcastShards) {
		if e := r.BroadcastShardFailures(); e != nil {
			errs = append(errs, e)
		}
	}
	return errs
}

// Stats provides statistics on operations happening in an index.
//
// GET /_stats
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
func (c indicesClient) Stats(ctx context.Context, req *IndicesStatsReq) (*IndicesStatsResp, error) {
	if req == nil {
		req = &IndicesStatsReq{}
	}

	var (
		data IndicesStatsResp
		err  error
	)
	if data.response, err = request(
		ctx,
		c.apiClient,
		http.MethodGet,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, collapsePerOpErrors(data.PartialFailures(c.apiClient.errorMask()), nil)
}
