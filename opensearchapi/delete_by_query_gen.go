// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strconv"
	"strings"
	"time"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
)

// DeleteByQueryReq represents the request for the delete_by_query operation.
//
// Deletes documents matching the provided query.
//
// POST /{index}/_delete_by_query
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/document-apis/delete-by-query/
type DeleteByQueryReq struct {
	// Indices specifies the list of path segments for the request URL.
	Indices []string

	// Body specifies the typed request body. When non-nil, it is
	// marshaled to JSON for the request payload.
	Body *DeleteByQueryBody

	// BodyReader provides an escape hatch for sending a raw request
	// body. It is used only when Body is nil.
	BodyReader io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *DeleteByQueryParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r DeleteByQueryReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.DeleteByQueryPath{
		Indices: r.Indices,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	var bodyReader io.Reader
	if r.Body != nil {
		bodyData, err := json.Marshal(r.Body)
		if err != nil {
			return nil, err
		}
		bodyReader = bytes.NewReader(bodyData)
	} else if r.BodyReader != nil {
		bodyReader = r.BodyReader
	}

	return build.Request(
		method,
		path,
		bodyReader,
		params,
		r.Header,
	)
}

// DeleteByQueryParams represents query parameters for the DeleteByQueryReq.
type DeleteByQueryParams struct {
	TimeoutParams
	DebugParams
	// Set to `true` or `false` to return the `_source` field or not, or a list
	// of fields to return.
	Source string

	// List of fields to exclude from the returned `_source` field.
	SourceExcludes []string

	// List of fields to extract and return from the `_source` field.
	SourceIncludes []string

	// If `false`, the request returns an error if any wildcard expression,
	// index alias, or `_all` value targets only missing or closed indexes.
	// This behavior applies even if the request targets other open indexes.
	// For example, a request targeting `foo*,bar*` returns an error if an
	// index starts with `foo` but no index starts with `bar`.
	AllowNoIndices *bool

	// If `true`, wildcard and prefix queries are analyzed.
	//
	// Default: false.
	AnalyzeWildcard *bool

	// Analyzer to use for the query string.
	Analyzer string

	// What to do if delete by query hits version conflicts: `abort` or
	// `proceed`.
	Conflicts string

	// The default operator for query string query: `AND` or `OR`.
	DefaultOperator string

	// Field to use as default where no field prefix is given in the query
	// string.
	Df string

	// Type of index that wildcard patterns can match. If the request can
	// target data streams, this argument determines whether wildcard
	// expressions match hidden data streams. Supports comma-separated values,
	// such as `open,hidden`. Valid values are: `all`, `open`, `closed`,
	// `hidden`, `none`.
	ExpandWildcards []string

	// Starting offset.
	//
	// Default: 0.
	From int

	// If `false`, the request returns an error if it targets a missing or
	// closed index.
	IgnoreUnavailable *bool

	// If `true`, format-based query failures (such as providing text to a
	// numeric field) in the query string will be ignored.
	Lenient *bool

	// Maximum number of documents to process. Defaults to all documents.
	MaxDocs int

	// Specifies the node or shard the operation should be performed on. Random
	// by default.
	//
	// Default: random.
	Preference string

	// Query in the Lucene query string syntax.
	Q string

	// If `true`, OpenSearch refreshes all shards involved in the delete by
	// query after the request completes.
	Refresh string

	// If `true`, the request cache is used for this request. Defaults to the
	// index-level setting.
	RequestCache *bool

	// The throttle for this request in sub-requests per second.
	//
	// Default: 0.
	RequestsPerSecond int

	// A custom value used to route operations to a specific shard.
	Routing []string

	// Period to retain the search context for scrolling.
	Scroll time.Duration

	// Size of the scroll request that powers the operation.
	//
	// Default: 100.
	ScrollSize int

	// Explicit timeout for each search request. Defaults to no timeout.
	SearchTimeout time.Duration

	// The type of the search operation. Available options: `query_then_fetch`,
	// `dfs_query_then_fetch`.
	SearchType string

	// Deprecated: use `max_docs` instead.
	Size int

	// The number of slices this task should be divided into.
	Slices string

	// A comma-separated list of <field>:<direction> pairs.
	Sort []string

	// Specific `tag` of the request for logging and statistical purposes.
	Stats []string

	// Maximum number of documents to collect for each shard. If a query
	// reaches this limit, OpenSearch terminates the query early. OpenSearch
	// collects documents before sorting. Use with caution. OpenSearch applies
	// this parameter to each shard handling the request. When possible, let
	// OpenSearch perform early termination automatically. Avoid specifying
	// this parameter for requests that target data streams with backing
	// indexes across multiple data tiers.
	TerminateAfter int

	// If `true`, returns the document version as part of a hit.
	Version *bool

	// The number of shard copies that must be active before proceeding with
	// the operation. Set to all or any positive integer up to the total number
	// of shards in the index (`number_of_replicas+1`).
	WaitForActiveShards string

	// If `true`, the request blocks until the operation is complete.
	//
	// Default: true.
	WaitForCompletion *bool
}

func (r DeleteByQueryParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.Source != "" {
		set("_source", r.Source)
	}

	if len(r.SourceExcludes) > 0 {
		set("_source_excludes", strings.Join(r.SourceExcludes, ","))
	}

	if len(r.SourceIncludes) > 0 {
		set("_source_includes", strings.Join(r.SourceIncludes, ","))
	}

	if r.AllowNoIndices != nil {
		set("allow_no_indices", strconv.FormatBool(*r.AllowNoIndices))
	}

	if r.AnalyzeWildcard != nil {
		set("analyze_wildcard", strconv.FormatBool(*r.AnalyzeWildcard))
	}

	if r.Analyzer != "" {
		set("analyzer", r.Analyzer)
	}

	if r.Conflicts != "" {
		set("conflicts", r.Conflicts)
	}

	if r.DefaultOperator != "" {
		set("default_operator", r.DefaultOperator)
	}

	if r.Df != "" {
		set("df", r.Df)
	}

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	if r.From != 0 {
		set("from", strconv.Itoa(r.From))
	}

	if r.IgnoreUnavailable != nil {
		set("ignore_unavailable", strconv.FormatBool(*r.IgnoreUnavailable))
	}

	if r.Lenient != nil {
		set("lenient", strconv.FormatBool(*r.Lenient))
	}

	if r.MaxDocs != 0 {
		set("max_docs", strconv.Itoa(r.MaxDocs))
	}

	if r.Preference != "" {
		set("preference", r.Preference)
	}

	if r.Q != "" {
		set("q", r.Q)
	}

	if r.Refresh != "" {
		set("refresh", r.Refresh)
	}

	if r.RequestCache != nil {
		set("request_cache", strconv.FormatBool(*r.RequestCache))
	}

	if r.RequestsPerSecond != 0 {
		set("requests_per_second", strconv.Itoa(r.RequestsPerSecond))
	}

	if len(r.Routing) > 0 {
		set("routing", strings.Join(r.Routing, ","))
	}

	if r.Scroll != 0 {
		set("scroll", formatDuration(r.Scroll))
	}

	if r.ScrollSize != 0 {
		set("scroll_size", strconv.Itoa(r.ScrollSize))
	}

	if r.SearchTimeout != 0 {
		set("search_timeout", formatDuration(r.SearchTimeout))
	}

	if r.SearchType != "" {
		set("search_type", r.SearchType)
	}

	if r.Size != 0 {
		set("size", strconv.Itoa(r.Size))
	}

	if r.Slices != "" {
		set("slices", r.Slices)
	}

	if len(r.Sort) > 0 {
		set("sort", strings.Join(r.Sort, ","))
	}

	if len(r.Stats) > 0 {
		set("stats", strings.Join(r.Stats, ","))
	}

	if r.TerminateAfter != 0 {
		set("terminate_after", strconv.Itoa(r.TerminateAfter))
	}

	if r.Version != nil {
		set("version", strconv.FormatBool(*r.Version))
	}

	if r.WaitForActiveShards != "" {
		set("wait_for_active_shards", r.WaitForActiveShards)
	}

	if r.WaitForCompletion != nil {
		set("wait_for_completion", strconv.FormatBool(*r.WaitForCompletion))
	}

	return params
}

// DeleteByQueryResp represents the response for the DeleteByQuery operation.
// The response body has a dynamic schema and is captured as raw JSON.
//
// Deletes documents matching the provided query.
//
// See: https://opensearch.org/docs/latest/api-reference/document-apis/delete-by-query/
type DeleteByQueryResp struct {
	Body     json.RawMessage `json:"-"`
	response *opensearch.Response
}

// UnmarshalJSON captures the raw response body.
//
//nolint:unparam // error return required by json.Unmarshaler; raw passthrough never fails
func (r *DeleteByQueryResp) UnmarshalJSON(b []byte) error {
	r.Body = append(r.Body[:0], b...)
	return nil
}

// MarshalJSON returns the raw response body for comparison testing.
//
//nolint:unparam // error return required by json.Marshaler; raw passthrough never fails
func (r DeleteByQueryResp) MarshalJSON() ([]byte, error) {
	if r.Body == nil {
		return build.NullJSON, nil
	}
	return r.Body, nil
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r DeleteByQueryResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r DeleteByQueryResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// DeleteByQueryRespBodyObject1 is a typed component of the delete_by_query operation.
type DeleteByQueryRespBodyObject1 struct {
	// The unique identifier of a task.
	Task *string `json:"task,omitempty"`
}

// DeleteByQueryRespBody is a discriminated union type (single-pass merge decode).
// Use Type() to determine which branch was decoded, then call
// the corresponding accessor.
type DeleteByQueryRespBody struct {
	typ   DeleteByQueryRespBodyType
	raw   json.RawMessage
	value any
}

// DeleteByQueryRespBodyType discriminates the branches of DeleteByQueryRespBody.
type DeleteByQueryRespBodyType int

const (
	DeleteByQueryRespBodyUnknownType DeleteByQueryRespBodyType = iota
	DeleteByQueryRespBodyBulkByScrollRespBaseType
	DeleteByQueryRespBodyObject1Type
)

// Type returns which union branch was populated during decoding.
// Returns DeleteByQueryRespBodyUnknownType if the value has not been decoded.
func (u *DeleteByQueryRespBody) Type() DeleteByQueryRespBodyType { return u.typ }

// RawJSON returns the union's JSON bytes. After decoding these are borrowed
// from the response buffer: valid only while the owning response value is
// reachable, must not be mutated, and must be copied if retained beyond it.
func (u *DeleteByQueryRespBody) RawJSON() json.RawMessage { return u.raw }

// SetRaw stages pre-encoded JSON for marshaling. MarshalJSON emits raw
// verbatim when no typed branch is set. Use the NewDeleteByQueryRespBodyFrom*
// constructors to populate a typed branch instead; SetRaw is the typed
// escape hatch for callers that already have wire-format bytes.
func (u *DeleteByQueryRespBody) SetRaw(raw json.RawMessage) {
	u.raw = raw
	u.value = nil
	u.typ = DeleteByQueryRespBodyUnknownType
}

// BulkByScrollRespBase returns the BulkByScrollRespBase branch value.
func (u *DeleteByQueryRespBody) BulkByScrollRespBase() BulkByScrollRespBase {
	if v, ok := u.value.(*BulkByScrollRespBase); ok {
		return *v
	}
	var zero BulkByScrollRespBase
	return zero
}

// NewDeleteByQueryRespBodyFromBulkByScrollRespBase returns a DeleteByQueryRespBody populated with v
// on the BulkByScrollRespBase branch.
func NewDeleteByQueryRespBodyFromBulkByScrollRespBase(v BulkByScrollRespBase) DeleteByQueryRespBody {
	return DeleteByQueryRespBody{
		typ:   DeleteByQueryRespBodyBulkByScrollRespBaseType,
		value: &v,
	}
}

// Object1 returns the DeleteByQueryRespBodyObject1 branch value.
func (u *DeleteByQueryRespBody) Object1() DeleteByQueryRespBodyObject1 {
	if v, ok := u.value.(*DeleteByQueryRespBodyObject1); ok {
		return *v
	}
	var zero DeleteByQueryRespBodyObject1
	return zero
}

// NewDeleteByQueryRespBodyFromObject1 returns a DeleteByQueryRespBody populated with v
// on the Object1 branch.
func NewDeleteByQueryRespBodyFromObject1(v DeleteByQueryRespBodyObject1) DeleteByQueryRespBody {
	return DeleteByQueryRespBody{
		typ:   DeleteByQueryRespBodyObject1Type,
		value: &v,
	}
}

func (u *DeleteByQueryRespBody) UnmarshalJSON(data []byte) error {
	u.raw = data
	u.value = nil
	u.typ = DeleteByQueryRespBodyUnknownType
	if len(data) == 0 || bytes.Equal(data, build.NullJSON) {
		return nil
	}
	// Single decode: embed the permissive (primary) branch and probe for the
	// discriminating keys of the other branches in one pass. encoding/json
	// populates the embedded primary directly; the probes only test presence.
	type merged struct {
		DeleteByQueryRespBodyObject1
		Disc0 json.RawMessage `json:"batches"`
	}
	var m merged
	if err := json.Unmarshal(data, &m); err != nil {
		return err
	}
	if len(m.Disc0) > 0 {
		var v BulkByScrollRespBase
		if err := json.Unmarshal(data, &v); err != nil {
			return err
		}
		u.typ = DeleteByQueryRespBodyBulkByScrollRespBaseType
		u.value = &v
		return nil
	}
	u.typ = DeleteByQueryRespBodyObject1Type
	u.value = &m.DeleteByQueryRespBodyObject1
	return nil
}

func (u DeleteByQueryRespBody) MarshalJSON() ([]byte, error) {
	if u.value != nil {
		return json.Marshal(u.value)
	}
	if len(u.raw) > 0 {
		return u.raw, nil
	}
	return build.NullJSON, nil
}

// DeleteByQueryBody is a typed component of the delete_by_query operation.
//
// The search definition using the Query DSL
type DeleteByQueryBody struct {
	// The maximum number of documents to delete.
	MaxDocs *int `json:"max_docs,omitempty"`

	Query *CommonQueryDSLQueryContainer `json:"query,omitempty"`

	// The configuration for a sliced scroll request.
	Slice *SlicedScroll `json:"slice,omitempty"`
}

// DeleteByQuery deletes documents matching the provided query.
//
// POST /{index}/_delete_by_query
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/document-apis/delete-by-query/
func (c Client) DeleteByQuery(ctx context.Context, req *DeleteByQueryReq) (*DeleteByQueryResp, error) {
	if req == nil {
		req = &DeleteByQueryReq{}
	}

	var (
		data DeleteByQueryResp
		err  error
	)
	if data.response, err = request(
		ctx,
		&c,
		http.MethodPost,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
