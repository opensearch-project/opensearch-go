// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strconv"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
)

// CatAliasesReq represents the request for the cat.aliases operation.
//
// Shows information about aliases currently configured to indexes, including filter and routing information.
//
// GET /_cat/aliases
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-aliases/
type CatAliasesReq struct {
	// Name specifies the list of path segments for the request URL.
	Name []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *CatAliasesParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r CatAliasesReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.CatAliasesPath{
		Name: r.Name,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	} else {
		// This operation defaults the format query param (the typed Resp
		// only decodes that format). The default lives in Params.get(), so
		// build it from a zero-value Params when the caller passes none.
		params = CatAliasesParams{}.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// CatAliasesParams represents query parameters for the CatAliasesReq.
type CatAliasesParams struct {
	TimeoutParams
	DebugParams
	ExpandWildcards []string

	// Whether to return information from the local node only instead of from
	// the cluster manager node.
	//
	// Default: false.
	Local *bool
}

func (r CatAliasesParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)
	if r.Format == "" {
		set("format", "json")
	}

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	if r.Local != nil {
		set("local", strconv.FormatBool(*r.Local))
	}

	return params
}

// CatAliasesResp represents the response for the CatAliases operation.
// The response body is a JSON array of records.
//
// Shows information about aliases currently configured to indexes, including filter and routing information.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-aliases/
type CatAliasesResp struct {
	Records  []CatAliasesRecord `json:"-"`
	response *opensearch.Response
}

// UnmarshalJSON decodes the response body as an array of records.
func (r *CatAliasesResp) UnmarshalJSON(b []byte) error {
	return json.Unmarshal(b, &r.Records)
}

// MarshalJSON encodes the response body for comparison testing.
func (r CatAliasesResp) MarshalJSON() ([]byte, error) {
	return json.Marshal(r.Records)
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r CatAliasesResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r CatAliasesResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// CatAliasesRecord is a typed component of the cat.aliases operation.
type CatAliasesRecord struct {
	// alias name
	Alias *string `json:"alias,omitempty"`

	// filter
	Filter *string `json:"filter,omitempty"`

	Index *string `json:"index,omitempty"`

	// write index
	IsWriteIndex *string `json:"is_write_index,omitempty"`

	// index routing
	RoutingIndex *string `json:"routing.index,omitempty"`

	// search routing
	RoutingSearch *string `json:"routing.search,omitempty"`
}

// Aliases shows information about aliases currently configured to indexes, including filter and routing information.
//
// GET /_cat/aliases
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-aliases/
func (c catClient) Aliases(ctx context.Context, req *CatAliasesReq) (*CatAliasesResp, error) {
	if req == nil {
		req = &CatAliasesReq{}
	}

	var (
		data CatAliasesResp
		err  error
	)
	if data.response, err = do(
		ctx,
		c.apiClient,
		http.MethodGet,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
