// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"io"
	"net/http"
	"strconv"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
)

// ClusterHealthReq represents the request for the cluster.health operation.
//
// Returns basic information about the health of the cluster.
//
// GET /_cluster/health
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-health/
type ClusterHealthReq struct {
	// Indices specifies the list of path segments for the request URL.
	Indices []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *ClusterHealthParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r ClusterHealthReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.ClusterHealthPath{
		Indices: r.Indices,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// ClusterHealthParams represents query parameters for the ClusterHealthReq.
type ClusterHealthParams struct {
	TimeoutParams
	DebugParams
	// The name of the awareness attribute for which to return the cluster
	// health status (for example, `zone`). Applicable only if `level` is set
	// to `awareness_attributes`.
	AwarenessAttribute string

	// Default: open.
	ExpandWildcards []string

	// Default: cluster.
	Level string

	// Whether to return information from the local node only instead of from
	// the cluster manager node.
	//
	// Default: false.
	Local *bool

	// Default: 0.
	WaitForActiveShards string

	WaitForEvents string

	// Whether to wait until there are no initializing shards in the cluster.
	//
	// Default: false.
	WaitForNoInitializingShards *bool

	// Whether to wait until there are no relocating shards in the cluster.
	//
	// Default: false.
	WaitForNoRelocatingShards *bool

	// Waits until the specified number of nodes (`N`) is available. Accepts
	// `>=N`, `<=N`, `>N`, and `<N`. You can also use `ge(N)`, `le(N)`,
	// `gt(N)`, and `lt(N)` notation.
	WaitForNodes string

	// Waits until the cluster health reaches the specified status or better.
	WaitForStatus string
}

func (r ClusterHealthParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.AwarenessAttribute != "" {
		set("awareness_attribute", r.AwarenessAttribute)
	}

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	if r.Level != "" {
		set("level", r.Level)
	}

	if r.Local != nil {
		set("local", strconv.FormatBool(*r.Local))
	}

	if r.WaitForActiveShards != "" {
		set("wait_for_active_shards", r.WaitForActiveShards)
	}

	if r.WaitForEvents != "" {
		set("wait_for_events", r.WaitForEvents)
	}

	if r.WaitForNoInitializingShards != nil {
		set("wait_for_no_initializing_shards", strconv.FormatBool(*r.WaitForNoInitializingShards))
	}

	if r.WaitForNoRelocatingShards != nil {
		set("wait_for_no_relocating_shards", strconv.FormatBool(*r.WaitForNoRelocatingShards))
	}

	if r.WaitForNodes != "" {
		set("wait_for_nodes", r.WaitForNodes)
	}

	if r.WaitForStatus != "" {
		set("wait_for_status", r.WaitForStatus)
	}

	return params
}

// ClusterHealthResp represents the response for the cluster.health operation.
//
// Returns basic information about the health of the cluster.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-health/
type ClusterHealthResp struct {
	// The number of active primary shards.
	ActivePrimaryShards int `json:"active_primary_shards"`

	// The total number of active primary and replica shards.
	ActiveShards int `json:"active_shards"`

	// The percentage value as a string.
	ActiveShardsPercent *string `json:"active_shards_percent,omitempty"`

	// The percentage value as a number.
	ActiveShardsPercentAsNumber float64 `json:"active_shards_percent_as_number"`

	// Cluster health information for each awareness attribute.
	//
	// Available: >= 2.5.0.
	AwarenessAttributes map[string]ClusterHealthAwarenessAttributeStats `json:"awareness_attributes,omitempty"`

	// The name of a resource or configuration element.
	ClusterName string `json:"cluster_name"`

	// The number of shards whose allocation has been delayed by the timeout
	// settings.
	DelayedUnassignedShards int `json:"delayed_unassigned_shards"`

	// True if the cluster-manager node has been discovered.
	//
	// Available: >= 2.0.0.
	DiscoveredClusterManager *bool `json:"discovered_cluster_manager,omitempty"`

	// True if the cluster-manager node has been discovered.
	//
	// Deprecated: since 2.0.0.
	DiscoveredMaster *bool `json:"discovered_master,omitempty"`

	Indices map[string]ClusterHealthIndexStats `json:"indices,omitempty"`

	// The number of shards that are under initialization.
	InitializingShards int `json:"initializing_shards"`

	// The number of nodes that are dedicated data nodes.
	NumberOfDataNodes int `json:"number_of_data_nodes"`

	// The number of unfinished fetches.
	NumberOfInFlightFetch int `json:"number_of_in_flight_fetch"`

	// The number of nodes within the cluster.
	NumberOfNodes int `json:"number_of_nodes"`

	// The number of cluster-level changes that have not yet been executed.
	NumberOfPendingTasks int `json:"number_of_pending_tasks"`

	// The number of shards that are under relocation.
	RelocatingShards int `json:"relocating_shards"`

	Status string `json:"status"`

	// A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s`
	// (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0`
	// without a unit and `-1` to indicate an unspecified value.
	TaskMaxWaitingInQueue *string `json:"task_max_waiting_in_queue,omitempty"`

	// The time unit for milliseconds.
	TaskMaxWaitingInQueueMillis int64 `json:"task_max_waiting_in_queue_millis"`

	// If `false` the response returned within the period of time that is
	// specified by the timeout parameter (`30s` by default).
	TimedOut bool `json:"timed_out"`

	// The number of shards that are not allocated.
	UnassignedShards int `json:"unassigned_shards"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r ClusterHealthResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r ClusterHealthResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// ClusterHealthAwarenessAttributeStats is a typed component of the cluster.health operation.
type ClusterHealthAwarenessAttributeStats struct {
	ActiveShards       *int `json:"active_shards,omitempty"`
	DataNodes          *int `json:"data_nodes,omitempty"`
	InitializingShards *int `json:"initializing_shards,omitempty"`
	RelocatingShards   *int `json:"relocating_shards,omitempty"`
	UnassignedShards   *int `json:"unassigned_shards,omitempty"`
	Weight             *int `json:"weight,omitempty"`
}

// ClusterHealthIndexStats is a typed component of the cluster.health operation.
type ClusterHealthIndexStats struct {
	ActivePrimaryShards int                                `json:"active_primary_shards"`
	ActiveShards        int                                `json:"active_shards"`
	InitializingShards  int                                `json:"initializing_shards"`
	NumberOfReplicas    int                                `json:"number_of_replicas"`
	NumberOfShards      int                                `json:"number_of_shards"`
	RelocatingShards    int                                `json:"relocating_shards"`
	Shards              map[string]ClusterHealthShardStats `json:"shards,omitempty"`
	Status              string                             `json:"status"`
	UnassignedShards    int                                `json:"unassigned_shards"`
}

// ClusterHealthShardStats is a typed component of the cluster.health operation.
type ClusterHealthShardStats struct {
	ActiveShards       int    `json:"active_shards"`
	InitializingShards int    `json:"initializing_shards"`
	PrimaryActive      bool   `json:"primary_active"`
	RelocatingShards   int    `json:"relocating_shards"`
	Status             string `json:"status"`
	UnassignedShards   int    `json:"unassigned_shards"`
}

// Health returns basic information about the health of the cluster.
//
// GET /_cluster/health
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-health/
func (c clusterClient) Health(ctx context.Context, req *ClusterHealthReq) (*ClusterHealthResp, error) {
	if req == nil {
		req = &ClusterHealthReq{}
	}

	var (
		data ClusterHealthResp
		err  error
	)
	if data.response, err = do(
		ctx,
		c.apiClient,
		http.MethodGet,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
