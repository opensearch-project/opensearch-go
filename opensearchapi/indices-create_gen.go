// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"encoding/json"
	"io"
	"net/http"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
)

// IndicesCreateReq represents the request for the indices.create operation.
//
// Creates an index with optional settings and mappings.
//
// PUT /{index}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/index-apis/create-index/
type IndicesCreateReq struct {
	// Index specifies the path segment for the request URL.
	Index string

	// Body specifies the typed request body. When non-nil, it is
	// marshaled to JSON for the request payload.
	Body *IndicesCreateBody

	// BodyReader provides an escape hatch for sending a raw request
	// body. It is used only when Body is nil.
	BodyReader io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *IndicesCreateParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesCreateReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.IndicesCreatePath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	var bodyReader io.Reader
	if r.Body != nil {
		bodyData, err := json.Marshal(r.Body)
		if err != nil {
			return nil, err
		}
		bodyReader = bytes.NewReader(bodyData)
	} else if r.BodyReader != nil {
		bodyReader = r.BodyReader
	}

	return build.Request(
		method,
		path,
		bodyReader,
		params,
		r.Header,
	)
}

// IndicesCreateParams represents query parameters for the IndicesCreateReq.
type IndicesCreateParams struct {
	TimeoutParams
	DebugParams
	// The number of shard copies that must be active before proceeding with
	// the operation. Set to `all` or any positive integer up to the total
	// number of shards in the index (`number_of_replicas+1`).
	WaitForActiveShards string
}

func (r IndicesCreateParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.WaitForActiveShards != "" {
		set("wait_for_active_shards", r.WaitForActiveShards)
	}

	return params
}

// IndicesCreateResp represents the response for the indices.create operation.
//
// Creates an index with optional settings and mappings.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/index-apis/create-index/
type IndicesCreateResp struct {
	Acknowledged       bool   `json:"acknowledged"`
	Index              string `json:"index"`
	ShardsAcknowledged bool   `json:"shards_acknowledged"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesCreateResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r IndicesCreateResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// IndicesCreateBody is a typed component of the indices.create operation.
//
// The configuration for the index (`settings` and `mappings`)
type IndicesCreateBody struct {
	// Aliases for the index.
	Aliases map[string]IndicesAlias `json:"aliases,omitempty"`

	Mappings *CommonMappingType `json:"mappings,omitempty"`

	// The configuration settings for an index.
	Settings *IndicesIndexSettings `json:"settings,omitempty"`
}

// Create creates an index with optional settings and mappings.
//
// PUT /{index}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/index-apis/create-index/
func (c IndicesClient) Create(ctx context.Context, req IndicesCreateReq) (*IndicesCreateResp, error) {
	var (
		data IndicesCreateResp
		err  error
	)
	if data.response, err = request(
		ctx,
		c.apiClient,
		http.MethodPut,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
