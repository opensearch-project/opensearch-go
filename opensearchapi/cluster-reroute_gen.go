// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strconv"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
)

// ClusterRerouteReq represents the request for the cluster.reroute operation.
//
// Allows to manually change the allocation of individual shards in the cluster.
//
// POST /_cluster/reroute
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type ClusterRerouteReq struct {

	// Body specifies the typed request body. When non-nil, it is
	// marshaled to JSON for the request payload.
	Body *ClusterRerouteBody

	// BodyReader provides an escape hatch for sending a raw request
	// body. It is used only when Body is nil.
	BodyReader io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *ClusterRerouteParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r ClusterRerouteReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.ClusterReroutePath{}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	var bodyReader io.Reader
	if r.Body != nil {
		bodyData, err := json.Marshal(r.Body)
		if err != nil {
			return nil, err
		}
		bodyReader = bytes.NewReader(bodyData)
	} else if r.BodyReader != nil {
		bodyReader = r.BodyReader
	}

	return build.Request(
		method,
		path,
		bodyReader,
		params,
		r.Header,
	)
}

// ClusterRerouteParams represents query parameters for the ClusterRerouteReq.
type ClusterRerouteParams struct {
	TimeoutParams
	DebugParams
	// When `true`, the request simulates the operation and returns the
	// resulting state.
	DryRun *bool

	// When `true`, the response contains an explanation of why reroute certain
	// commands can or cannot be executed.
	Explain *bool

	// Limits the information returned to the specified metrics.
	Metric []string

	// When `true`, retries shard allocation if it was blocked because of too
	// many subsequent failures.
	RetryFailed *bool
}

func (r ClusterRerouteParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.DryRun != nil {
		set("dry_run", strconv.FormatBool(*r.DryRun))
	}

	if r.Explain != nil {
		set("explain", strconv.FormatBool(*r.Explain))
	}

	if len(r.Metric) > 0 {
		set("metric", strings.Join(r.Metric, ","))
	}

	if r.RetryFailed != nil {
		set("retry_failed", strconv.FormatBool(*r.RetryFailed))
	}

	return params
}

// ClusterRerouteResp represents the response for the cluster.reroute operation.
//
// Allows to manually change the allocation of individual shards in the cluster.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type ClusterRerouteResp struct {
	AcknowledgedRespBase
	Explanations []ClusterRerouteExplanation `json:"explanations,omitempty"`

	// Shows the internal representation of the structure, which can differ
	// from the external representation.
	State json.RawMessage `json:"state"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r ClusterRerouteResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r ClusterRerouteResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// ClusterRerouteExplanation is a typed component of the cluster.reroute operation.
type ClusterRerouteExplanation struct {
	Command    string                   `json:"command"`
	Decisions  []ClusterRerouteDecision `json:"decisions"`
	Parameters ClusterRerouteParameters `json:"parameters"`
}

// ClusterRerouteDecision is a typed component of the cluster.reroute operation.
type ClusterRerouteDecision struct {
	Decider     string `json:"decider"`
	Decision    string `json:"decision"`
	Explanation string `json:"explanation"`
}

// ClusterRerouteParameters is a typed component of the cluster.reroute operation.
type ClusterRerouteParameters struct {
	AllowPrimary bool `json:"allow_primary"`

	// The name of the node.
	FromNode *string `json:"from_node,omitempty"`

	Index string `json:"index"`

	// The name of the node.
	Node string `json:"node"`

	Shard int `json:"shard"`

	// The name of the node.
	ToNode *string `json:"to_node,omitempty"`
}

// ClusterRerouteBody is a typed component of the cluster.reroute operation.
//
// The definition of `commands` to perform (`move`, `cancel`, `allocate`)
type ClusterRerouteBody struct {
	// Defines the reroute commands to perform, either `move`, `cancel`, or
	// `allocate`.
	Commands []ClusterRerouteCommand `json:"commands,omitempty"`
}

// ClusterRerouteCommand is a typed component of the cluster.reroute operation.
type ClusterRerouteCommand struct {
	// Allocate an empty primary shard to a node. Accepts `index` and `shard`
	// for index name and shard number, and `node` to allocate the shard to.
	// Using this command leads to a complete loss of all data that was indexed
	// into this shard, if it was previously started. If a node which has a
	// copy of the data rejoins the cluster later on, that data will be
	// deleted. To ensure that these implications are well-understood, this
	// command requires the flag `accept_data_loss` to be explicitly set to
	// true.
	AllocateEmptyPrimary *ClusterRerouteCommandAllocatePrimaryAction `json:"allocate_empty_primary,omitempty"`

	// Allocate an unassigned replica shard to a node. Accepts `index` and
	// `shard` for index name and shard number, and `node` to allocate the
	// shard to. Takes allocation deciders into account.
	AllocateReplica *ClusterRerouteCommandAllocateReplicaAction `json:"allocate_replica,omitempty"`

	// Allocate a primary shard to a node that holds a stale copy. Accepts
	// `index` and `shard` for index name and shard number, and `node` to
	// allocate the shard to. Using this command may lead to data loss for the
	// provided shard id. If a node which has the good copy of the data rejoins
	// the cluster later on, that data will be deleted or overwritten with the
	// data of the stale copy that was forcefully allocated with this command.
	// To ensure that these implications are well-understood, this command
	// requires the flag `accept_data_loss` to be explicitly set to true.
	AllocateStalePrimary *ClusterRerouteCommandAllocatePrimaryAction `json:"allocate_stale_primary,omitempty"`

	// Cancel allocation of a shard (or recovery). Accepts `index` and `shard`
	// for index name and shard number, and `node` for the node to cancel the
	// shard allocation on. This can be used to force resynchronization of
	// existing replicas from the primary shard by cancelling them and allowing
	// them to be reinitialized through the standard recovery process. By
	// default only replica shard allocations can be cancelled. If it is
	// necessary to cancel the allocation of a primary shard then the
	// `allow_primary` flag must also be included in the request.
	Cancel *ClusterRerouteCommandCancelAction `json:"cancel,omitempty"`

	// Move a started shard from one node to another node. Accepts `index` and
	// `shard` for index name and shard number, `from_node` for the node to
	// move the shard from, and `to_node` for the node to move the shard to.
	Move *ClusterRerouteCommandMoveAction `json:"move,omitempty"`
}

// ClusterRerouteCommandAllocatePrimaryAction is a typed component of the cluster.reroute operation.
//
// Allocate an empty primary shard to a node. Accepts `index` and `shard` for
// index name and shard number, and `node` to allocate the shard to. Using this
// command leads to a complete loss of all data that was indexed into this
// shard, if it was previously started. If a node which has a copy of the data
// rejoins the cluster later on, that data will be deleted. To ensure that these
// implications are well-understood, this command requires the flag
// `accept_data_loss` to be explicitly set to true.
type ClusterRerouteCommandAllocatePrimaryAction struct {
	// If a node which has a copy of the data rejoins the cluster later on,
	// that data will be deleted. To ensure that these implications are
	// well-understood, this command requires the flag `accept_data_loss` to be
	// explicitly set to `true`.
	AcceptDataLoss bool `json:"accept_data_loss"`

	Index string `json:"index"`
	Node  string `json:"node"`
	Shard int    `json:"shard"`
}

// ClusterRerouteCommandAllocateReplicaAction is a typed component of the cluster.reroute operation.
//
// Allocate an unassigned replica shard to a node. Accepts `index` and `shard` for
// index name and shard number, and `node` to allocate the shard to. Takes
// allocation deciders into account.
type ClusterRerouteCommandAllocateReplicaAction struct {
	Index string `json:"index"`
	Node  string `json:"node"`
	Shard int    `json:"shard"`
}

// ClusterRerouteCommandCancelAction is a typed component of the cluster.reroute operation.
//
// Cancel allocation of a shard (or recovery). Accepts `index` and `shard` for index
// name and shard number, and `node` for the node to cancel the shard allocation
// on. This can be used to force resynchronization of existing replicas from the
// primary shard by cancelling them and allowing them to be reinitialized
// through the standard recovery process. By default only replica shard
// allocations can be cancelled. If it is necessary to cancel the allocation of
// a primary shard then the `allow_primary` flag must also be included in the
// request.
type ClusterRerouteCommandCancelAction struct {
	AllowPrimary *bool  `json:"allow_primary,omitempty"`
	Index        string `json:"index"`
	Node         string `json:"node"`
	Shard        int    `json:"shard"`
}

// ClusterRerouteCommandMoveAction is a typed component of the cluster.reroute operation.
//
// Move a started shard from one node to another node. Accepts `index` and `shard`
// for index name and shard number, `from_node` for the node to move the shard
// from, and `to_node` for the node to move the shard to.
type ClusterRerouteCommandMoveAction struct {
	// The node to move the shard from
	FromNode string `json:"from_node"`

	Index string `json:"index"`
	Shard int    `json:"shard"`

	// The node to move the shard to
	ToNode string `json:"to_node"`
}

// Reroute allows to manually change the allocation of individual shards in the cluster.
//
// POST /_cluster/reroute
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
func (c clusterClient) Reroute(ctx context.Context, req *ClusterRerouteReq) (*ClusterRerouteResp, error) {
	if req == nil {
		req = &ClusterRerouteReq{}
	}

	var (
		data ClusterRerouteResp
		err  error
	)
	if data.response, err = request(
		ctx,
		c.apiClient,
		http.MethodPost,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
