// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"io"
	"net/http"
	"strconv"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
)

// TasksListReq represents the request for the tasks.list operation.
//
// Returns a list of tasks.
//
// GET /_tasks
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/tasks/
type TasksListReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *TasksListParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r TasksListReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.TasksListPath{}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// TasksListParams represents query parameters for the TasksListReq.
type TasksListParams struct {
	TimeoutParams
	DebugParams
	// A comma-separated list of actions that should be returned. Keep empty to
	// return all.
	Actions []string

	// When `true`, the response includes detailed information about shard
	// recoveries.
	//
	// Default: false.
	Detailed *bool

	// Groups tasks by parent/child relationships or nodes.
	//
	// Default: nodes.
	GroupBy string

	// A comma-separated list of node IDs or names used to limit the returned
	// information. Use `_local` to return information from the node you're
	// connecting to, specify the node name to get information from a specific
	// node, or keep the parameter empty to get information from all nodes.
	Nodes []string

	// Returns tasks with a specified parent task ID (`node_id:task_number`).
	// Keep empty or set to -1 to return all.
	ParentTaskID string

	// Waits for the matching task to complete. When `true`, the request is
	// blocked until the task has completed.
	//
	// Default: false.
	WaitForCompletion *bool
}

func (r TasksListParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if len(r.Actions) > 0 {
		set("actions", strings.Join(r.Actions, ","))
	}

	if r.Detailed != nil {
		set("detailed", strconv.FormatBool(*r.Detailed))
	}

	if r.GroupBy != "" {
		set("group_by", r.GroupBy)
	}

	if len(r.Nodes) > 0 {
		set("nodes", strings.Join(r.Nodes, ","))
	}

	if r.ParentTaskID != "" {
		set("parent_task_id", r.ParentTaskID)
	}

	if r.WaitForCompletion != nil {
		set("wait_for_completion", strconv.FormatBool(*r.WaitForCompletion))
	}

	return params
}

// TasksListResp represents the response for the tasks.list operation.
//
// Returns a list of tasks.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/tasks/
type TasksListResp struct {
	NodeFailures []ErrorCause `json:"node_failures,omitempty"`

	// Task information grouped by node, if `group_by` was set to `node` (the
	// default).
	Nodes map[string]TasksTaskExecutingNode `json:"nodes,omitempty"`

	TaskFailures []TaskFailure   `json:"task_failures,omitempty"`
	Tasks        *TasksTaskInfos `json:"tasks,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r TasksListResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r TasksListResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// List returns a list of tasks.
//
// GET /_tasks
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/tasks/
func (c TasksClient) List(ctx context.Context, req *TasksListReq) (*TasksListResp, error) {
	if req == nil {
		req = &TasksListReq{}
	}

	var (
		data TasksListResp
		err  error
	)
	if data.response, err = request(
		ctx,
		c.apiClient,
		http.MethodGet,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
