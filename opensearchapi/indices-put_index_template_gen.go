// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strconv"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
)

// IndicesPutIndexTemplateReq represents the request for the indices.put_index_template operation.
//
// Creates or updates an index template.
//
// Path: /_index_template/{name}
//
// Methods: POST, PUT
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/index-templates/
type IndicesPutIndexTemplateReq struct {
	// Name specifies the path segment for the request URL.
	Name string

	// Body specifies the typed request body. When non-nil, it is
	// marshaled to JSON for the request payload.
	Body *IndicesPutIndexTemplateBody

	// BodyReader provides an escape hatch for sending a raw request
	// body. It is used only when Body is nil.
	BodyReader io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *IndicesPutIndexTemplateParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesPutIndexTemplateReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.IndicesPutIndexTemplatePath{
		Name: r.Name,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	var bodyReader io.Reader
	if r.Body != nil {
		bodyData, err := json.Marshal(r.Body)
		if err != nil {
			return nil, err
		}
		bodyReader = bytes.NewReader(bodyData)
	} else if r.BodyReader != nil {
		bodyReader = r.BodyReader
	}

	return build.Request(
		method,
		path,
		bodyReader,
		params,
		r.Header,
	)
}

// IndicesPutIndexTemplateParams represents query parameters for the IndicesPutIndexTemplateReq.
type IndicesPutIndexTemplateParams struct {
	TimeoutParams
	DebugParams
	// User defined reason for creating/updating the index template.
	//
	// Default: false.
	Cause string

	// If `true`, this request cannot replace or update existing index
	// templates.
	//
	// Default: false.
	Create *bool
}

func (r IndicesPutIndexTemplateParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.Cause != "" {
		set("cause", r.Cause)
	}

	if r.Create != nil {
		set("create", strconv.FormatBool(*r.Create))
	}

	return params
}

// IndicesPutIndexTemplateResp represents the response for the indices.put_index_template operation.
//
// Creates or updates an index template.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/index-templates/
type IndicesPutIndexTemplateResp struct {
	// For a successful response, this value is always true. On failure, an
	// exception is returned instead.
	Acknowledged bool `json:"acknowledged"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesPutIndexTemplateResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r IndicesPutIndexTemplateResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// IndicesPutIndexTemplateBody is a typed component of the indices.put_index_template operation.
//
// The template definition
type IndicesPutIndexTemplateBody struct {
	// The custom metadata attached to a resource.
	Meta map[string]json.RawMessage `json:"_meta,omitempty"`

	// An ordered list of component template names. Component templates are
	// merged in the order specified, meaning that the last component template
	// specified has the highest precedence.
	ComposedOf []string `json:"composed_of,omitempty"`

	// The data stream configuration for an index template.
	DataStream *IndicesIndexTemplateDataStreamConfiguration `json:"data_stream,omitempty"`

	// A comma-separated list of data streams, indexes, and aliases used to
	// limit the request. Supports wildcards (`*`). To target all data streams
	// and indexes, omit this parameter or use `*` or `_all`.
	IndexPatterns []string `json:"index_patterns,omitempty"`

	// Priority to determine index template precedence when a new data stream
	// or index is created. The index template with the highest priority is
	// chosen. If no priority is specified the template is treated as though it
	// is of priority 0 (lowest priority). This number is not automatically
	// generated by OpenSearch.
	Priority *int `json:"priority,omitempty"`

	Template *IndicesPutIndexTemplateIndexTemplateMapping `json:"template,omitempty"`
	Version  *int64                                       `json:"version,omitempty"`
}

// IndicesPutIndexTemplateIndexTemplateMapping is a typed component of the indices.put_index_template operation.
type IndicesPutIndexTemplateIndexTemplateMapping struct {
	// The aliases to add. If the index template includes a `data_stream`
	// object, these are data stream aliases. Otherwise, these are index
	// aliases. Data stream aliases ignore the `index_routing`, `routing`, and
	// `search_routing` options.
	Aliases map[string]IndicesAlias `json:"aliases,omitempty"`

	Mappings *CommonMappingType `json:"mappings,omitempty"`

	// The configuration settings for an index.
	Settings *IndicesIndexSettings `json:"settings,omitempty"`
}

// PutIndexTemplate creates or updates an index template.
//
// Path: /_index_template/{name}
//
// Methods: POST, PUT
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/index-templates/
func (c indicesClient) PutIndexTemplate(ctx context.Context, req IndicesPutIndexTemplateReq) (*IndicesPutIndexTemplateResp, error) {
	var (
		data IndicesPutIndexTemplateResp
		err  error
	)
	if data.response, err = request(
		ctx,
		c.apiClient,
		http.MethodPost,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
