// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"io"
	"net/http"
	"strconv"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
)

// ClusterPendingTasksReq represents the request for the cluster.pending_tasks operation.
//
// Returns a list of pending cluster-level tasks, such as index creation, mapping updates,
// or new allocations.
//
// GET /_cluster/pending_tasks
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type ClusterPendingTasksReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *ClusterPendingTasksParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r ClusterPendingTasksReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.ClusterPendingTasksPath{}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// ClusterPendingTasksParams represents query parameters for the ClusterPendingTasksReq.
type ClusterPendingTasksParams struct {
	TimeoutParams
	DebugParams
	// When `true`, the request retrieves information from the local node only.
	// When `false`, information is retrieved from the cluster manager node.
	//
	// Default: false.
	Local *bool
}

func (r ClusterPendingTasksParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.Local != nil {
		set("local", strconv.FormatBool(*r.Local))
	}

	return params
}

// ClusterPendingTasksResp represents the response for the cluster.pending_tasks operation.
//
// Returns a list of pending cluster-level tasks, such as index creation, mapping updates,
// or new allocations.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type ClusterPendingTasksResp struct {
	Tasks []ClusterPendingTasksPendingTask `json:"tasks"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r ClusterPendingTasksResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r ClusterPendingTasksResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// ClusterPendingTasksPendingTask is a typed component of the cluster.pending_tasks operation.
type ClusterPendingTasksPendingTask struct {
	// Indicates whether the pending tasks are currently executing or not.
	Executing bool `json:"executing"`

	// The number that represents when the task has been inserted into the task
	// queue.
	InsertOrder int `json:"insert_order"`

	// The priority of the pending task. The valid priorities in descending
	// priority order are: `IMMEDIATE` > `URGENT` > `HIGH` > `NORMAL` > `LOW` >
	// `LANGUID`.
	Priority string `json:"priority"`

	// A general description of the cluster task that may include a reason and
	// origin.
	Source string `json:"source"`

	// A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s`
	// (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0`
	// without a unit and `-1` to indicate an unspecified value.
	TimeInExecution *string `json:"time_in_execution,omitempty"`

	// The time unit for milliseconds.
	TimeInExecutionMillis *int64 `json:"time_in_execution_millis,omitempty"`

	// A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s`
	// (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0`
	// without a unit and `-1` to indicate an unspecified value.
	TimeInQueue *string `json:"time_in_queue,omitempty"`

	// The time unit for milliseconds.
	TimeInQueueMillis int64 `json:"time_in_queue_millis"`
}

// PendingTasks returns a list of pending cluster-level tasks, such as index creation, mapping updates,.
//
// or new allocations.
//
// GET /_cluster/pending_tasks
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
func (c ClusterClient) PendingTasks(ctx context.Context, req *ClusterPendingTasksReq) (*ClusterPendingTasksResp, error) {
	if req == nil {
		req = &ClusterPendingTasksReq{}
	}

	var (
		data ClusterPendingTasksResp
		err  error
	)
	if data.response, err = request(
		ctx,
		c.apiClient,
		http.MethodGet,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
