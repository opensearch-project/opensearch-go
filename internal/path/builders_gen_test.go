// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package path //nolint:testpackage // generated table-driven tests share the production package; the .Fields literals reference unqualified type names

import (
	"net/url"
	"strings"
	"testing"

	"github.com/stretchr/testify/require"
)

// assertPathRoundTrip verifies that a built path is a valid URI path
// with no structural anomalies and that every segment round-trips
// cleanly through url.PathUnescape -> url.PathEscape. Catches:
//
//   - Double-encoding (a literal "%XX" surviving into the decoded
//     form, meaning we encoded an already-encoded value).
//   - Empty segments ("//"), the original bug class behind #650.
//   - URL-parse failures, which would indicate malformed encoding.
//   - Routing metacharacters ("/", "?", "#") that escaped into the
//     decoded form, the path-injection class the encoder is meant to
//     prevent.
//
// Multi-value segments are written as a comma-joined list of
// independently-escaped sub-values; the round-trip splits on "," before
// decoding so encoded commas in user values stay distinguishable from
// list separators.
func assertPathRoundTrip(t *testing.T, raw string) {
	t.Helper()
	if raw == "" || raw == "/" {
		return
	}

	require.Truef(t, strings.HasPrefix(raw, "/"), "path %q must be rooted at /", raw)
	require.NotContainsf(t, raw, "//", "path %q must not contain empty segments (#650)", raw)

	if _, err := url.ParseRequestURI(raw); err != nil {
		t.Fatalf("path %q must be a valid request-URI: %v", raw, err)
	}

	for _, seg := range strings.Split(strings.TrimPrefix(raw, "/"), "/") {
		require.NotEmptyf(t, seg, "path %q must not contain empty segments", raw)

		parts := strings.Split(seg, ",")
		encoded := make([]string, len(parts))
		for i, p := range parts {
			decoded, err := url.PathUnescape(p)
			require.NoErrorf(t, err, "sub-segment %q must decode cleanly", p)

			// Double-encoding check: a clean decoded value should not
			// contain any further percent-triplets. If a second
			// PathUnescape would change the value, decoded still
			// carries encoded data, which means the writer escaped an
			// already-escaped input.
			if again, err := url.PathUnescape(decoded); err == nil {
				require.Equalf(t, decoded, again,
					"sub-segment %q decodes to %q which still decodes further to %q (double-encoded)",
					p, decoded, again)
			}

			// Routing metacharacters must not survive into the decoded
			// form unencoded. If they did, a caller could inject path
			// segments or query strings via a field value (#650).
			for _, bad := range []string{"/", "?", "#"} {
				require.NotContainsf(t, decoded, bad,
					"sub-segment %q decodes to %q which contains routing metacharacter %q",
					p, decoded, bad)
			}

			encoded[i] = strings.ReplaceAll(url.PathEscape(decoded), "/", "%2F")
		}
		require.Equalf(t, seg, strings.Join(encoded, ","), "segment %q must round-trip stably", seg)
	}
}

func TestAsynchronousSearchDeletePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     AsynchronousSearchDeletePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: AsynchronousSearchDeletePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: AsynchronousSearchDeletePath{ID: "test-id"}, wantPath: "/_plugins/_asynchronous_search/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestAsynchronousSearchGetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     AsynchronousSearchGetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: AsynchronousSearchGetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: AsynchronousSearchGetPath{ID: "test-id"}, wantPath: "/_plugins/_asynchronous_search/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestAsynchronousSearchSearchPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     AsynchronousSearchSearchPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: AsynchronousSearchSearchPath{}, wantPath: "/_plugins/_asynchronous_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestAsynchronousSearchStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     AsynchronousSearchStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: AsynchronousSearchStatsPath{}, wantPath: "/_plugins/_asynchronous_search/stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestBulkPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     BulkPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: BulkPath{}, wantPath: "/_bulk", wantErr: false},
		{name: "all fields", path: BulkPath{Index: "test-index"}, wantPath: "/test-index/_bulk", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestBulkStreamPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     BulkStreamPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: BulkStreamPath{}, wantPath: "/_bulk/stream", wantErr: false},
		{name: "all fields", path: BulkStreamPath{Index: "test-index"}, wantPath: "/test-index/_bulk/stream", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatAliasesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatAliasesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatAliasesPath{}, wantPath: "/_cat/aliases", wantErr: false},
		{name: "all fields", path: CatAliasesPath{Name: []string{"test-name"}}, wantPath: "/_cat/aliases/test-name", wantErr: false},
		{name: "Name multi-value", path: CatAliasesPath{Name: []string{"a", "b", "c"}}, wantPath: "/_cat/aliases/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatAllPITSegmentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatAllPITSegmentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatAllPITSegmentsPath{}, wantPath: "/_cat/pit_segments/_all", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatAllocationPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatAllocationPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatAllocationPath{}, wantPath: "/_cat/allocation", wantErr: false},
		{name: "all fields", path: CatAllocationPath{NodeID: []string{"test-nodeid"}}, wantPath: "/_cat/allocation/test-nodeid", wantErr: false},
		{name: "NodeID multi-value", path: CatAllocationPath{NodeID: []string{"a", "b", "c"}}, wantPath: "/_cat/allocation/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatClusterManagerPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatClusterManagerPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatClusterManagerPath{}, wantPath: "/_cat/cluster_manager", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatCountPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatCountPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatCountPath{}, wantPath: "/_cat/count", wantErr: false},
		{name: "all fields", path: CatCountPath{Index: []string{"test-index"}}, wantPath: "/_cat/count/test-index", wantErr: false},
		{name: "Index multi-value", path: CatCountPath{Index: []string{"a", "b", "c"}}, wantPath: "/_cat/count/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatFielddataPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatFielddataPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatFielddataPath{}, wantPath: "/_cat/fielddata", wantErr: false},
		{name: "all fields", path: CatFielddataPath{Fields: []string{"test-fields"}}, wantPath: "/_cat/fielddata/test-fields", wantErr: false},
		{name: "Fields multi-value", path: CatFielddataPath{Fields: []string{"a", "b", "c"}}, wantPath: "/_cat/fielddata/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatHealthPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatHealthPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatHealthPath{}, wantPath: "/_cat/health", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatHelpPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatHelpPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatHelpPath{}, wantPath: "/_cat", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatIndicesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatIndicesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatIndicesPath{}, wantPath: "/_cat/indices", wantErr: false},
		{name: "all fields", path: CatIndicesPath{Index: []string{"test-index"}}, wantPath: "/_cat/indices/test-index", wantErr: false},
		{name: "Index multi-value", path: CatIndicesPath{Index: []string{"a", "b", "c"}}, wantPath: "/_cat/indices/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatMasterPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatMasterPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatMasterPath{}, wantPath: "/_cat/master", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatNodeattrsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatNodeattrsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatNodeattrsPath{}, wantPath: "/_cat/nodeattrs", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatNodesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatNodesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatNodesPath{}, wantPath: "/_cat/nodes", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatPendingTasksPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatPendingTasksPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatPendingTasksPath{}, wantPath: "/_cat/pending_tasks", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatPITSegmentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatPITSegmentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatPITSegmentsPath{}, wantPath: "/_cat/pit_segments", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatPluginsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatPluginsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatPluginsPath{}, wantPath: "/_cat/plugins", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatRecoveryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatRecoveryPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatRecoveryPath{}, wantPath: "/_cat/recovery", wantErr: false},
		{name: "all fields", path: CatRecoveryPath{Index: []string{"test-index"}}, wantPath: "/_cat/recovery/test-index", wantErr: false},
		{name: "Index multi-value", path: CatRecoveryPath{Index: []string{"a", "b", "c"}}, wantPath: "/_cat/recovery/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatRepositoriesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatRepositoriesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatRepositoriesPath{}, wantPath: "/_cat/repositories", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatSegmentReplicationPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatSegmentReplicationPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatSegmentReplicationPath{}, wantPath: "/_cat/segment_replication", wantErr: false},
		{name: "all fields", path: CatSegmentReplicationPath{Index: []string{"test-index"}}, wantPath: "/_cat/segment_replication/test-index", wantErr: false},
		{name: "Index multi-value", path: CatSegmentReplicationPath{Index: []string{"a", "b", "c"}}, wantPath: "/_cat/segment_replication/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatSegmentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatSegmentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatSegmentsPath{}, wantPath: "/_cat/segments", wantErr: false},
		{name: "all fields", path: CatSegmentsPath{Index: []string{"test-index"}}, wantPath: "/_cat/segments/test-index", wantErr: false},
		{name: "Index multi-value", path: CatSegmentsPath{Index: []string{"a", "b", "c"}}, wantPath: "/_cat/segments/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatShardsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatShardsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatShardsPath{}, wantPath: "/_cat/shards", wantErr: false},
		{name: "all fields", path: CatShardsPath{Index: []string{"test-index"}}, wantPath: "/_cat/shards/test-index", wantErr: false},
		{name: "Index multi-value", path: CatShardsPath{Index: []string{"a", "b", "c"}}, wantPath: "/_cat/shards/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatSnapshotsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatSnapshotsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatSnapshotsPath{}, wantPath: "/_cat/snapshots", wantErr: false},
		{name: "all fields", path: CatSnapshotsPath{Repository: []string{"test-repository"}}, wantPath: "/_cat/snapshots/test-repository", wantErr: false},
		{name: "Repository multi-value", path: CatSnapshotsPath{Repository: []string{"a", "b", "c"}}, wantPath: "/_cat/snapshots/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatTasksPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatTasksPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatTasksPath{}, wantPath: "/_cat/tasks", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatTemplatesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatTemplatesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatTemplatesPath{}, wantPath: "/_cat/templates", wantErr: false},
		{name: "all fields", path: CatTemplatesPath{Name: "test-name"}, wantPath: "/_cat/templates/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCatThreadPoolPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CatThreadPoolPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CatThreadPoolPath{}, wantPath: "/_cat/thread_pool", wantErr: false},
		{name: "all fields", path: CatThreadPoolPath{ThreadPoolPatterns: []string{"test-threadpoolpatterns"}}, wantPath: "/_cat/thread_pool/test-threadpoolpatterns", wantErr: false},
		{name: "ThreadPoolPatterns multi-value", path: CatThreadPoolPath{ThreadPoolPatterns: []string{"a", "b", "c"}}, wantPath: "/_cat/thread_pool/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClearScrollPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClearScrollPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClearScrollPath{}, wantPath: "/_search/scroll", wantErr: false},
		{name: "all fields", path: ClearScrollPath{ScrollID: []string{"test-scrollid"}}, wantPath: "/_search/scroll/test-scrollid", wantErr: false},
		{name: "ScrollID multi-value", path: ClearScrollPath{ScrollID: []string{"a", "b", "c"}}, wantPath: "/_search/scroll/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterAllocationExplainPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterAllocationExplainPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterAllocationExplainPath{}, wantPath: "/_cluster/allocation/explain", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterDeleteComponentTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterDeleteComponentTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ClusterDeleteComponentTemplatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ClusterDeleteComponentTemplatePath{Name: "test-name"}, wantPath: "/_component_template/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterDeleteDecommissionAwarenessPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterDeleteDecommissionAwarenessPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterDeleteDecommissionAwarenessPath{}, wantPath: "/_cluster/decommission/awareness", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterDeleteVotingConfigExclusionsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterDeleteVotingConfigExclusionsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterDeleteVotingConfigExclusionsPath{}, wantPath: "/_cluster/voting_config_exclusions", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterDeleteWeightedRoutingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterDeleteWeightedRoutingPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterDeleteWeightedRoutingPath{}, wantPath: "/_cluster/routing/awareness/weights", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterExistsComponentTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterExistsComponentTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ClusterExistsComponentTemplatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ClusterExistsComponentTemplatePath{Name: "test-name"}, wantPath: "/_component_template/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterGetComponentTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterGetComponentTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterGetComponentTemplatePath{}, wantPath: "/_component_template", wantErr: false},
		{name: "all fields", path: ClusterGetComponentTemplatePath{Name: "test-name"}, wantPath: "/_component_template/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterGetDecommissionAwarenessPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterGetDecommissionAwarenessPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ClusterGetDecommissionAwarenessPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ClusterGetDecommissionAwarenessPath{AwarenessAttributeName: "test-awarenessattributename"}, wantPath: "/_cluster/decommission/awareness/test-awarenessattributename/_status", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterGetSettingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterGetSettingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterGetSettingsPath{}, wantPath: "/_cluster/settings", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterGetWeightedRoutingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterGetWeightedRoutingPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ClusterGetWeightedRoutingPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ClusterGetWeightedRoutingPath{Attribute: "test-attribute"}, wantPath: "/_cluster/routing/awareness/test-attribute/weights", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterHealthPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterHealthPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterHealthPath{}, wantPath: "/_cluster/health", wantErr: false},
		{name: "all fields", path: ClusterHealthPath{Index: []string{"test-index"}}, wantPath: "/_cluster/health/test-index", wantErr: false},
		{name: "Index multi-value", path: ClusterHealthPath{Index: []string{"a", "b", "c"}}, wantPath: "/_cluster/health/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterPendingTasksPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterPendingTasksPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterPendingTasksPath{}, wantPath: "/_cluster/pending_tasks", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterPostVotingConfigExclusionsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterPostVotingConfigExclusionsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterPostVotingConfigExclusionsPath{}, wantPath: "/_cluster/voting_config_exclusions", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterPutComponentTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterPutComponentTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ClusterPutComponentTemplatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ClusterPutComponentTemplatePath{Name: "test-name"}, wantPath: "/_component_template/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterPutDecommissionAwarenessPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterPutDecommissionAwarenessPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ClusterPutDecommissionAwarenessPath{}, wantPath: "", wantErr: true},
		{name: "missing AwarenessAttributeName", path: ClusterPutDecommissionAwarenessPath{AwarenessAttributeValue: "req-awarenessattributevalue"}, wantPath: "", wantErr: true},
		{name: "missing AwarenessAttributeValue", path: ClusterPutDecommissionAwarenessPath{AwarenessAttributeName: "req-awarenessattributename"}, wantPath: "", wantErr: true},
		{name: "all fields", path: ClusterPutDecommissionAwarenessPath{AwarenessAttributeName: "test-awarenessattributename", AwarenessAttributeValue: "test-awarenessattributevalue"}, wantPath: "/_cluster/decommission/awareness/test-awarenessattributename/test-awarenessattributevalue", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterPutSettingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterPutSettingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterPutSettingsPath{}, wantPath: "/_cluster/settings", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterPutWeightedRoutingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterPutWeightedRoutingPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ClusterPutWeightedRoutingPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ClusterPutWeightedRoutingPath{Attribute: "test-attribute"}, wantPath: "/_cluster/routing/awareness/test-attribute/weights", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterRemoteInfoPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterRemoteInfoPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterRemoteInfoPath{}, wantPath: "/_remote/info", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterReroutePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterReroutePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterReroutePath{}, wantPath: "/_cluster/reroute", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterStatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterStatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterStatePath{}, wantPath: "/_cluster/state", wantErr: false},
		{name: "all fields", path: ClusterStatePath{Metric: []string{"test-metric"}, Index: []string{"test-index"}}, wantPath: "/_cluster/state/test-metric/test-index", wantErr: false},
		{name: "only Metric", path: ClusterStatePath{Metric: []string{"val-metric"}}, wantPath: "/_cluster/state/val-metric", wantErr: false},
		{name: "only Index", path: ClusterStatePath{Index: []string{"val-index"}}, wantPath: "", wantErr: true},
		{name: "Metric multi-value", path: ClusterStatePath{Metric: []string{"a", "b", "c"}}, wantPath: "/_cluster/state/a,b,c", wantErr: false},
		{name: "Index multi-value", path: ClusterStatePath{Index: []string{"a", "b", "c"}}, wantPath: "", wantErr: true},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestClusterStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ClusterStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ClusterStatsPath{}, wantPath: "/_cluster/stats", wantErr: false},
		{name: "all fields", path: ClusterStatsPath{Metric: []string{"test-metric"}, IndexMetric: []string{"test-indexmetric"}, NodeID: []string{"test-nodeid"}}, wantPath: "/_cluster/stats/test-metric/test-indexmetric/nodes/test-nodeid", wantErr: false},
		{name: "only Metric", path: ClusterStatsPath{Metric: []string{"val-metric"}}, wantPath: "", wantErr: true},
		{name: "only IndexMetric", path: ClusterStatsPath{IndexMetric: []string{"val-indexmetric"}}, wantPath: "", wantErr: true},
		{name: "only NodeID", path: ClusterStatsPath{NodeID: []string{"val-nodeid"}}, wantPath: "/_cluster/stats/nodes/val-nodeid", wantErr: false},
		{name: "Metric multi-value", path: ClusterStatsPath{Metric: []string{"a", "b", "c"}}, wantPath: "", wantErr: true},
		{name: "IndexMetric multi-value", path: ClusterStatsPath{IndexMetric: []string{"a", "b", "c"}}, wantPath: "", wantErr: true},
		{name: "NodeID multi-value", path: ClusterStatsPath{NodeID: []string{"a", "b", "c"}}, wantPath: "/_cluster/stats/nodes/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCountPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CountPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: CountPath{}, wantPath: "/_count", wantErr: false},
		{name: "all fields", path: CountPath{Index: []string{"test-index"}}, wantPath: "/test-index/_count", wantErr: false},
		{name: "Index multi-value", path: CountPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_count", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCreatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CreatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: CreatePath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: CreatePath{ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: CreatePath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: CreatePath{Index: "test-index", ID: "test-id"}, wantPath: "/test-index/_create/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestCreatePITPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     CreatePITPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: CreatePITPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: CreatePITPath{Index: []string{"test-index"}}, wantPath: "/test-index/_search/point_in_time", wantErr: false},
		{name: "Index multi-value", path: CreatePITPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_search/point_in_time", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestDanglingIndicesDeleteDanglingIndexPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     DanglingIndicesDeleteDanglingIndexPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: DanglingIndicesDeleteDanglingIndexPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: DanglingIndicesDeleteDanglingIndexPath{IndexUUID: "test-indexuuid"}, wantPath: "/_dangling/test-indexuuid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestDanglingIndicesImportDanglingIndexPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     DanglingIndicesImportDanglingIndexPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: DanglingIndicesImportDanglingIndexPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: DanglingIndicesImportDanglingIndexPath{IndexUUID: "test-indexuuid"}, wantPath: "/_dangling/test-indexuuid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestDanglingIndicesListDanglingIndicesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     DanglingIndicesListDanglingIndicesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: DanglingIndicesListDanglingIndicesPath{}, wantPath: "/_dangling", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestDeletePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     DeletePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: DeletePath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: DeletePath{ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: DeletePath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: DeletePath{Index: "test-index", ID: "test-id"}, wantPath: "/test-index/_doc/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestDeleteAllPitsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     DeleteAllPitsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: DeleteAllPitsPath{}, wantPath: "/_search/point_in_time/_all", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestDeleteByQueryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     DeleteByQueryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: DeleteByQueryPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: DeleteByQueryPath{Index: []string{"test-index"}}, wantPath: "/test-index/_delete_by_query", wantErr: false},
		{name: "Index multi-value", path: DeleteByQueryPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_delete_by_query", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestDeleteByQueryRethrottlePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     DeleteByQueryRethrottlePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: DeleteByQueryRethrottlePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: DeleteByQueryRethrottlePath{TaskID: "test-taskid"}, wantPath: "/_delete_by_query/test-taskid/_rethrottle", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestDeletePITPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     DeletePITPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: DeletePITPath{}, wantPath: "/_search/point_in_time", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestDeleteScriptPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     DeleteScriptPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: DeleteScriptPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: DeleteScriptPath{ID: "test-id"}, wantPath: "/_scripts/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestExistsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ExistsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ExistsPath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: ExistsPath{ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: ExistsPath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: ExistsPath{Index: "test-index", ID: "test-id"}, wantPath: "/test-index/_doc/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestExistsSourcePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ExistsSourcePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ExistsSourcePath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: ExistsSourcePath{ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: ExistsSourcePath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: ExistsSourcePath{Index: "test-index", ID: "test-id"}, wantPath: "/test-index/_source/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestExplainPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ExplainPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ExplainPath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: ExplainPath{ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: ExplainPath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: ExplainPath{Index: "test-index", ID: "test-id"}, wantPath: "/test-index/_explain/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFieldCapsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FieldCapsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: FieldCapsPath{}, wantPath: "/_field_caps", wantErr: false},
		{name: "all fields", path: FieldCapsPath{Index: []string{"test-index"}}, wantPath: "/test-index/_field_caps", wantErr: false},
		{name: "Index multi-value", path: FieldCapsPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_field_caps", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFlowFrameworkCreatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FlowFrameworkCreatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: FlowFrameworkCreatePath{}, wantPath: "/_plugins/_flow_framework/workflow", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFlowFrameworkDeletePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FlowFrameworkDeletePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: FlowFrameworkDeletePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: FlowFrameworkDeletePath{WorkflowID: "test-workflowid"}, wantPath: "/_plugins/_flow_framework/workflow/test-workflowid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFlowFrameworkDeprovisionPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FlowFrameworkDeprovisionPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: FlowFrameworkDeprovisionPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: FlowFrameworkDeprovisionPath{WorkflowID: "test-workflowid"}, wantPath: "/_plugins/_flow_framework/workflow/test-workflowid/_deprovision", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFlowFrameworkGetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FlowFrameworkGetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: FlowFrameworkGetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: FlowFrameworkGetPath{WorkflowID: "test-workflowid"}, wantPath: "/_plugins/_flow_framework/workflow/test-workflowid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFlowFrameworkGetStatusPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FlowFrameworkGetStatusPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: FlowFrameworkGetStatusPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: FlowFrameworkGetStatusPath{WorkflowID: "test-workflowid"}, wantPath: "/_plugins/_flow_framework/workflow/test-workflowid/_status", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFlowFrameworkGetStepsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FlowFrameworkGetStepsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: FlowFrameworkGetStepsPath{}, wantPath: "/_plugins/_flow_framework/workflow/_steps", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFlowFrameworkProvisionPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FlowFrameworkProvisionPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: FlowFrameworkProvisionPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: FlowFrameworkProvisionPath{WorkflowID: "test-workflowid"}, wantPath: "/_plugins/_flow_framework/workflow/test-workflowid/_provision", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFlowFrameworkSearchPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FlowFrameworkSearchPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: FlowFrameworkSearchPath{}, wantPath: "/_plugins/_flow_framework/workflow/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFlowFrameworkSearchStatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FlowFrameworkSearchStatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: FlowFrameworkSearchStatePath{}, wantPath: "/_plugins/_flow_framework/workflow/state/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestFlowFrameworkUpdatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     FlowFrameworkUpdatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: FlowFrameworkUpdatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: FlowFrameworkUpdatePath{WorkflowID: "test-workflowid"}, wantPath: "/_plugins/_flow_framework/workflow/test-workflowid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGeospatialDeleteIp2geoDatasourcePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GeospatialDeleteIp2geoDatasourcePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: GeospatialDeleteIp2geoDatasourcePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: GeospatialDeleteIp2geoDatasourcePath{Name: "test-name"}, wantPath: "/_plugins/geospatial/ip2geo/datasource/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGeospatialGeojsonUploadPostPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GeospatialGeojsonUploadPostPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: GeospatialGeojsonUploadPostPath{}, wantPath: "/_plugins/geospatial/geojson/_upload", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGeospatialGeojsonUploadPutPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GeospatialGeojsonUploadPutPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: GeospatialGeojsonUploadPutPath{}, wantPath: "/_plugins/geospatial/geojson/_upload", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGeospatialGetIp2geoDatasourcePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GeospatialGetIp2geoDatasourcePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: GeospatialGetIp2geoDatasourcePath{}, wantPath: "/_plugins/geospatial/ip2geo/datasource", wantErr: false},
		{name: "all fields", path: GeospatialGetIp2geoDatasourcePath{Name: []string{"test-name"}}, wantPath: "/_plugins/geospatial/ip2geo/datasource/test-name", wantErr: false},
		{name: "Name multi-value", path: GeospatialGetIp2geoDatasourcePath{Name: []string{"a", "b", "c"}}, wantPath: "/_plugins/geospatial/ip2geo/datasource/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGeospatialGetUploadStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GeospatialGetUploadStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: GeospatialGetUploadStatsPath{}, wantPath: "/_plugins/geospatial/_upload/stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGeospatialPutIp2geoDatasourcePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GeospatialPutIp2geoDatasourcePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: GeospatialPutIp2geoDatasourcePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: GeospatialPutIp2geoDatasourcePath{Name: "test-name"}, wantPath: "/_plugins/geospatial/ip2geo/datasource/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGeospatialPutIp2geoDatasourceSettingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GeospatialPutIp2geoDatasourceSettingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: GeospatialPutIp2geoDatasourceSettingsPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: GeospatialPutIp2geoDatasourceSettingsPath{Name: "test-name"}, wantPath: "/_plugins/geospatial/ip2geo/datasource/test-name/_settings", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: GetPath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: GetPath{ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: GetPath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: GetPath{Index: "test-index", ID: "test-id"}, wantPath: "/test-index/_doc/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGetAllPitsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GetAllPitsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: GetAllPitsPath{}, wantPath: "/_search/point_in_time/_all", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGetScriptPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GetScriptPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: GetScriptPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: GetScriptPath{ID: "test-id"}, wantPath: "/_scripts/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGetScriptContextPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GetScriptContextPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: GetScriptContextPath{}, wantPath: "/_script_context", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGetScriptLanguagesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GetScriptLanguagesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: GetScriptLanguagesPath{}, wantPath: "/_script_language", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestGetSourcePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     GetSourcePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: GetSourcePath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: GetSourcePath{ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: GetSourcePath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: GetSourcePath{Index: "test-index", ID: "test-id"}, wantPath: "/test-index/_source/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndexPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndexPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndexPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndexPath{Index: "test-index", ID: "test-id"}, wantPath: "/test-index/_doc/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesAddBlockPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesAddBlockPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesAddBlockPath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: IndicesAddBlockPath{Block: "req-block"}, wantPath: "", wantErr: true},
		{name: "missing Block", path: IndicesAddBlockPath{Index: []string{"req-index"}}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesAddBlockPath{Index: []string{"test-index"}, Block: "test-block"}, wantPath: "/test-index/_block/test-block", wantErr: false},
		{name: "Index multi-value", path: IndicesAddBlockPath{Index: []string{"a", "b", "c"}, Block: "req-block"}, wantPath: "/a,b,c/_block/req-block", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesAnalyzePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesAnalyzePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesAnalyzePath{}, wantPath: "/_analyze", wantErr: false},
		{name: "all fields", path: IndicesAnalyzePath{Index: "test-index"}, wantPath: "/test-index/_analyze", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesClearCachePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesClearCachePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesClearCachePath{}, wantPath: "/_cache/clear", wantErr: false},
		{name: "all fields", path: IndicesClearCachePath{Index: []string{"test-index"}}, wantPath: "/test-index/_cache/clear", wantErr: false},
		{name: "Index multi-value", path: IndicesClearCachePath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_cache/clear", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesClonePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesClonePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesClonePath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: IndicesClonePath{Target: "req-target"}, wantPath: "", wantErr: true},
		{name: "missing Target", path: IndicesClonePath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesClonePath{Index: "test-index", Target: "test-target"}, wantPath: "/test-index/_clone/test-target", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesClosePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesClosePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesClosePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesClosePath{Index: []string{"test-index"}}, wantPath: "/test-index/_close", wantErr: false},
		{name: "Index multi-value", path: IndicesClosePath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_close", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesCreatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesCreatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesCreatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesCreatePath{Index: "test-index"}, wantPath: "/test-index", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesCreateDataStreamPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesCreateDataStreamPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesCreateDataStreamPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesCreateDataStreamPath{Name: "test-name"}, wantPath: "/_data_stream/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesDataStreamsStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesDataStreamsStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesDataStreamsStatsPath{}, wantPath: "/_data_stream/_stats", wantErr: false},
		{name: "all fields", path: IndicesDataStreamsStatsPath{Name: []string{"test-name"}}, wantPath: "/_data_stream/test-name/_stats", wantErr: false},
		{name: "Name multi-value", path: IndicesDataStreamsStatsPath{Name: []string{"a", "b", "c"}}, wantPath: "/_data_stream/a,b,c/_stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesDeletePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesDeletePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesDeletePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesDeletePath{Index: []string{"test-index"}}, wantPath: "/test-index", wantErr: false},
		{name: "Index multi-value", path: IndicesDeletePath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesDeleteAliasPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesDeleteAliasPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesDeleteAliasPath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: IndicesDeleteAliasPath{Name: []string{"req-name"}}, wantPath: "", wantErr: true},
		{name: "missing Name", path: IndicesDeleteAliasPath{Index: []string{"req-index"}}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesDeleteAliasPath{Index: []string{"test-index"}, Name: []string{"test-name"}}, wantPath: "/test-index/_alias/test-name", wantErr: false},
		{name: "Index multi-value", path: IndicesDeleteAliasPath{Index: []string{"a", "b", "c"}, Name: []string{"req-name"}}, wantPath: "/a,b,c/_alias/req-name", wantErr: false},
		{name: "Name multi-value", path: IndicesDeleteAliasPath{Index: []string{"req-index"}, Name: []string{"a", "b", "c"}}, wantPath: "/req-index/_alias/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesDeleteDataStreamPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesDeleteDataStreamPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesDeleteDataStreamPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesDeleteDataStreamPath{Name: []string{"test-name"}}, wantPath: "/_data_stream/test-name", wantErr: false},
		{name: "Name multi-value", path: IndicesDeleteDataStreamPath{Name: []string{"a", "b", "c"}}, wantPath: "/_data_stream/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesDeleteIndexTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesDeleteIndexTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesDeleteIndexTemplatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesDeleteIndexTemplatePath{Name: "test-name"}, wantPath: "/_index_template/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesDeleteTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesDeleteTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesDeleteTemplatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesDeleteTemplatePath{Name: "test-name"}, wantPath: "/_template/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesExistsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesExistsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesExistsPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesExistsPath{Index: []string{"test-index"}}, wantPath: "/test-index", wantErr: false},
		{name: "Index multi-value", path: IndicesExistsPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesExistsAliasPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesExistsAliasPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesExistsAliasPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesExistsAliasPath{Index: []string{"test-index"}, Name: []string{"test-name"}}, wantPath: "/test-index/_alias/test-name", wantErr: false},
		{name: "Index multi-value", path: IndicesExistsAliasPath{Index: []string{"a", "b", "c"}, Name: []string{"req-name"}}, wantPath: "/a,b,c/_alias/req-name", wantErr: false},
		{name: "Name multi-value", path: IndicesExistsAliasPath{Name: []string{"a", "b", "c"}}, wantPath: "/_alias/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesExistsIndexTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesExistsIndexTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesExistsIndexTemplatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesExistsIndexTemplatePath{Name: "test-name"}, wantPath: "/_index_template/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesExistsTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesExistsTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesExistsTemplatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesExistsTemplatePath{Name: []string{"test-name"}}, wantPath: "/_template/test-name", wantErr: false},
		{name: "Name multi-value", path: IndicesExistsTemplatePath{Name: []string{"a", "b", "c"}}, wantPath: "/_template/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesFlushPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesFlushPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesFlushPath{}, wantPath: "/_flush", wantErr: false},
		{name: "all fields", path: IndicesFlushPath{Index: []string{"test-index"}}, wantPath: "/test-index/_flush", wantErr: false},
		{name: "Index multi-value", path: IndicesFlushPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_flush", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesForcemergePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesForcemergePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesForcemergePath{}, wantPath: "/_forcemerge", wantErr: false},
		{name: "all fields", path: IndicesForcemergePath{Index: []string{"test-index"}}, wantPath: "/test-index/_forcemerge", wantErr: false},
		{name: "Index multi-value", path: IndicesForcemergePath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_forcemerge", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesGetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesGetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesGetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesGetPath{Index: []string{"test-index"}}, wantPath: "/test-index", wantErr: false},
		{name: "Index multi-value", path: IndicesGetPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesGetAliasPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesGetAliasPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesGetAliasPath{}, wantPath: "/_alias", wantErr: false},
		{name: "all fields", path: IndicesGetAliasPath{Index: []string{"test-index"}, Name: []string{"test-name"}}, wantPath: "/test-index/_alias/test-name", wantErr: false},
		{name: "only Index", path: IndicesGetAliasPath{Index: []string{"val-index"}}, wantPath: "/val-index/_alias", wantErr: false},
		{name: "only Name", path: IndicesGetAliasPath{Name: []string{"val-name"}}, wantPath: "/_alias/val-name", wantErr: false},
		{name: "Index multi-value", path: IndicesGetAliasPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_alias", wantErr: false},
		{name: "Name multi-value", path: IndicesGetAliasPath{Name: []string{"a", "b", "c"}}, wantPath: "/_alias/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesGetDataStreamPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesGetDataStreamPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesGetDataStreamPath{}, wantPath: "/_data_stream", wantErr: false},
		{name: "all fields", path: IndicesGetDataStreamPath{Name: []string{"test-name"}}, wantPath: "/_data_stream/test-name", wantErr: false},
		{name: "Name multi-value", path: IndicesGetDataStreamPath{Name: []string{"a", "b", "c"}}, wantPath: "/_data_stream/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesGetFieldMappingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesGetFieldMappingPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesGetFieldMappingPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesGetFieldMappingPath{Index: []string{"test-index"}, Fields: []string{"test-fields"}}, wantPath: "/test-index/_mapping/field/test-fields", wantErr: false},
		{name: "Index multi-value", path: IndicesGetFieldMappingPath{Index: []string{"a", "b", "c"}, Fields: []string{"req-fields"}}, wantPath: "/a,b,c/_mapping/field/req-fields", wantErr: false},
		{name: "Fields multi-value", path: IndicesGetFieldMappingPath{Fields: []string{"a", "b", "c"}}, wantPath: "/_mapping/field/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesGetIndexTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesGetIndexTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesGetIndexTemplatePath{}, wantPath: "/_index_template", wantErr: false},
		{name: "all fields", path: IndicesGetIndexTemplatePath{Name: "test-name"}, wantPath: "/_index_template/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesGetMappingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesGetMappingPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesGetMappingPath{}, wantPath: "/_mapping", wantErr: false},
		{name: "all fields", path: IndicesGetMappingPath{Index: []string{"test-index"}}, wantPath: "/test-index/_mapping", wantErr: false},
		{name: "Index multi-value", path: IndicesGetMappingPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_mapping", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesGetSettingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesGetSettingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesGetSettingsPath{}, wantPath: "/_settings", wantErr: false},
		{name: "all fields", path: IndicesGetSettingsPath{Index: []string{"test-index"}, Name: []string{"test-name"}}, wantPath: "/test-index/_settings/test-name", wantErr: false},
		{name: "only Index", path: IndicesGetSettingsPath{Index: []string{"val-index"}}, wantPath: "/val-index/_settings", wantErr: false},
		{name: "only Name", path: IndicesGetSettingsPath{Name: []string{"val-name"}}, wantPath: "/_settings/val-name", wantErr: false},
		{name: "Index multi-value", path: IndicesGetSettingsPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_settings", wantErr: false},
		{name: "Name multi-value", path: IndicesGetSettingsPath{Name: []string{"a", "b", "c"}}, wantPath: "/_settings/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesGetTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesGetTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesGetTemplatePath{}, wantPath: "/_template", wantErr: false},
		{name: "all fields", path: IndicesGetTemplatePath{Name: []string{"test-name"}}, wantPath: "/_template/test-name", wantErr: false},
		{name: "Name multi-value", path: IndicesGetTemplatePath{Name: []string{"a", "b", "c"}}, wantPath: "/_template/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesGetUpgradePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesGetUpgradePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesGetUpgradePath{}, wantPath: "/_upgrade", wantErr: false},
		{name: "all fields", path: IndicesGetUpgradePath{Index: []string{"test-index"}}, wantPath: "/test-index/_upgrade", wantErr: false},
		{name: "Index multi-value", path: IndicesGetUpgradePath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_upgrade", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesOpenPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesOpenPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesOpenPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesOpenPath{Index: []string{"test-index"}}, wantPath: "/test-index/_open", wantErr: false},
		{name: "Index multi-value", path: IndicesOpenPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_open", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesPutAliasPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesPutAliasPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesPutAliasPath{}, wantPath: "/_alias", wantErr: false},
		{name: "all fields", path: IndicesPutAliasPath{Index: []string{"test-index"}, Name: "test-name"}, wantPath: "/test-index/_alias/test-name", wantErr: false},
		{name: "only Index", path: IndicesPutAliasPath{Index: []string{"val-index"}}, wantPath: "/val-index/_alias", wantErr: false},
		{name: "only Name", path: IndicesPutAliasPath{Name: "val-name"}, wantPath: "/_alias/val-name", wantErr: false},
		{name: "Index multi-value", path: IndicesPutAliasPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_alias", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesPutIndexTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesPutIndexTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesPutIndexTemplatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesPutIndexTemplatePath{Name: "test-name"}, wantPath: "/_index_template/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesPutMappingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesPutMappingPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesPutMappingPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesPutMappingPath{Index: []string{"test-index"}}, wantPath: "/test-index/_mapping", wantErr: false},
		{name: "Index multi-value", path: IndicesPutMappingPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_mapping", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesPutSettingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesPutSettingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesPutSettingsPath{}, wantPath: "/_settings", wantErr: false},
		{name: "all fields", path: IndicesPutSettingsPath{Index: []string{"test-index"}}, wantPath: "/test-index/_settings", wantErr: false},
		{name: "Index multi-value", path: IndicesPutSettingsPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_settings", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesPutTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesPutTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesPutTemplatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesPutTemplatePath{Name: "test-name"}, wantPath: "/_template/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesRecoveryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesRecoveryPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesRecoveryPath{}, wantPath: "/_recovery", wantErr: false},
		{name: "all fields", path: IndicesRecoveryPath{Index: []string{"test-index"}}, wantPath: "/test-index/_recovery", wantErr: false},
		{name: "Index multi-value", path: IndicesRecoveryPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_recovery", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesRefreshPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesRefreshPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesRefreshPath{}, wantPath: "/_refresh", wantErr: false},
		{name: "all fields", path: IndicesRefreshPath{Index: []string{"test-index"}}, wantPath: "/test-index/_refresh", wantErr: false},
		{name: "Index multi-value", path: IndicesRefreshPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_refresh", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesResolveIndexPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesResolveIndexPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesResolveIndexPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesResolveIndexPath{Name: []string{"test-name"}}, wantPath: "/_resolve/index/test-name", wantErr: false},
		{name: "Name multi-value", path: IndicesResolveIndexPath{Name: []string{"a", "b", "c"}}, wantPath: "/_resolve/index/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesRolloverPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesRolloverPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesRolloverPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesRolloverPath{Alias: "test-alias", NewIndex: "test-newindex"}, wantPath: "/test-alias/_rollover/test-newindex", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesSegmentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesSegmentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesSegmentsPath{}, wantPath: "/_segments", wantErr: false},
		{name: "all fields", path: IndicesSegmentsPath{Index: []string{"test-index"}}, wantPath: "/test-index/_segments", wantErr: false},
		{name: "Index multi-value", path: IndicesSegmentsPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_segments", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesShardStoresPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesShardStoresPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesShardStoresPath{}, wantPath: "/_shard_stores", wantErr: false},
		{name: "all fields", path: IndicesShardStoresPath{Index: []string{"test-index"}}, wantPath: "/test-index/_shard_stores", wantErr: false},
		{name: "Index multi-value", path: IndicesShardStoresPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_shard_stores", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesShrinkPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesShrinkPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesShrinkPath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: IndicesShrinkPath{Target: "req-target"}, wantPath: "", wantErr: true},
		{name: "missing Target", path: IndicesShrinkPath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesShrinkPath{Index: "test-index", Target: "test-target"}, wantPath: "/test-index/_shrink/test-target", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesSimulateIndexTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesSimulateIndexTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesSimulateIndexTemplatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesSimulateIndexTemplatePath{Name: "test-name"}, wantPath: "/_index_template/_simulate_index/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesSimulateTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesSimulateTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesSimulateTemplatePath{}, wantPath: "/_index_template/_simulate", wantErr: false},
		{name: "all fields", path: IndicesSimulateTemplatePath{Name: "test-name"}, wantPath: "/_index_template/_simulate/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesSplitPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesSplitPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IndicesSplitPath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: IndicesSplitPath{Target: "req-target"}, wantPath: "", wantErr: true},
		{name: "missing Target", path: IndicesSplitPath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: IndicesSplitPath{Index: "test-index", Target: "test-target"}, wantPath: "/test-index/_split/test-target", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesStatsPath{}, wantPath: "/_stats", wantErr: false},
		{name: "all fields", path: IndicesStatsPath{Index: []string{"test-index"}, Metric: []string{"test-metric"}}, wantPath: "/test-index/_stats/test-metric", wantErr: false},
		{name: "only Index", path: IndicesStatsPath{Index: []string{"val-index"}}, wantPath: "/val-index/_stats", wantErr: false},
		{name: "only Metric", path: IndicesStatsPath{Metric: []string{"val-metric"}}, wantPath: "/_stats/val-metric", wantErr: false},
		{name: "Index multi-value", path: IndicesStatsPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_stats", wantErr: false},
		{name: "Metric multi-value", path: IndicesStatsPath{Metric: []string{"a", "b", "c"}}, wantPath: "/_stats/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesUpdateAliasesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesUpdateAliasesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesUpdateAliasesPath{}, wantPath: "/_aliases", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesUpgradePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesUpgradePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesUpgradePath{}, wantPath: "/_upgrade", wantErr: false},
		{name: "all fields", path: IndicesUpgradePath{Index: []string{"test-index"}}, wantPath: "/test-index/_upgrade", wantErr: false},
		{name: "Index multi-value", path: IndicesUpgradePath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_upgrade", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIndicesValidateQueryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IndicesValidateQueryPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IndicesValidateQueryPath{}, wantPath: "/_validate/query", wantErr: false},
		{name: "all fields", path: IndicesValidateQueryPath{Index: []string{"test-index"}}, wantPath: "/test-index/_validate/query", wantErr: false},
		{name: "Index multi-value", path: IndicesValidateQueryPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_validate/query", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestInfoPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     InfoPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: InfoPath{}, wantPath: "/", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIngestDeletePipelinePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IngestDeletePipelinePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IngestDeletePipelinePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IngestDeletePipelinePath{ID: "test-id"}, wantPath: "/_ingest/pipeline/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIngestGetPipelinePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IngestGetPipelinePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IngestGetPipelinePath{}, wantPath: "/_ingest/pipeline", wantErr: false},
		{name: "all fields", path: IngestGetPipelinePath{ID: "test-id"}, wantPath: "/_ingest/pipeline/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIngestProcessorGrokPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IngestProcessorGrokPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IngestProcessorGrokPath{}, wantPath: "/_ingest/processor/grok", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIngestPutPipelinePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IngestPutPipelinePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IngestPutPipelinePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IngestPutPipelinePath{ID: "test-id"}, wantPath: "/_ingest/pipeline/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIngestSimulatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IngestSimulatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IngestSimulatePath{}, wantPath: "/_ingest/pipeline/_simulate", wantErr: false},
		{name: "all fields", path: IngestSimulatePath{ID: "test-id"}, wantPath: "/_ingest/pipeline/test-id/_simulate", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIngestionGetStatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IngestionGetStatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IngestionGetStatePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IngestionGetStatePath{Index: "test-index"}, wantPath: "/test-index/ingestion/_state", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIngestionPausePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IngestionPausePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IngestionPausePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IngestionPausePath{Index: "test-index"}, wantPath: "/test-index/ingestion/_pause", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIngestionResumePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IngestionResumePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IngestionResumePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IngestionResumePath{Index: "test-index"}, wantPath: "/test-index/ingestion/_resume", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestInsightsTopQueriesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     InsightsTopQueriesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: InsightsTopQueriesPath{}, wantPath: "/_insights/top_queries", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmAddPolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmAddPolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IsmAddPolicyPath{}, wantPath: "/_plugins/_ism/add", wantErr: false},
		{name: "all fields", path: IsmAddPolicyPath{Index: []string{"test-index"}}, wantPath: "/_plugins/_ism/add/test-index", wantErr: false},
		{name: "Index multi-value", path: IsmAddPolicyPath{Index: []string{"a", "b", "c"}}, wantPath: "/_plugins/_ism/add/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmChangePolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmChangePolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IsmChangePolicyPath{}, wantPath: "/_plugins/_ism/change_policy", wantErr: false},
		{name: "all fields", path: IsmChangePolicyPath{Index: []string{"test-index"}}, wantPath: "/_plugins/_ism/change_policy/test-index", wantErr: false},
		{name: "Index multi-value", path: IsmChangePolicyPath{Index: []string{"a", "b", "c"}}, wantPath: "/_plugins/_ism/change_policy/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmDeletePolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmDeletePolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IsmDeletePolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IsmDeletePolicyPath{PolicyID: "test-policyid"}, wantPath: "/_plugins/_ism/policies/test-policyid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmExistsPolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmExistsPolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IsmExistsPolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IsmExistsPolicyPath{PolicyID: "test-policyid"}, wantPath: "/_plugins/_ism/policies/test-policyid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmExplainPolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmExplainPolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IsmExplainPolicyPath{}, wantPath: "/_plugins/_ism/explain", wantErr: false},
		{name: "all fields", path: IsmExplainPolicyPath{Index: []string{"test-index"}}, wantPath: "/_plugins/_ism/explain/test-index", wantErr: false},
		{name: "Index multi-value", path: IsmExplainPolicyPath{Index: []string{"a", "b", "c"}}, wantPath: "/_plugins/_ism/explain/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmGetPoliciesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmGetPoliciesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IsmGetPoliciesPath{}, wantPath: "/_plugins/_ism/policies", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmGetPolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmGetPolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IsmGetPolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IsmGetPolicyPath{PolicyID: "test-policyid"}, wantPath: "/_plugins/_ism/policies/test-policyid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmPutPoliciesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmPutPoliciesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IsmPutPoliciesPath{}, wantPath: "/_plugins/_ism/policies", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmPutPolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmPutPolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IsmPutPolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IsmPutPolicyPath{PolicyID: "test-policyid"}, wantPath: "/_plugins/_ism/policies/test-policyid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmRefreshSearchAnalyzersPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmRefreshSearchAnalyzersPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: IsmRefreshSearchAnalyzersPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: IsmRefreshSearchAnalyzersPath{Index: []string{"test-index"}}, wantPath: "/_plugins/_refresh_search_analyzers/test-index", wantErr: false},
		{name: "Index multi-value", path: IsmRefreshSearchAnalyzersPath{Index: []string{"a", "b", "c"}}, wantPath: "/_plugins/_refresh_search_analyzers/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmRemovePolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmRemovePolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IsmRemovePolicyPath{}, wantPath: "/_plugins/_ism/remove", wantErr: false},
		{name: "all fields", path: IsmRemovePolicyPath{Index: []string{"test-index"}}, wantPath: "/_plugins/_ism/remove/test-index", wantErr: false},
		{name: "Index multi-value", path: IsmRemovePolicyPath{Index: []string{"a", "b", "c"}}, wantPath: "/_plugins/_ism/remove/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestIsmRetryIndexPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     IsmRetryIndexPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: IsmRetryIndexPath{}, wantPath: "/_plugins/_ism/retry", wantErr: false},
		{name: "all fields", path: IsmRetryIndexPath{Index: []string{"test-index"}}, wantPath: "/_plugins/_ism/retry/test-index", wantErr: false},
		{name: "Index multi-value", path: IsmRetryIndexPath{Index: []string{"a", "b", "c"}}, wantPath: "/_plugins/_ism/retry/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestKnnDeleteModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     KnnDeleteModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: KnnDeleteModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: KnnDeleteModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_knn/models/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestKnnGetModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     KnnGetModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: KnnGetModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: KnnGetModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_knn/models/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestKnnSearchModelsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     KnnSearchModelsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: KnnSearchModelsPath{}, wantPath: "/_plugins/_knn/models/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestKnnStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     KnnStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: KnnStatsPath{}, wantPath: "/_plugins/_knn/stats", wantErr: false},
		{name: "all fields", path: KnnStatsPath{NodeID: []string{"test-nodeid"}, Stat: []string{"test-stat"}}, wantPath: "/_plugins/_knn/test-nodeid/stats/test-stat", wantErr: false},
		{name: "only NodeID", path: KnnStatsPath{NodeID: []string{"val-nodeid"}}, wantPath: "/_plugins/_knn/val-nodeid/stats", wantErr: false},
		{name: "only Stat", path: KnnStatsPath{Stat: []string{"val-stat"}}, wantPath: "/_plugins/_knn/stats/val-stat", wantErr: false},
		{name: "NodeID multi-value", path: KnnStatsPath{NodeID: []string{"a", "b", "c"}}, wantPath: "/_plugins/_knn/a,b,c/stats", wantErr: false},
		{name: "Stat multi-value", path: KnnStatsPath{Stat: []string{"a", "b", "c"}}, wantPath: "/_plugins/_knn/stats/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestKnnTrainModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     KnnTrainModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: KnnTrainModelPath{}, wantPath: "/_plugins/_knn/models/_train", wantErr: false},
		{name: "all fields", path: KnnTrainModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_knn/models/test-modelid/_train", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestKnnWarmupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     KnnWarmupPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: KnnWarmupPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: KnnWarmupPath{Index: []string{"test-index"}}, wantPath: "/_plugins/_knn/warmup/test-index", wantErr: false},
		{name: "Index multi-value", path: KnnWarmupPath{Index: []string{"a", "b", "c"}}, wantPath: "/_plugins/_knn/warmup/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestListHelpPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ListHelpPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ListHelpPath{}, wantPath: "/_list", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestListIndicesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ListIndicesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ListIndicesPath{}, wantPath: "/_list/indices", wantErr: false},
		{name: "all fields", path: ListIndicesPath{Index: []string{"test-index"}}, wantPath: "/_list/indices/test-index", wantErr: false},
		{name: "Index multi-value", path: ListIndicesPath{Index: []string{"a", "b", "c"}}, wantPath: "/_list/indices/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestListShardsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ListShardsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ListShardsPath{}, wantPath: "/_list/shards", wantErr: false},
		{name: "all fields", path: ListShardsPath{Index: []string{"test-index"}}, wantPath: "/_list/shards/test-index", wantErr: false},
		{name: "Index multi-value", path: ListShardsPath{Index: []string{"a", "b", "c"}}, wantPath: "/_list/shards/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrAddFeaturesToSetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrAddFeaturesToSetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrAddFeaturesToSetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrAddFeaturesToSetPath{Store: "test-store", Name: "test-name"}, wantPath: "/_ltr/test-store/_featureset/test-name/_addfeatures", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrAddFeaturesToSetByQueryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrAddFeaturesToSetByQueryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrAddFeaturesToSetByQueryPath{}, wantPath: "", wantErr: true},
		{name: "missing Name", path: LtrAddFeaturesToSetByQueryPath{Query: "req-query"}, wantPath: "", wantErr: true},
		{name: "missing Query", path: LtrAddFeaturesToSetByQueryPath{Name: "req-name"}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrAddFeaturesToSetByQueryPath{Store: "test-store", Name: "test-name", Query: "test-query"}, wantPath: "/_ltr/test-store/_featureset/test-name/_addfeatures/test-query", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrCacheStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrCacheStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: LtrCacheStatsPath{}, wantPath: "/_ltr/_cachestats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrClearCachePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrClearCachePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: LtrClearCachePath{}, wantPath: "/_ltr/_clearcache", wantErr: false},
		{name: "all fields", path: LtrClearCachePath{Store: "test-store"}, wantPath: "/_ltr/test-store/_clearcache", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrCreateDefaultStorePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrCreateDefaultStorePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: LtrCreateDefaultStorePath{}, wantPath: "/_ltr", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrCreateFeaturePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrCreateFeaturePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrCreateFeaturePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrCreateFeaturePath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_feature/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrCreateFeaturesetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrCreateFeaturesetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrCreateFeaturesetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrCreateFeaturesetPath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_featureset/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrCreateModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrCreateModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrCreateModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrCreateModelPath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_model/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrCreateModelFromSetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrCreateModelFromSetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrCreateModelFromSetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrCreateModelFromSetPath{Store: "test-store", Name: "test-name"}, wantPath: "/_ltr/test-store/_featureset/test-name/_createmodel", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrCreateStorePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrCreateStorePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrCreateStorePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrCreateStorePath{Store: "test-store"}, wantPath: "/_ltr/test-store", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrDeleteDefaultStorePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrDeleteDefaultStorePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: LtrDeleteDefaultStorePath{}, wantPath: "/_ltr", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrDeleteFeaturePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrDeleteFeaturePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrDeleteFeaturePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrDeleteFeaturePath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_feature/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrDeleteFeaturesetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrDeleteFeaturesetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrDeleteFeaturesetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrDeleteFeaturesetPath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_featureset/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrDeleteModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrDeleteModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrDeleteModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrDeleteModelPath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_model/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrDeleteStorePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrDeleteStorePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrDeleteStorePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrDeleteStorePath{Store: "test-store"}, wantPath: "/_ltr/test-store", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrGetFeaturePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrGetFeaturePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrGetFeaturePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrGetFeaturePath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_feature/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrGetFeaturesetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrGetFeaturesetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrGetFeaturesetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrGetFeaturesetPath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_featureset/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrGetModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrGetModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrGetModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrGetModelPath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_model/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrGetStorePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrGetStorePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrGetStorePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrGetStorePath{Store: "test-store"}, wantPath: "/_ltr/test-store", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrListStoresPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrListStoresPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: LtrListStoresPath{}, wantPath: "/_ltr", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrSearchFeaturesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrSearchFeaturesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: LtrSearchFeaturesPath{}, wantPath: "/_ltr/_feature", wantErr: false},
		{name: "all fields", path: LtrSearchFeaturesPath{Store: "test-store"}, wantPath: "/_ltr/test-store/_feature", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrSearchFeaturesetsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrSearchFeaturesetsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: LtrSearchFeaturesetsPath{}, wantPath: "/_ltr/_featureset", wantErr: false},
		{name: "all fields", path: LtrSearchFeaturesetsPath{Store: "test-store"}, wantPath: "/_ltr/test-store/_featureset", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrSearchModelsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrSearchModelsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: LtrSearchModelsPath{}, wantPath: "/_ltr/_model", wantErr: false},
		{name: "all fields", path: LtrSearchModelsPath{Store: "test-store"}, wantPath: "/_ltr/test-store/_model", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: LtrStatsPath{}, wantPath: "/_plugins/_ltr/stats", wantErr: false},
		{name: "all fields", path: LtrStatsPath{NodeID: []string{"test-nodeid"}, Stat: []string{"test-stat"}}, wantPath: "/_plugins/_ltr/test-nodeid/stats/test-stat", wantErr: false},
		{name: "only NodeID", path: LtrStatsPath{NodeID: []string{"val-nodeid"}}, wantPath: "/_plugins/_ltr/val-nodeid/stats", wantErr: false},
		{name: "only Stat", path: LtrStatsPath{Stat: []string{"val-stat"}}, wantPath: "/_plugins/_ltr/stats/val-stat", wantErr: false},
		{name: "NodeID multi-value", path: LtrStatsPath{NodeID: []string{"a", "b", "c"}}, wantPath: "/_plugins/_ltr/a,b,c/stats", wantErr: false},
		{name: "Stat multi-value", path: LtrStatsPath{Stat: []string{"a", "b", "c"}}, wantPath: "/_plugins/_ltr/stats/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrUpdateFeaturePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrUpdateFeaturePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrUpdateFeaturePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrUpdateFeaturePath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_feature/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestLtrUpdateFeaturesetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     LtrUpdateFeaturesetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: LtrUpdateFeaturesetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: LtrUpdateFeaturesetPath{Store: "test-store", ID: "test-id"}, wantPath: "/_ltr/test-store/_featureset/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMgetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MgetPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MgetPath{}, wantPath: "/_mget", wantErr: false},
		{name: "all fields", path: MgetPath{Index: "test-index"}, wantPath: "/test-index/_mget", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlAddAgenticMemoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlAddAgenticMemoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlAddAgenticMemoryPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlAddAgenticMemoryPath{MemoryContainerID: "test-memorycontainerid"}, wantPath: "/_plugins/_ml/memory_containers/test-memorycontainerid/memories", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlChunkModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlChunkModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlChunkModelPath{}, wantPath: "", wantErr: true},
		{name: "missing ModelID", path: MlChunkModelPath{ChunkNumber: "req-chunknumber"}, wantPath: "", wantErr: true},
		{name: "missing ChunkNumber", path: MlChunkModelPath{ModelID: "req-modelid"}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlChunkModelPath{ModelID: "test-modelid", ChunkNumber: "test-chunknumber"}, wantPath: "/_plugins/_ml/models/test-modelid/chunk/test-chunknumber", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlCreateConnectorPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlCreateConnectorPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlCreateConnectorPath{}, wantPath: "/_plugins/_ml/connectors/_create", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlCreateControllerPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlCreateControllerPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlCreateControllerPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlCreateControllerPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/controllers/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlCreateMemoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlCreateMemoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlCreateMemoryPath{}, wantPath: "/_plugins/_ml/memory", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlCreateMemoryContainerPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlCreateMemoryContainerPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlCreateMemoryContainerPath{}, wantPath: "/_plugins/_ml/memory_containers/_create", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlCreateMemoryContainerSessionPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlCreateMemoryContainerSessionPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlCreateMemoryContainerSessionPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlCreateMemoryContainerSessionPath{MemoryContainerID: "test-memorycontainerid"}, wantPath: "/_plugins/_ml/memory_containers/test-memorycontainerid/memories/sessions", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlCreateMessagePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlCreateMessagePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlCreateMessagePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlCreateMessagePath{MemoryID: "test-memoryid"}, wantPath: "/_plugins/_ml/memory/test-memoryid/messages", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlCreateModelMetaPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlCreateModelMetaPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlCreateModelMetaPath{}, wantPath: "/_plugins/_ml/models/meta", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeleteAgentPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeleteAgentPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeleteAgentPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeleteAgentPath{AgentID: "test-agentid"}, wantPath: "/_plugins/_ml/agents/test-agentid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeleteAgenticMemoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeleteAgenticMemoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeleteAgenticMemoryPath{}, wantPath: "", wantErr: true},
		{name: "missing MemoryContainerID", path: MlDeleteAgenticMemoryPath{Type: "req-type", ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing Type", path: MlDeleteAgenticMemoryPath{MemoryContainerID: "req-memorycontainerid", ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: MlDeleteAgenticMemoryPath{MemoryContainerID: "req-memorycontainerid", Type: "req-type"}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeleteAgenticMemoryPath{MemoryContainerID: "test-memorycontainerid", Type: "test-type", ID: "test-id"}, wantPath: "/_plugins/_ml/memory_containers/test-memorycontainerid/memories/test-type/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeleteAgenticMemoryQueryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeleteAgenticMemoryQueryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeleteAgenticMemoryQueryPath{}, wantPath: "", wantErr: true},
		{name: "missing MemoryContainerID", path: MlDeleteAgenticMemoryQueryPath{Type: "req-type"}, wantPath: "", wantErr: true},
		{name: "missing Type", path: MlDeleteAgenticMemoryQueryPath{MemoryContainerID: "req-memorycontainerid"}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeleteAgenticMemoryQueryPath{MemoryContainerID: "test-memorycontainerid", Type: "test-type"}, wantPath: "/_plugins/_ml/memory_containers/test-memorycontainerid/memories/test-type/_delete_by_query", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeleteConnectorPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeleteConnectorPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeleteConnectorPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeleteConnectorPath{ConnectorID: "test-connectorid"}, wantPath: "/_plugins/_ml/connectors/test-connectorid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeleteControllerPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeleteControllerPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeleteControllerPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeleteControllerPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/controllers/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeleteMemoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeleteMemoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeleteMemoryPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeleteMemoryPath{MemoryID: "test-memoryid"}, wantPath: "/_plugins/_ml/memory/test-memoryid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeleteMemoryContainerPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeleteMemoryContainerPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeleteMemoryContainerPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeleteMemoryContainerPath{MemoryContainerID: "test-memorycontainerid"}, wantPath: "/_plugins/_ml/memory_containers/test-memorycontainerid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeleteModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeleteModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeleteModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeleteModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/models/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeleteModelGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeleteModelGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeleteModelGroupPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeleteModelGroupPath{ModelGroupID: "test-modelgroupid"}, wantPath: "/_plugins/_ml/model_groups/test-modelgroupid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeleteTaskPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeleteTaskPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeleteTaskPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeleteTaskPath{TaskID: "test-taskid"}, wantPath: "/_plugins/_ml/tasks/test-taskid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlDeployModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlDeployModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlDeployModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlDeployModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/models/test-modelid/_deploy", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlExecuteAgentPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlExecuteAgentPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlExecuteAgentPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlExecuteAgentPath{AgentID: "test-agentid"}, wantPath: "/_plugins/_ml/agents/test-agentid/_execute", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlExecuteAgentStreamPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlExecuteAgentStreamPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlExecuteAgentStreamPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlExecuteAgentStreamPath{AgentID: "test-agentid"}, wantPath: "/_plugins/_ml/agents/test-agentid/_execute/stream", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlExecuteAlgorithmPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlExecuteAlgorithmPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlExecuteAlgorithmPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlExecuteAlgorithmPath{AlgorithmName: "test-algorithmname"}, wantPath: "/_plugins/_ml/_execute/test-algorithmname", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlExecuteToolPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlExecuteToolPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlExecuteToolPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlExecuteToolPath{ToolName: "test-toolname"}, wantPath: "/_plugins/_ml/tools/_execute/test-toolname", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetAgentPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetAgentPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetAgentPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetAgentPath{AgentID: "test-agentid"}, wantPath: "/_plugins/_ml/agents/test-agentid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetAgenticMemoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetAgenticMemoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetAgenticMemoryPath{}, wantPath: "", wantErr: true},
		{name: "missing MemoryContainerID", path: MlGetAgenticMemoryPath{Type: "req-type", ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing Type", path: MlGetAgenticMemoryPath{MemoryContainerID: "req-memorycontainerid", ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: MlGetAgenticMemoryPath{MemoryContainerID: "req-memorycontainerid", Type: "req-type"}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetAgenticMemoryPath{MemoryContainerID: "test-memorycontainerid", Type: "test-type", ID: "test-id"}, wantPath: "/_plugins/_ml/memory_containers/test-memorycontainerid/memories/test-type/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetAllMemoriesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetAllMemoriesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlGetAllMemoriesPath{}, wantPath: "/_plugins/_ml/memory", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetAllMessagesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetAllMessagesPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetAllMessagesPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetAllMessagesPath{MemoryID: "test-memoryid"}, wantPath: "/_plugins/_ml/memory/test-memoryid/messages", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetAllToolsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetAllToolsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlGetAllToolsPath{}, wantPath: "/_plugins/_ml/tools", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetConnectorPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetConnectorPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetConnectorPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetConnectorPath{ConnectorID: "test-connectorid"}, wantPath: "/_plugins/_ml/connectors/test-connectorid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetControllerPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetControllerPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetControllerPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetControllerPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/controllers/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetMemoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetMemoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetMemoryPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetMemoryPath{MemoryID: "test-memoryid"}, wantPath: "/_plugins/_ml/memory/test-memoryid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetMemoryContainerPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetMemoryContainerPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetMemoryContainerPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetMemoryContainerPath{MemoryContainerID: "test-memorycontainerid"}, wantPath: "/_plugins/_ml/memory_containers/test-memorycontainerid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetMessagePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetMessagePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetMessagePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetMessagePath{MessageID: "test-messageid"}, wantPath: "/_plugins/_ml/memory/message/test-messageid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetMessageTracesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetMessageTracesPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetMessageTracesPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetMessageTracesPath{MessageID: "test-messageid"}, wantPath: "/_plugins/_ml/memory/message/test-messageid/traces", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/models/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetModelGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetModelGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetModelGroupPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetModelGroupPath{ModelGroupID: "test-modelgroupid"}, wantPath: "/_plugins/_ml/model_groups/test-modelgroupid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetProfilePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetProfilePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlGetProfilePath{}, wantPath: "/_plugins/_ml/profile", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetProfileModelsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetProfileModelsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlGetProfileModelsPath{}, wantPath: "/_plugins/_ml/profile/models", wantErr: false},
		{name: "all fields", path: MlGetProfileModelsPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/profile/models/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetProfileTasksPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetProfileTasksPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlGetProfileTasksPath{}, wantPath: "/_plugins/_ml/profile/tasks", wantErr: false},
		{name: "all fields", path: MlGetProfileTasksPath{TaskID: "test-taskid"}, wantPath: "/_plugins/_ml/profile/tasks/test-taskid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlGetStatsPath{}, wantPath: "/_plugins/_ml/stats", wantErr: false},
		{name: "all fields", path: MlGetStatsPath{NodeID: "test-nodeid", Stat: []string{"test-stat"}}, wantPath: "/_plugins/_ml/test-nodeid/stats/test-stat", wantErr: false},
		{name: "only NodeID", path: MlGetStatsPath{NodeID: "val-nodeid"}, wantPath: "/_plugins/_ml/val-nodeid/stats", wantErr: false},
		{name: "only Stat", path: MlGetStatsPath{Stat: []string{"val-stat"}}, wantPath: "/_plugins/_ml/stats/val-stat", wantErr: false},
		{name: "Stat multi-value", path: MlGetStatsPath{Stat: []string{"a", "b", "c"}}, wantPath: "/_plugins/_ml/stats/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetTaskPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetTaskPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetTaskPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetTaskPath{TaskID: "test-taskid"}, wantPath: "/_plugins/_ml/tasks/test-taskid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlGetToolPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlGetToolPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlGetToolPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlGetToolPath{ToolName: "test-toolname"}, wantPath: "/_plugins/_ml/tools/test-toolname", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlLoadModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlLoadModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlLoadModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlLoadModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/models/test-modelid/_load", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlPredictPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlPredictPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlPredictPath{}, wantPath: "", wantErr: true},
		{name: "missing AlgorithmName", path: MlPredictPath{ModelID: "req-modelid"}, wantPath: "", wantErr: true},
		{name: "missing ModelID", path: MlPredictPath{AlgorithmName: "req-algorithmname"}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlPredictPath{AlgorithmName: "test-algorithmname", ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/_predict/test-algorithmname/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlPredictModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlPredictModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlPredictModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlPredictModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/models/test-modelid/_predict", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlPredictModelStreamPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlPredictModelStreamPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlPredictModelStreamPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlPredictModelStreamPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/models/test-modelid/_predict/stream", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlRegisterAgentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlRegisterAgentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlRegisterAgentsPath{}, wantPath: "/_plugins/_ml/agents/_register", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlRegisterModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlRegisterModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlRegisterModelPath{}, wantPath: "/_plugins/_ml/models/_register", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlRegisterModelGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlRegisterModelGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlRegisterModelGroupPath{}, wantPath: "/_plugins/_ml/model_groups/_register", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlRegisterModelMetaPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlRegisterModelMetaPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlRegisterModelMetaPath{}, wantPath: "/_plugins/_ml/models/_register_meta", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlSearchAgenticMemoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlSearchAgenticMemoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlSearchAgenticMemoryPath{}, wantPath: "", wantErr: true},
		{name: "missing MemoryContainerID", path: MlSearchAgenticMemoryPath{Type: "req-type"}, wantPath: "", wantErr: true},
		{name: "missing Type", path: MlSearchAgenticMemoryPath{MemoryContainerID: "req-memorycontainerid"}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlSearchAgenticMemoryPath{MemoryContainerID: "test-memorycontainerid", Type: "test-type"}, wantPath: "/_plugins/_ml/memory_containers/test-memorycontainerid/memories/test-type/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlSearchAgentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlSearchAgentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlSearchAgentsPath{}, wantPath: "/_plugins/_ml/agents/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlSearchConnectorsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlSearchConnectorsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlSearchConnectorsPath{}, wantPath: "/_plugins/_ml/connectors/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlSearchMemoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlSearchMemoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlSearchMemoryPath{}, wantPath: "/_plugins/_ml/memory/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlSearchMemoryContainerPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlSearchMemoryContainerPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlSearchMemoryContainerPath{}, wantPath: "/_plugins/_ml/memory_containers/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlSearchMessagePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlSearchMessagePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlSearchMessagePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlSearchMessagePath{MemoryID: "test-memoryid"}, wantPath: "/_plugins/_ml/memory/test-memoryid/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlSearchModelGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlSearchModelGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlSearchModelGroupPath{}, wantPath: "/_plugins/_ml/model_groups/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlSearchModelsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlSearchModelsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlSearchModelsPath{}, wantPath: "/_plugins/_ml/models/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlSearchTasksPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlSearchTasksPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlSearchTasksPath{}, wantPath: "/_plugins/_ml/tasks/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlTrainPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlTrainPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlTrainPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlTrainPath{AlgorithmName: "test-algorithmname"}, wantPath: "/_plugins/_ml/_train/test-algorithmname", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlTrainPredictPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlTrainPredictPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlTrainPredictPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlTrainPredictPath{AlgorithmName: "test-algorithmname"}, wantPath: "/_plugins/_ml/_train_predict/test-algorithmname", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUndeployModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUndeployModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlUndeployModelPath{}, wantPath: "/_plugins/_ml/models/_undeploy", wantErr: false},
		{name: "all fields", path: MlUndeployModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/models/test-modelid/_undeploy", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUnloadModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUnloadModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlUnloadModelPath{}, wantPath: "/_plugins/_ml/models/_unload", wantErr: false},
		{name: "all fields", path: MlUnloadModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/models/test-modelid/_unload", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUpdateAgenticMemoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUpdateAgenticMemoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlUpdateAgenticMemoryPath{}, wantPath: "", wantErr: true},
		{name: "missing MemoryContainerID", path: MlUpdateAgenticMemoryPath{Type: "req-type", ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing Type", path: MlUpdateAgenticMemoryPath{MemoryContainerID: "req-memorycontainerid", ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: MlUpdateAgenticMemoryPath{MemoryContainerID: "req-memorycontainerid", Type: "req-type"}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlUpdateAgenticMemoryPath{MemoryContainerID: "test-memorycontainerid", Type: "test-type", ID: "test-id"}, wantPath: "/_plugins/_ml/memory_containers/test-memorycontainerid/memories/test-type/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUpdateConnectorPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUpdateConnectorPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlUpdateConnectorPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlUpdateConnectorPath{ConnectorID: "test-connectorid"}, wantPath: "/_plugins/_ml/connectors/test-connectorid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUpdateControllerPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUpdateControllerPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlUpdateControllerPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlUpdateControllerPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/controllers/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUpdateMemoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUpdateMemoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlUpdateMemoryPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlUpdateMemoryPath{MemoryID: "test-memoryid"}, wantPath: "/_plugins/_ml/memory/test-memoryid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUpdateMemoryContainerPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUpdateMemoryContainerPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlUpdateMemoryContainerPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlUpdateMemoryContainerPath{MemoryContainerID: "test-memorycontainerid"}, wantPath: "/_plugins/_ml/memory_containers/test-memorycontainerid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUpdateMessagePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUpdateMessagePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlUpdateMessagePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlUpdateMessagePath{MessageID: "test-messageid"}, wantPath: "/_plugins/_ml/memory/message/test-messageid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUpdateModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUpdateModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlUpdateModelPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlUpdateModelPath{ModelID: "test-modelid"}, wantPath: "/_plugins/_ml/models/test-modelid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUpdateModelGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUpdateModelGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlUpdateModelGroupPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlUpdateModelGroupPath{ModelGroupID: "test-modelgroupid"}, wantPath: "/_plugins/_ml/model_groups/test-modelgroupid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUploadChunkPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUploadChunkPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: MlUploadChunkPath{}, wantPath: "", wantErr: true},
		{name: "missing ModelID", path: MlUploadChunkPath{ChunkNumber: "req-chunknumber"}, wantPath: "", wantErr: true},
		{name: "missing ChunkNumber", path: MlUploadChunkPath{ModelID: "req-modelid"}, wantPath: "", wantErr: true},
		{name: "all fields", path: MlUploadChunkPath{ModelID: "test-modelid", ChunkNumber: "test-chunknumber"}, wantPath: "/_plugins/_ml/models/test-modelid/upload_chunk/test-chunknumber", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMlUploadModelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MlUploadModelPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MlUploadModelPath{}, wantPath: "/_plugins/_ml/models/_upload", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMsearchPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MsearchPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MsearchPath{}, wantPath: "/_msearch", wantErr: false},
		{name: "all fields", path: MsearchPath{Index: []string{"test-index"}}, wantPath: "/test-index/_msearch", wantErr: false},
		{name: "Index multi-value", path: MsearchPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_msearch", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMsearchTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MsearchTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MsearchTemplatePath{}, wantPath: "/_msearch/template", wantErr: false},
		{name: "all fields", path: MsearchTemplatePath{Index: []string{"test-index"}}, wantPath: "/test-index/_msearch/template", wantErr: false},
		{name: "Index multi-value", path: MsearchTemplatePath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_msearch/template", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestMtermvectorsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     MtermvectorsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: MtermvectorsPath{}, wantPath: "/_mtermvectors", wantErr: false},
		{name: "all fields", path: MtermvectorsPath{Index: "test-index"}, wantPath: "/test-index/_mtermvectors", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNeuralStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NeuralStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NeuralStatsPath{}, wantPath: "/_plugins/_neural/stats", wantErr: false},
		{name: "all fields", path: NeuralStatsPath{NodeID: "test-nodeid", Stat: []string{"test-stat"}}, wantPath: "/_plugins/_neural/test-nodeid/stats/test-stat", wantErr: false},
		{name: "only NodeID", path: NeuralStatsPath{NodeID: "val-nodeid"}, wantPath: "/_plugins/_neural/val-nodeid/stats", wantErr: false},
		{name: "only Stat", path: NeuralStatsPath{Stat: []string{"val-stat"}}, wantPath: "/_plugins/_neural/stats/val-stat", wantErr: false},
		{name: "Stat multi-value", path: NeuralStatsPath{Stat: []string{"a", "b", "c"}}, wantPath: "/_plugins/_neural/stats/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNodesHotThreadsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NodesHotThreadsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NodesHotThreadsPath{}, wantPath: "/_nodes/hot_threads", wantErr: false},
		{name: "all fields", path: NodesHotThreadsPath{NodeID: []string{"test-nodeid"}}, wantPath: "/_nodes/test-nodeid/hot_threads", wantErr: false},
		{name: "NodeID multi-value", path: NodesHotThreadsPath{NodeID: []string{"a", "b", "c"}}, wantPath: "/_nodes/a,b,c/hot_threads", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNodesInfoPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NodesInfoPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NodesInfoPath{}, wantPath: "/_nodes", wantErr: false},
		{name: "all fields", path: NodesInfoPath{NodeID: []string{"test-nodeid"}, Metric: []string{"test-metric"}}, wantPath: "/_nodes/test-nodeid/test-metric", wantErr: false},
		{name: "only NodeID", path: NodesInfoPath{NodeID: []string{"val-nodeid"}}, wantPath: "/_nodes/val-nodeid", wantErr: false},
		{name: "only Metric", path: NodesInfoPath{Metric: []string{"val-metric"}}, wantPath: "/_nodes/val-metric", wantErr: false},
		{name: "NodeID multi-value", path: NodesInfoPath{NodeID: []string{"a", "b", "c"}}, wantPath: "/_nodes/a,b,c", wantErr: false},
		{name: "Metric multi-value", path: NodesInfoPath{Metric: []string{"a", "b", "c"}}, wantPath: "/_nodes/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNodesReloadSecureSettingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NodesReloadSecureSettingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NodesReloadSecureSettingsPath{}, wantPath: "/_nodes/reload_secure_settings", wantErr: false},
		{name: "all fields", path: NodesReloadSecureSettingsPath{NodeID: []string{"test-nodeid"}}, wantPath: "/_nodes/test-nodeid/reload_secure_settings", wantErr: false},
		{name: "NodeID multi-value", path: NodesReloadSecureSettingsPath{NodeID: []string{"a", "b", "c"}}, wantPath: "/_nodes/a,b,c/reload_secure_settings", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNodesStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NodesStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NodesStatsPath{}, wantPath: "/_nodes/stats", wantErr: false},
		{name: "all fields", path: NodesStatsPath{NodeID: []string{"test-nodeid"}, Metric: []string{"test-metric"}, IndexMetric: []string{"test-indexmetric"}}, wantPath: "/_nodes/test-nodeid/stats/test-metric/test-indexmetric", wantErr: false},
		{name: "only NodeID", path: NodesStatsPath{NodeID: []string{"val-nodeid"}}, wantPath: "/_nodes/val-nodeid/stats", wantErr: false},
		{name: "only Metric", path: NodesStatsPath{Metric: []string{"val-metric"}}, wantPath: "/_nodes/stats/val-metric", wantErr: false},
		{name: "only IndexMetric", path: NodesStatsPath{IndexMetric: []string{"val-indexmetric"}}, wantPath: "", wantErr: true},
		{name: "NodeID multi-value", path: NodesStatsPath{NodeID: []string{"a", "b", "c"}}, wantPath: "/_nodes/a,b,c/stats", wantErr: false},
		{name: "Metric multi-value", path: NodesStatsPath{Metric: []string{"a", "b", "c"}}, wantPath: "/_nodes/stats/a,b,c", wantErr: false},
		{name: "IndexMetric multi-value", path: NodesStatsPath{IndexMetric: []string{"a", "b", "c"}}, wantPath: "", wantErr: true},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNodesUsagePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NodesUsagePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NodesUsagePath{}, wantPath: "/_nodes/usage", wantErr: false},
		{name: "all fields", path: NodesUsagePath{NodeID: []string{"test-nodeid"}, Metric: []string{"test-metric"}}, wantPath: "/_nodes/test-nodeid/usage/test-metric", wantErr: false},
		{name: "only NodeID", path: NodesUsagePath{NodeID: []string{"val-nodeid"}}, wantPath: "/_nodes/val-nodeid/usage", wantErr: false},
		{name: "only Metric", path: NodesUsagePath{Metric: []string{"val-metric"}}, wantPath: "/_nodes/usage/val-metric", wantErr: false},
		{name: "NodeID multi-value", path: NodesUsagePath{NodeID: []string{"a", "b", "c"}}, wantPath: "/_nodes/a,b,c/usage", wantErr: false},
		{name: "Metric multi-value", path: NodesUsagePath{Metric: []string{"a", "b", "c"}}, wantPath: "/_nodes/usage/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNotificationsCreateConfigPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NotificationsCreateConfigPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NotificationsCreateConfigPath{}, wantPath: "/_plugins/_notifications/configs", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNotificationsDeleteConfigPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NotificationsDeleteConfigPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: NotificationsDeleteConfigPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: NotificationsDeleteConfigPath{ConfigID: "test-configid"}, wantPath: "/_plugins/_notifications/configs/test-configid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNotificationsDeleteConfigsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NotificationsDeleteConfigsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NotificationsDeleteConfigsPath{}, wantPath: "/_plugins/_notifications/configs", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNotificationsGetConfigPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NotificationsGetConfigPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: NotificationsGetConfigPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: NotificationsGetConfigPath{ConfigID: "test-configid"}, wantPath: "/_plugins/_notifications/configs/test-configid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNotificationsGetConfigsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NotificationsGetConfigsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NotificationsGetConfigsPath{}, wantPath: "/_plugins/_notifications/configs", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNotificationsListChannelsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NotificationsListChannelsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NotificationsListChannelsPath{}, wantPath: "/_plugins/_notifications/channels", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNotificationsListFeaturesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NotificationsListFeaturesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: NotificationsListFeaturesPath{}, wantPath: "/_plugins/_notifications/features", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNotificationsSendTestPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NotificationsSendTestPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: NotificationsSendTestPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: NotificationsSendTestPath{ConfigID: "test-configid"}, wantPath: "/_plugins/_notifications/feature/test/test-configid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestNotificationsUpdateConfigPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     NotificationsUpdateConfigPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: NotificationsUpdateConfigPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: NotificationsUpdateConfigPath{ConfigID: "test-configid"}, wantPath: "/_plugins/_notifications/configs/test-configid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestObservabilityCreateObjectPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ObservabilityCreateObjectPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ObservabilityCreateObjectPath{}, wantPath: "/_plugins/_observability/object", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestObservabilityDeleteObjectPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ObservabilityDeleteObjectPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ObservabilityDeleteObjectPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ObservabilityDeleteObjectPath{ObjectID: "test-objectid"}, wantPath: "/_plugins/_observability/object/test-objectid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestObservabilityDeleteObjectsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ObservabilityDeleteObjectsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ObservabilityDeleteObjectsPath{}, wantPath: "/_plugins/_observability/object", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestObservabilityGetLocalstatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ObservabilityGetLocalstatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ObservabilityGetLocalstatsPath{}, wantPath: "/_plugins/_observability/_local/stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestObservabilityGetObjectPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ObservabilityGetObjectPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ObservabilityGetObjectPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ObservabilityGetObjectPath{ObjectID: "test-objectid"}, wantPath: "/_plugins/_observability/object/test-objectid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestObservabilityListObjectsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ObservabilityListObjectsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ObservabilityListObjectsPath{}, wantPath: "/_plugins/_observability/object", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestObservabilityUpdateObjectPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ObservabilityUpdateObjectPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ObservabilityUpdateObjectPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ObservabilityUpdateObjectPath{ObjectID: "test-objectid"}, wantPath: "/_plugins/_observability/object/test-objectid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestPingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     PingPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: PingPath{}, wantPath: "/", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestPplExplainPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     PplExplainPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: PplExplainPath{}, wantPath: "/_plugins/_ppl/_explain", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestPplGetStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     PplGetStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: PplGetStatsPath{}, wantPath: "/_plugins/_ppl/stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestPplPostStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     PplPostStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: PplPostStatsPath{}, wantPath: "/_plugins/_ppl/stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestPplQueryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     PplQueryPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: PplQueryPath{}, wantPath: "/_plugins/_ppl", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestPutScriptPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     PutScriptPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: PutScriptPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: PutScriptPath{ID: "test-id", Context: "test-context"}, wantPath: "/_scripts/test-id/test-context", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestQueryDatasourceDeletePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     QueryDatasourceDeletePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: QueryDatasourceDeletePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: QueryDatasourceDeletePath{DatasourceName: "test-datasourcename"}, wantPath: "/_plugins/_query/_datasources/test-datasourcename", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestQueryDatasourceRetrievePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     QueryDatasourceRetrievePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: QueryDatasourceRetrievePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: QueryDatasourceRetrievePath{DatasourceName: "test-datasourcename"}, wantPath: "/_plugins/_query/_datasources/test-datasourcename", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestQueryDatasourcesCreatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     QueryDatasourcesCreatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: QueryDatasourcesCreatePath{}, wantPath: "/_plugins/_query/_datasources", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestQueryDatasourcesListPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     QueryDatasourcesListPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: QueryDatasourcesListPath{}, wantPath: "/_plugins/_query/_datasources", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestQueryDatasourcesUpdatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     QueryDatasourcesUpdatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: QueryDatasourcesUpdatePath{}, wantPath: "/_plugins/_query/_datasources", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestRankEvalPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     RankEvalPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: RankEvalPath{}, wantPath: "/_rank_eval", wantErr: false},
		{name: "all fields", path: RankEvalPath{Index: []string{"test-index"}}, wantPath: "/test-index/_rank_eval", wantErr: false},
		{name: "Index multi-value", path: RankEvalPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_rank_eval", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReindexPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReindexPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ReindexPath{}, wantPath: "/_reindex", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReindexRethrottlePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReindexRethrottlePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ReindexRethrottlePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ReindexRethrottlePath{TaskID: "test-taskid"}, wantPath: "/_reindex/test-taskid/_rethrottle", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestRemoteStoreRestorePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     RemoteStoreRestorePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: RemoteStoreRestorePath{}, wantPath: "/_remotestore/_restore", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestRenderSearchTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     RenderSearchTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: RenderSearchTemplatePath{}, wantPath: "/_render/template", wantErr: false},
		{name: "all fields", path: RenderSearchTemplatePath{ID: "test-id"}, wantPath: "/_render/template/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationAutofollowStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationAutofollowStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ReplicationAutofollowStatsPath{}, wantPath: "/_plugins/_replication/autofollow_stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationCreateReplicationRulePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationCreateReplicationRulePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ReplicationCreateReplicationRulePath{}, wantPath: "/_plugins/_replication/_autofollow", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationDeleteReplicationRulePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationDeleteReplicationRulePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ReplicationDeleteReplicationRulePath{}, wantPath: "/_plugins/_replication/_autofollow", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationFollowerStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationFollowerStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ReplicationFollowerStatsPath{}, wantPath: "/_plugins/_replication/follower_stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationLeaderStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationLeaderStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ReplicationLeaderStatsPath{}, wantPath: "/_plugins/_replication/leader_stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationPausePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationPausePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ReplicationPausePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ReplicationPausePath{Index: "test-index"}, wantPath: "/_plugins/_replication/test-index/_pause", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationResumePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationResumePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ReplicationResumePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ReplicationResumePath{Index: "test-index"}, wantPath: "/_plugins/_replication/test-index/_resume", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationStartPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationStartPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ReplicationStartPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ReplicationStartPath{Index: "test-index"}, wantPath: "/_plugins/_replication/test-index/_start", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationStatusPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationStatusPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ReplicationStatusPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ReplicationStatusPath{Index: "test-index"}, wantPath: "/_plugins/_replication/test-index/_status", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationStopPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationStopPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ReplicationStopPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ReplicationStopPath{Index: "test-index"}, wantPath: "/_plugins/_replication/test-index/_stop", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestReplicationUpdateSettingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ReplicationUpdateSettingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: ReplicationUpdateSettingsPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: ReplicationUpdateSettingsPath{Index: "test-index"}, wantPath: "/_plugins/_replication/test-index/_update", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestRollupsDeletePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     RollupsDeletePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: RollupsDeletePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: RollupsDeletePath{ID: "test-id"}, wantPath: "/_plugins/_rollup/jobs/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestRollupsExplainPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     RollupsExplainPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: RollupsExplainPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: RollupsExplainPath{ID: "test-id"}, wantPath: "/_plugins/_rollup/jobs/test-id/_explain", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestRollupsGetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     RollupsGetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: RollupsGetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: RollupsGetPath{ID: "test-id"}, wantPath: "/_plugins/_rollup/jobs/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestRollupsPutPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     RollupsPutPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: RollupsPutPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: RollupsPutPath{ID: "test-id"}, wantPath: "/_plugins/_rollup/jobs/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestRollupsStartPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     RollupsStartPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: RollupsStartPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: RollupsStartPath{ID: "test-id"}, wantPath: "/_plugins/_rollup/jobs/test-id/_start", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestRollupsStopPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     RollupsStopPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: RollupsStopPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: RollupsStopPath{ID: "test-id"}, wantPath: "/_plugins/_rollup/jobs/test-id/_stop", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestScriptsPainlessExecutePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ScriptsPainlessExecutePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ScriptsPainlessExecutePath{}, wantPath: "/_scripts/painless/_execute", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestScrollPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     ScrollPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: ScrollPath{}, wantPath: "/_search/scroll", wantErr: false},
		{name: "all fields", path: ScrollPath{ScrollID: "test-scrollid"}, wantPath: "/_search/scroll/test-scrollid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchPath{}, wantPath: "/_search", wantErr: false},
		{name: "all fields", path: SearchPath{Index: []string{"test-index"}}, wantPath: "/test-index/_search", wantErr: false},
		{name: "Index multi-value", path: SearchPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchPipelineDeletePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchPipelineDeletePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SearchPipelineDeletePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SearchPipelineDeletePath{ID: "test-id"}, wantPath: "/_search/pipeline/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchPipelineGetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchPipelineGetPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchPipelineGetPath{}, wantPath: "/_search/pipeline", wantErr: false},
		{name: "all fields", path: SearchPipelineGetPath{ID: "test-id"}, wantPath: "/_search/pipeline/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchPipelinePutPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchPipelinePutPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SearchPipelinePutPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SearchPipelinePutPath{ID: "test-id"}, wantPath: "/_search/pipeline/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceDeleteExperimentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceDeleteExperimentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SearchRelevanceDeleteExperimentsPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SearchRelevanceDeleteExperimentsPath{ExperimentID: "test-experimentid"}, wantPath: "/_plugins/_search_relevance/experiments/test-experimentid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceDeleteJudgmentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceDeleteJudgmentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SearchRelevanceDeleteJudgmentsPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SearchRelevanceDeleteJudgmentsPath{JudgmentID: "test-judgmentid"}, wantPath: "/_plugins/_search_relevance/judgments/test-judgmentid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceDeleteQuerySetsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceDeleteQuerySetsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SearchRelevanceDeleteQuerySetsPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SearchRelevanceDeleteQuerySetsPath{QuerySetID: "test-querysetid"}, wantPath: "/_plugins/_search_relevance/query_sets/test-querysetid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceDeleteScheduledExperimentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceDeleteScheduledExperimentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SearchRelevanceDeleteScheduledExperimentsPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SearchRelevanceDeleteScheduledExperimentsPath{ExperimentID: "test-experimentid"}, wantPath: "/_plugins/_search_relevance/experiments/schedule/test-experimentid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceDeleteSearchConfigurationsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceDeleteSearchConfigurationsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SearchRelevanceDeleteSearchConfigurationsPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SearchRelevanceDeleteSearchConfigurationsPath{SearchConfigurationID: "test-searchconfigurationid"}, wantPath: "/_plugins/_search_relevance/search_configurations/test-searchconfigurationid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceGetExperimentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceGetExperimentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevanceGetExperimentsPath{}, wantPath: "/_plugins/_search_relevance/experiments", wantErr: false},
		{name: "all fields", path: SearchRelevanceGetExperimentsPath{ExperimentID: "test-experimentid"}, wantPath: "/_plugins/_search_relevance/experiments/test-experimentid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceGetJudgmentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceGetJudgmentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevanceGetJudgmentsPath{}, wantPath: "/_plugins/_search_relevance/judgments", wantErr: false},
		{name: "all fields", path: SearchRelevanceGetJudgmentsPath{JudgmentID: "test-judgmentid"}, wantPath: "/_plugins/_search_relevance/judgments/test-judgmentid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceGetNodeStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceGetNodeStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SearchRelevanceGetNodeStatsPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SearchRelevanceGetNodeStatsPath{NodeID: "test-nodeid", Stat: "test-stat"}, wantPath: "/_plugins/_search_relevance/test-nodeid/stats/test-stat", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceGetQuerySetsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceGetQuerySetsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevanceGetQuerySetsPath{}, wantPath: "/_plugins/_search_relevance/query_sets", wantErr: false},
		{name: "all fields", path: SearchRelevanceGetQuerySetsPath{QuerySetID: "test-querysetid"}, wantPath: "/_plugins/_search_relevance/query_sets/test-querysetid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceGetScheduledExperimentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceGetScheduledExperimentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevanceGetScheduledExperimentsPath{}, wantPath: "/_plugins/_search_relevance/experiments/schedule", wantErr: false},
		{name: "all fields", path: SearchRelevanceGetScheduledExperimentsPath{ExperimentID: "test-experimentid"}, wantPath: "/_plugins/_search_relevance/experiments/schedule/test-experimentid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceGetSearchConfigurationsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceGetSearchConfigurationsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevanceGetSearchConfigurationsPath{}, wantPath: "/_plugins/_search_relevance/search_configurations", wantErr: false},
		{name: "all fields", path: SearchRelevanceGetSearchConfigurationsPath{SearchConfigurationID: "test-searchconfigurationid"}, wantPath: "/_plugins/_search_relevance/search_configurations/test-searchconfigurationid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevanceGetStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevanceGetStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevanceGetStatsPath{}, wantPath: "/_plugins/_search_relevance/stats", wantErr: false},
		{name: "all fields", path: SearchRelevanceGetStatsPath{Stat: "test-stat"}, wantPath: "/_plugins/_search_relevance/stats/test-stat", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevancePostQuerySetsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevancePostQuerySetsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevancePostQuerySetsPath{}, wantPath: "/_plugins/_search_relevance/query_sets", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevancePostScheduledExperimentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevancePostScheduledExperimentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevancePostScheduledExperimentsPath{}, wantPath: "/_plugins/_search_relevance/experiments/schedule", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevancePutExperimentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevancePutExperimentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevancePutExperimentsPath{}, wantPath: "/_plugins/_search_relevance/experiments", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevancePutJudgmentsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevancePutJudgmentsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevancePutJudgmentsPath{}, wantPath: "/_plugins/_search_relevance/judgments", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevancePutQuerySetsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevancePutQuerySetsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevancePutQuerySetsPath{}, wantPath: "/_plugins/_search_relevance/query_sets", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchRelevancePutSearchConfigurationsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchRelevancePutSearchConfigurationsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchRelevancePutSearchConfigurationsPath{}, wantPath: "/_plugins/_search_relevance/search_configurations", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchShardsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchShardsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchShardsPath{}, wantPath: "/_search_shards", wantErr: false},
		{name: "all fields", path: SearchShardsPath{Index: []string{"test-index"}}, wantPath: "/test-index/_search_shards", wantErr: false},
		{name: "Index multi-value", path: SearchShardsPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_search_shards", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSearchTemplatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SearchTemplatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SearchTemplatePath{}, wantPath: "/_search/template", wantErr: false},
		{name: "all fields", path: SearchTemplatePath{Index: []string{"test-index"}}, wantPath: "/test-index/_search/template", wantErr: false},
		{name: "Index multi-value", path: SearchTemplatePath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_search/template", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityAuthinfoPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityAuthinfoPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityAuthinfoPath{}, wantPath: "/_plugins/_security/authinfo", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityAuthtokenPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityAuthtokenPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityAuthtokenPath{}, wantPath: "/_plugins/_security/api/authtoken", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityChangePasswordPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityChangePasswordPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityChangePasswordPath{}, wantPath: "/_plugins/_security/api/account", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityConfigUpgradeCheckPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityConfigUpgradeCheckPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityConfigUpgradeCheckPath{}, wantPath: "/_plugins/_security/api/_upgrade_check", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityConfigUpgradePerformPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityConfigUpgradePerformPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityConfigUpgradePerformPath{}, wantPath: "/_plugins/_security/api/_upgrade_perform", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityCreateActionGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityCreateActionGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityCreateActionGroupPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityCreateActionGroupPath{ActionGroup: "test-actiongroup"}, wantPath: "/_plugins/_security/api/actiongroups/test-actiongroup", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityCreateAllowlistPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityCreateAllowlistPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityCreateAllowlistPath{}, wantPath: "/_plugins/_security/api/allowlist", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityCreateRolePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityCreateRolePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityCreateRolePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityCreateRolePath{Role: "test-role"}, wantPath: "/_plugins/_security/api/roles/test-role", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityCreateRoleMappingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityCreateRoleMappingPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityCreateRoleMappingPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityCreateRoleMappingPath{Role: "test-role"}, wantPath: "/_plugins/_security/api/rolesmapping/test-role", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityCreateTenantPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityCreateTenantPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityCreateTenantPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityCreateTenantPath{Tenant: "test-tenant"}, wantPath: "/_plugins/_security/api/tenants/test-tenant", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityCreateUpdateTenancyConfigPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityCreateUpdateTenancyConfigPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityCreateUpdateTenancyConfigPath{}, wantPath: "/_plugins/_security/api/tenancy/config", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityCreateUserPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityCreateUserPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityCreateUserPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityCreateUserPath{Username: "test-username"}, wantPath: "/_plugins/_security/api/internalusers/test-username", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityCreateUserLegacyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityCreateUserLegacyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityCreateUserLegacyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityCreateUserLegacyPath{Username: "test-username"}, wantPath: "/_plugins/_security/api/user/test-username", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityDeleteActionGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityDeleteActionGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityDeleteActionGroupPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityDeleteActionGroupPath{ActionGroup: "test-actiongroup"}, wantPath: "/_plugins/_security/api/actiongroups/test-actiongroup", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityDeleteDistinguishedNamePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityDeleteDistinguishedNamePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityDeleteDistinguishedNamePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityDeleteDistinguishedNamePath{ClusterName: "test-clustername"}, wantPath: "/_plugins/_security/api/nodesdn/test-clustername", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityDeleteRolePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityDeleteRolePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityDeleteRolePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityDeleteRolePath{Role: "test-role"}, wantPath: "/_plugins/_security/api/roles/test-role", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityDeleteRoleMappingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityDeleteRoleMappingPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityDeleteRoleMappingPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityDeleteRoleMappingPath{Role: "test-role"}, wantPath: "/_plugins/_security/api/rolesmapping/test-role", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityDeleteTenantPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityDeleteTenantPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityDeleteTenantPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityDeleteTenantPath{Tenant: "test-tenant"}, wantPath: "/_plugins/_security/api/tenants/test-tenant", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityDeleteUserPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityDeleteUserPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityDeleteUserPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityDeleteUserPath{Username: "test-username"}, wantPath: "/_plugins/_security/api/internalusers/test-username", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityDeleteUserLegacyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityDeleteUserLegacyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityDeleteUserLegacyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityDeleteUserLegacyPath{Username: "test-username"}, wantPath: "/_plugins/_security/api/user/test-username", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityFlushCachePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityFlushCachePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityFlushCachePath{}, wantPath: "/_plugins/_security/api/cache", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGenerateOboTokenPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGenerateOboTokenPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGenerateOboTokenPath{}, wantPath: "/_plugins/_security/api/generateonbehalfoftoken", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGenerateUserTokenPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGenerateUserTokenPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityGenerateUserTokenPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityGenerateUserTokenPath{Username: "test-username"}, wantPath: "/_plugins/_security/api/internalusers/test-username/authtoken", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGenerateUserTokenLegacyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGenerateUserTokenLegacyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityGenerateUserTokenLegacyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityGenerateUserTokenLegacyPath{Username: "test-username"}, wantPath: "/_plugins/_security/api/user/test-username/authtoken", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetAccountDetailsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetAccountDetailsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetAccountDetailsPath{}, wantPath: "/_plugins/_security/api/account", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetActionGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetActionGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityGetActionGroupPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityGetActionGroupPath{ActionGroup: "test-actiongroup"}, wantPath: "/_plugins/_security/api/actiongroups/test-actiongroup", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetActionGroupsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetActionGroupsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetActionGroupsPath{}, wantPath: "/_plugins/_security/api/actiongroups", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetAllCertificatesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetAllCertificatesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetAllCertificatesPath{}, wantPath: "/_plugins/_security/api/certificates", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetAllowlistPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetAllowlistPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetAllowlistPath{}, wantPath: "/_plugins/_security/api/allowlist", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetAuditConfigurationPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetAuditConfigurationPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetAuditConfigurationPath{}, wantPath: "/_plugins/_security/api/audit", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetCertificatesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetCertificatesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetCertificatesPath{}, wantPath: "/_plugins/_security/api/ssl/certs", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetConfigurationPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetConfigurationPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetConfigurationPath{}, wantPath: "/_plugins/_security/api/securityconfig", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetDashboardsInfoPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetDashboardsInfoPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetDashboardsInfoPath{}, wantPath: "/_plugins/_security/dashboardsinfo", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetDistinguishedNamePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetDistinguishedNamePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityGetDistinguishedNamePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityGetDistinguishedNamePath{ClusterName: "test-clustername"}, wantPath: "/_plugins/_security/api/nodesdn/test-clustername", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetDistinguishedNamesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetDistinguishedNamesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetDistinguishedNamesPath{}, wantPath: "/_plugins/_security/api/nodesdn", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetNodeCertificatesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetNodeCertificatesPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityGetNodeCertificatesPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityGetNodeCertificatesPath{NodeID: "test-nodeid"}, wantPath: "/_plugins/_security/api/certificates/test-nodeid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetPermissionsInfoPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetPermissionsInfoPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetPermissionsInfoPath{}, wantPath: "/_plugins/_security/api/permissionsinfo", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetRolePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetRolePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityGetRolePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityGetRolePath{Role: "test-role"}, wantPath: "/_plugins/_security/api/roles/test-role", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetRoleMappingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetRoleMappingPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityGetRoleMappingPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityGetRoleMappingPath{Role: "test-role"}, wantPath: "/_plugins/_security/api/rolesmapping/test-role", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetRoleMappingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetRoleMappingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetRoleMappingsPath{}, wantPath: "/_plugins/_security/api/rolesmapping", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetRolesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetRolesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetRolesPath{}, wantPath: "/_plugins/_security/api/roles", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetSslinfoPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetSslinfoPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetSslinfoPath{}, wantPath: "/_opendistro/_security/sslinfo", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetTenancyConfigPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetTenancyConfigPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetTenancyConfigPath{}, wantPath: "/_plugins/_security/api/tenancy/config", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetTenantPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetTenantPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityGetTenantPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityGetTenantPath{Tenant: "test-tenant"}, wantPath: "/_plugins/_security/api/tenants/test-tenant", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetTenantsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetTenantsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetTenantsPath{}, wantPath: "/_plugins/_security/api/tenants", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetUserPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetUserPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityGetUserPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityGetUserPath{Username: "test-username"}, wantPath: "/_plugins/_security/api/internalusers/test-username", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetUserLegacyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetUserLegacyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityGetUserLegacyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityGetUserLegacyPath{Username: "test-username"}, wantPath: "/_plugins/_security/api/user/test-username", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetUsersPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetUsersPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetUsersPath{}, wantPath: "/_plugins/_security/api/internalusers", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityGetUsersLegacyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityGetUsersLegacyPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityGetUsersLegacyPath{}, wantPath: "/_plugins/_security/api/user", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityHealthPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityHealthPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityHealthPath{}, wantPath: "/_plugins/_security/health", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityMigratePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityMigratePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityMigratePath{}, wantPath: "/_plugins/_security/api/migrate", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchActionGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchActionGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityPatchActionGroupPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityPatchActionGroupPath{ActionGroup: "test-actiongroup"}, wantPath: "/_plugins/_security/api/actiongroups/test-actiongroup", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchActionGroupsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchActionGroupsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityPatchActionGroupsPath{}, wantPath: "/_plugins/_security/api/actiongroups", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchAllowlistPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchAllowlistPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityPatchAllowlistPath{}, wantPath: "/_plugins/_security/api/allowlist", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchAuditConfigurationPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchAuditConfigurationPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityPatchAuditConfigurationPath{}, wantPath: "/_plugins/_security/api/audit", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchConfigurationPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchConfigurationPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityPatchConfigurationPath{}, wantPath: "/_plugins/_security/api/securityconfig", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchDistinguishedNamePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchDistinguishedNamePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityPatchDistinguishedNamePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityPatchDistinguishedNamePath{ClusterName: "test-clustername"}, wantPath: "/_plugins/_security/api/nodesdn/test-clustername", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchDistinguishedNamesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchDistinguishedNamesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityPatchDistinguishedNamesPath{}, wantPath: "/_plugins/_security/api/nodesdn", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchRolePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchRolePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityPatchRolePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityPatchRolePath{Role: "test-role"}, wantPath: "/_plugins/_security/api/roles/test-role", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchRoleMappingPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchRoleMappingPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityPatchRoleMappingPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityPatchRoleMappingPath{Role: "test-role"}, wantPath: "/_plugins/_security/api/rolesmapping/test-role", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchRoleMappingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchRoleMappingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityPatchRoleMappingsPath{}, wantPath: "/_plugins/_security/api/rolesmapping", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchRolesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchRolesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityPatchRolesPath{}, wantPath: "/_plugins/_security/api/roles", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchTenantPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchTenantPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityPatchTenantPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityPatchTenantPath{Tenant: "test-tenant"}, wantPath: "/_plugins/_security/api/tenants/test-tenant", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchTenantsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchTenantsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityPatchTenantsPath{}, wantPath: "/_plugins/_security/api/tenants", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchUserPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchUserPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityPatchUserPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityPatchUserPath{Username: "test-username"}, wantPath: "/_plugins/_security/api/internalusers/test-username", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPatchUsersPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPatchUsersPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityPatchUsersPath{}, wantPath: "/_plugins/_security/api/internalusers", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityPostDashboardsInfoPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityPostDashboardsInfoPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityPostDashboardsInfoPath{}, wantPath: "/_plugins/_security/dashboardsinfo", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityReloadHTTPCertificatesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityReloadHTTPCertificatesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityReloadHTTPCertificatesPath{}, wantPath: "/_plugins/_security/api/ssl/http/reloadcerts", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityReloadTransportCertificatesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityReloadTransportCertificatesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityReloadTransportCertificatesPath{}, wantPath: "/_plugins/_security/api/ssl/transport/reloadcerts", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityTenantInfoPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityTenantInfoPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityTenantInfoPath{}, wantPath: "/_plugins/_security/tenantinfo", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityUpdateAuditConfigurationPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityUpdateAuditConfigurationPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityUpdateAuditConfigurationPath{}, wantPath: "/_plugins/_security/api/audit/config", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityUpdateConfigurationPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityUpdateConfigurationPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityUpdateConfigurationPath{}, wantPath: "/_plugins/_security/api/securityconfig/config", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityUpdateDistinguishedNamePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityUpdateDistinguishedNamePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SecurityUpdateDistinguishedNamePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SecurityUpdateDistinguishedNamePath{ClusterName: "test-clustername"}, wantPath: "/_plugins/_security/api/nodesdn/test-clustername", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityValidatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityValidatePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityValidatePath{}, wantPath: "/_plugins/_security/api/validate", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityWhoAmIPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityWhoAmIPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityWhoAmIPath{}, wantPath: "/_plugins/_security/whoami", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityWhoAmIProtectedPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityWhoAmIProtectedPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityWhoAmIProtectedPath{}, wantPath: "/_plugins/_security/whoamiprotected", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityAnalyticsGetAlertsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityAnalyticsGetAlertsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityAnalyticsGetAlertsPath{}, wantPath: "/_plugins/_security_analytics/alerts", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityAnalyticsGetFindingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityAnalyticsGetFindingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityAnalyticsGetFindingsPath{}, wantPath: "/_plugins/_security_analytics/findings/_search", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSecurityAnalyticsSearchFindingCorrelationsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SecurityAnalyticsSearchFindingCorrelationsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SecurityAnalyticsSearchFindingCorrelationsPath{}, wantPath: "/_plugins/_security_analytics/findings/correlate", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSmCreatePolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SmCreatePolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SmCreatePolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SmCreatePolicyPath{PolicyName: "test-policyname"}, wantPath: "/_plugins/_sm/policies/test-policyname", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSmDeletePolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SmDeletePolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SmDeletePolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SmDeletePolicyPath{PolicyName: "test-policyname"}, wantPath: "/_plugins/_sm/policies/test-policyname", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSmExplainPolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SmExplainPolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SmExplainPolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SmExplainPolicyPath{PolicyName: "test-policyname"}, wantPath: "/_plugins/_sm/policies/test-policyname/_explain", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSmGetPoliciesPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SmGetPoliciesPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SmGetPoliciesPath{}, wantPath: "/_plugins/_sm/policies", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSmGetPolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SmGetPolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SmGetPolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SmGetPolicyPath{PolicyName: "test-policyname"}, wantPath: "/_plugins/_sm/policies/test-policyname", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSmStartPolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SmStartPolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SmStartPolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SmStartPolicyPath{PolicyName: "test-policyname"}, wantPath: "/_plugins/_sm/policies/test-policyname/_start", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSmStopPolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SmStopPolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SmStopPolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SmStopPolicyPath{PolicyName: "test-policyname"}, wantPath: "/_plugins/_sm/policies/test-policyname/_stop", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSmUpdatePolicyPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SmUpdatePolicyPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SmUpdatePolicyPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SmUpdatePolicyPath{PolicyName: "test-policyname"}, wantPath: "/_plugins/_sm/policies/test-policyname", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotCleanupRepositoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotCleanupRepositoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SnapshotCleanupRepositoryPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SnapshotCleanupRepositoryPath{Repository: "test-repository"}, wantPath: "/_snapshot/test-repository/_cleanup", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotClonePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotClonePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SnapshotClonePath{}, wantPath: "", wantErr: true},
		{name: "missing Repository", path: SnapshotClonePath{Snapshot: "req-snapshot", TargetSnapshot: "req-targetsnapshot"}, wantPath: "", wantErr: true},
		{name: "missing Snapshot", path: SnapshotClonePath{Repository: "req-repository", TargetSnapshot: "req-targetsnapshot"}, wantPath: "", wantErr: true},
		{name: "missing TargetSnapshot", path: SnapshotClonePath{Repository: "req-repository", Snapshot: "req-snapshot"}, wantPath: "", wantErr: true},
		{name: "all fields", path: SnapshotClonePath{Repository: "test-repository", Snapshot: "test-snapshot", TargetSnapshot: "test-targetsnapshot"}, wantPath: "/_snapshot/test-repository/test-snapshot/_clone/test-targetsnapshot", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotCreatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotCreatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SnapshotCreatePath{}, wantPath: "", wantErr: true},
		{name: "missing Repository", path: SnapshotCreatePath{Snapshot: "req-snapshot"}, wantPath: "", wantErr: true},
		{name: "missing Snapshot", path: SnapshotCreatePath{Repository: "req-repository"}, wantPath: "", wantErr: true},
		{name: "all fields", path: SnapshotCreatePath{Repository: "test-repository", Snapshot: "test-snapshot"}, wantPath: "/_snapshot/test-repository/test-snapshot", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotCreateRepositoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotCreateRepositoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SnapshotCreateRepositoryPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SnapshotCreateRepositoryPath{Repository: "test-repository"}, wantPath: "/_snapshot/test-repository", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotDeletePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotDeletePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SnapshotDeletePath{}, wantPath: "", wantErr: true},
		{name: "missing Repository", path: SnapshotDeletePath{Snapshot: []string{"req-snapshot"}}, wantPath: "", wantErr: true},
		{name: "missing Snapshot", path: SnapshotDeletePath{Repository: "req-repository"}, wantPath: "", wantErr: true},
		{name: "all fields", path: SnapshotDeletePath{Repository: "test-repository", Snapshot: []string{"test-snapshot"}}, wantPath: "/_snapshot/test-repository/test-snapshot", wantErr: false},
		{name: "Snapshot multi-value", path: SnapshotDeletePath{Repository: "req-repository", Snapshot: []string{"a", "b", "c"}}, wantPath: "/_snapshot/req-repository/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotDeleteRepositoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotDeleteRepositoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SnapshotDeleteRepositoryPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SnapshotDeleteRepositoryPath{Repository: []string{"test-repository"}}, wantPath: "/_snapshot/test-repository", wantErr: false},
		{name: "Repository multi-value", path: SnapshotDeleteRepositoryPath{Repository: []string{"a", "b", "c"}}, wantPath: "/_snapshot/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotGetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotGetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SnapshotGetPath{}, wantPath: "", wantErr: true},
		{name: "missing Repository", path: SnapshotGetPath{Snapshot: []string{"req-snapshot"}}, wantPath: "", wantErr: true},
		{name: "missing Snapshot", path: SnapshotGetPath{Repository: "req-repository"}, wantPath: "", wantErr: true},
		{name: "all fields", path: SnapshotGetPath{Repository: "test-repository", Snapshot: []string{"test-snapshot"}}, wantPath: "/_snapshot/test-repository/test-snapshot", wantErr: false},
		{name: "Snapshot multi-value", path: SnapshotGetPath{Repository: "req-repository", Snapshot: []string{"a", "b", "c"}}, wantPath: "/_snapshot/req-repository/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotGetRepositoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotGetRepositoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SnapshotGetRepositoryPath{}, wantPath: "/_snapshot", wantErr: false},
		{name: "all fields", path: SnapshotGetRepositoryPath{Repository: []string{"test-repository"}}, wantPath: "/_snapshot/test-repository", wantErr: false},
		{name: "Repository multi-value", path: SnapshotGetRepositoryPath{Repository: []string{"a", "b", "c"}}, wantPath: "/_snapshot/a,b,c", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotRestorePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotRestorePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SnapshotRestorePath{}, wantPath: "", wantErr: true},
		{name: "missing Repository", path: SnapshotRestorePath{Snapshot: "req-snapshot"}, wantPath: "", wantErr: true},
		{name: "missing Snapshot", path: SnapshotRestorePath{Repository: "req-repository"}, wantPath: "", wantErr: true},
		{name: "all fields", path: SnapshotRestorePath{Repository: "test-repository", Snapshot: "test-snapshot"}, wantPath: "/_snapshot/test-repository/test-snapshot/_restore", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotStatusPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotStatusPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SnapshotStatusPath{}, wantPath: "/_snapshot/_status", wantErr: false},
		{name: "all fields", path: SnapshotStatusPath{Repository: "test-repository", Snapshot: []string{"test-snapshot"}}, wantPath: "/_snapshot/test-repository/test-snapshot/_status", wantErr: false},
		{name: "only Repository", path: SnapshotStatusPath{Repository: "val-repository"}, wantPath: "/_snapshot/val-repository/_status", wantErr: false},
		{name: "only Snapshot", path: SnapshotStatusPath{Snapshot: []string{"val-snapshot"}}, wantPath: "", wantErr: true},
		{name: "Snapshot multi-value", path: SnapshotStatusPath{Snapshot: []string{"a", "b", "c"}}, wantPath: "", wantErr: true},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSnapshotVerifyRepositoryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SnapshotVerifyRepositoryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: SnapshotVerifyRepositoryPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: SnapshotVerifyRepositoryPath{Repository: "test-repository"}, wantPath: "/_snapshot/test-repository/_verify", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSQLClosePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SQLClosePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SQLClosePath{}, wantPath: "/_plugins/_sql/close", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSQLExplainPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SQLExplainPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SQLExplainPath{}, wantPath: "/_plugins/_sql/_explain", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSQLGetStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SQLGetStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SQLGetStatsPath{}, wantPath: "/_plugins/_sql/stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSQLPostStatsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SQLPostStatsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SQLPostStatsPath{}, wantPath: "/_plugins/_sql/stats", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSQLQueryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SQLQueryPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SQLQueryPath{}, wantPath: "/_plugins/_sql", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestSQLSettingsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     SQLSettingsPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: SQLSettingsPath{}, wantPath: "/_plugins/_query/settings", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTasksCancelPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TasksCancelPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: TasksCancelPath{}, wantPath: "/_tasks/_cancel", wantErr: false},
		{name: "all fields", path: TasksCancelPath{TaskID: "test-taskid"}, wantPath: "/_tasks/test-taskid/_cancel", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTasksGetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TasksGetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: TasksGetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: TasksGetPath{TaskID: "test-taskid"}, wantPath: "/_tasks/test-taskid", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTasksListPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TasksListPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: TasksListPath{}, wantPath: "/_tasks", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTermvectorsPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TermvectorsPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: TermvectorsPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: TermvectorsPath{Index: "test-index", ID: "test-id"}, wantPath: "/test-index/_termvectors/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTransformsDeletePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TransformsDeletePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: TransformsDeletePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: TransformsDeletePath{ID: "test-id"}, wantPath: "/_plugins/_transform/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTransformsExplainPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TransformsExplainPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: TransformsExplainPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: TransformsExplainPath{ID: "test-id"}, wantPath: "/_plugins/_transform/test-id/_explain", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTransformsGetPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TransformsGetPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: TransformsGetPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: TransformsGetPath{ID: "test-id"}, wantPath: "/_plugins/_transform/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTransformsPreviewPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TransformsPreviewPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: TransformsPreviewPath{}, wantPath: "/_plugins/_transform/_preview", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTransformsPutPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TransformsPutPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: TransformsPutPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: TransformsPutPath{ID: "test-id"}, wantPath: "/_plugins/_transform/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTransformsSearchPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TransformsSearchPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: TransformsSearchPath{}, wantPath: "/_plugins/_transform", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTransformsStartPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TransformsStartPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: TransformsStartPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: TransformsStartPath{ID: "test-id"}, wantPath: "/_plugins/_transform/test-id/_start", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestTransformsStopPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     TransformsStopPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: TransformsStopPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: TransformsStopPath{ID: "test-id"}, wantPath: "/_plugins/_transform/test-id/_stop", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestUbiInitializePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     UbiInitializePath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: UbiInitializePath{}, wantPath: "/_plugins/ubi/initialize", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestUpdatePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     UpdatePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: UpdatePath{}, wantPath: "", wantErr: true},
		{name: "missing Index", path: UpdatePath{ID: "req-id"}, wantPath: "", wantErr: true},
		{name: "missing ID", path: UpdatePath{Index: "req-index"}, wantPath: "", wantErr: true},
		{name: "all fields", path: UpdatePath{Index: "test-index", ID: "test-id"}, wantPath: "/test-index/_update/test-id", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestUpdateByQueryPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     UpdateByQueryPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: UpdateByQueryPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: UpdateByQueryPath{Index: []string{"test-index"}}, wantPath: "/test-index/_update_by_query", wantErr: false},
		{name: "Index multi-value", path: UpdateByQueryPath{Index: []string{"a", "b", "c"}}, wantPath: "/a,b,c/_update_by_query", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestUpdateByQueryRethrottlePath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     UpdateByQueryRethrottlePath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: UpdateByQueryRethrottlePath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: UpdateByQueryRethrottlePath{TaskID: "test-taskid"}, wantPath: "/_update_by_query/test-taskid/_rethrottle", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestWlmCreateQueryGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     WlmCreateQueryGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: WlmCreateQueryGroupPath{}, wantPath: "/_wlm/query_group", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestWlmDeleteQueryGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     WlmDeleteQueryGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: WlmDeleteQueryGroupPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: WlmDeleteQueryGroupPath{Name: "test-name"}, wantPath: "/_wlm/query_group/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestWlmGetQueryGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     WlmGetQueryGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "all empty", path: WlmGetQueryGroupPath{}, wantPath: "/_wlm/query_group", wantErr: false},
		{name: "all fields", path: WlmGetQueryGroupPath{Name: "test-name"}, wantPath: "/_wlm/query_group/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}

func TestWlmUpdateQueryGroupPath_Build(t *testing.T) {
	t.Parallel()
	tests := []struct {
		name     string
		path     WlmUpdateQueryGroupPath
		wantPath string
		wantErr  bool
	}{
		{name: "required fields empty", path: WlmUpdateQueryGroupPath{}, wantPath: "", wantErr: true},
		{name: "all fields", path: WlmUpdateQueryGroupPath{Name: "test-name"}, wantPath: "/_wlm/query_group/test-name", wantErr: false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			t.Parallel()
			gotPath, err := tt.path.Build()
			if tt.wantErr {
				require.ErrorIs(t, err, errRequired)
				return
			}
			require.NoError(t, err)
			require.Equal(t, tt.wantPath, gotPath)
			assertPathRoundTrip(t, gotPath)
		})
	}
}
