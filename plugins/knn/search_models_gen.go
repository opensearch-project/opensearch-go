// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package knn

import (
	"bytes"
	"io"
	"net/http"
	"strconv"
	"strings"
	"time"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
	"github.com/opensearch-project/opensearch-go/v5/opensearchapi"
)

// SearchModelsReq represents the request for the knn.search_models operation.
//
// Use an OpenSearch query to search for models in the index.
//
// Path: /_plugins/_knn/models/_search
//
// Methods: GET, POST
//
// Available: >= 1.0.0.
//
// See: https://docs.opensearch.org/latest/vector-search/api/knn/#search-for-a-model
type SearchModelsReq struct {

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *SearchModelsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r SearchModelsReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.KNNSearchModelsPath{}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		r.Body,
		params,
		r.Header,
	)
}

// SearchModelsParams represents query parameters for the SearchModelsReq.
type SearchModelsParams struct {
	opensearchapi.TimeoutParams
	opensearchapi.DebugParams
	// Set to `true` or `false` to return the `_source` field or not, or a list
	// of fields to return.
	Source []string

	// List of fields to exclude from the returned `_source` field.
	SourceExcludes []string

	// List of fields to extract and return from the `_source` field.
	SourceIncludes []string

	// Whether to ignore if a wildcard indexes expression resolves into no
	// concrete indexes. (This includes `_all` string or when no indexes have
	// been specified).
	AllowNoIndices *bool

	// Indicate if an error should be returned if there is a partial search
	// failure or timeout.
	//
	// Default: true.
	AllowPartialSearchResults *bool

	// Specify whether wildcard and prefix queries should be analyzed.
	//
	// Default: false.
	AnalyzeWildcard *bool

	// The analyzer to use for the query string.
	Analyzer string

	// The number of shard results that should be reduced at once on the
	// coordinating node. This value should be used as a protection mechanism
	// to reduce the memory overhead per search request if the potential number
	// of shards in the request can be large.
	//
	// Default: 512.
	BatchedReduceSize int

	// Indicates whether network round-trips should be minimized as part of
	// cross-cluster search requests execution.
	//
	// Default: true.
	CcsMinimizeRoundtrips *bool

	// The default operator for query string query (AND or OR).
	DefaultOperator string

	// The field to use as default where no field prefix is given in the query
	// string.
	Df string

	// A comma-separated list of fields to return as the docvalue
	// representation of a field for each hit.
	DocvalueFields []string

	// Whether to expand wildcard expression to concrete indexes that are open,
	// closed or both.
	ExpandWildcards []string

	// Specify whether to return detailed information about score computation
	// as part of a hit.
	Explain *bool

	// Starting offset.
	//
	// Default: 0.
	From int

	// Whether specified concrete, expanded or aliased indexes should be
	// ignored when throttled.
	IgnoreThrottled *bool

	// Whether specified concrete indexes should be ignored when unavailable
	// (missing or closed).
	IgnoreUnavailable *bool

	// Specify whether format-based query failures (such as providing text to a
	// numeric field) should be ignored.
	Lenient *bool

	// The number of concurrent shard requests per node this search executes
	// concurrently. This value should be used to limit the impact of the
	// search on the cluster in order to limit the number of concurrent shard
	// requests.
	//
	// Default: 5.
	MaxConcurrentShardRequests int

	// Threshold that enforces a pre-filter round-trip to prefilter search
	// shards based on query rewriting if the number of shards the search
	// request expands to exceeds the threshold. This filter round-trip can
	// limit the number of shards significantly if for instance a shard can not
	// match any documents based on its rewrite method, that is if date filters
	// are mandatory to match but the shard bounds and the query are disjoint.
	PreFilterShardSize int

	// Specify the node or shard the operation should be performed on.
	//
	// Default: random.
	Preference string

	// Query in the Lucene query string syntax.
	Q string

	// Specify if request cache should be used for this request or not,
	// defaults to index level setting.
	RequestCache *bool

	// Indicates whether `hits.total` should be rendered as an integer or an
	// object in the rest search response.
	//
	// Default: false.
	RestTotalHitsAsInt *bool

	// A comma-separated list of specific routing values.
	Routing []string

	// Specify how long a consistent view of the index should be maintained for
	// scrolled search.
	Scroll time.Duration

	// Search operation type.
	SearchType string

	// Specify whether to return sequence number and primary term of the last
	// modification of each hit.
	SeqNoPrimaryTerm *bool

	// Number of hits to return.
	//
	// Default: 10.
	Size int

	// A comma-separated list of <field>:<direction> pairs.
	Sort []string

	// Specific 'tag' of the request for logging and statistical purposes.
	Stats []string

	// A comma-separated list of stored fields to return.
	StoredFields []string

	// Specify which field to use for suggestions.
	SuggestField string

	// Specify suggest mode.
	SuggestMode string

	// How many suggestions to return in response.
	SuggestSize int

	// The source text for which the suggestions should be returned.
	SuggestText string

	// The maximum number of documents to collect for each shard, upon reaching
	// which the query execution will terminate early.
	TerminateAfter int

	// Whether to calculate and return scores even if they are not used for
	// sorting.
	TrackScores *bool

	// Indicate if the number of documents that match the query should be
	// tracked.
	TrackTotalHits *bool

	// Specify whether aggregation and suggester names should be prefixed by
	// their respective types in the response.
	TypedKeys *bool

	// Whether to return document version as part of a hit.
	Version *bool
}

func (r SearchModelsParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if len(r.Source) > 0 {
		set("_source", strings.Join(r.Source, ","))
	}

	if len(r.SourceExcludes) > 0 {
		set("_source_excludes", strings.Join(r.SourceExcludes, ","))
	}

	if len(r.SourceIncludes) > 0 {
		set("_source_includes", strings.Join(r.SourceIncludes, ","))
	}

	if r.AllowNoIndices != nil {
		set("allow_no_indices", strconv.FormatBool(*r.AllowNoIndices))
	}

	if r.AllowPartialSearchResults != nil {
		set("allow_partial_search_results", strconv.FormatBool(*r.AllowPartialSearchResults))
	}

	if r.AnalyzeWildcard != nil {
		set("analyze_wildcard", strconv.FormatBool(*r.AnalyzeWildcard))
	}

	if r.Analyzer != "" {
		set("analyzer", r.Analyzer)
	}

	if r.BatchedReduceSize != 0 {
		set("batched_reduce_size", strconv.Itoa(r.BatchedReduceSize))
	}

	if r.CcsMinimizeRoundtrips != nil {
		set("ccs_minimize_roundtrips", strconv.FormatBool(*r.CcsMinimizeRoundtrips))
	}

	if r.DefaultOperator != "" {
		set("default_operator", r.DefaultOperator)
	}

	if r.Df != "" {
		set("df", r.Df)
	}

	if len(r.DocvalueFields) > 0 {
		set("docvalue_fields", strings.Join(r.DocvalueFields, ","))
	}

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	if r.Explain != nil {
		set("explain", strconv.FormatBool(*r.Explain))
	}

	if r.From != 0 {
		set("from", strconv.Itoa(r.From))
	}

	if r.IgnoreThrottled != nil {
		set("ignore_throttled", strconv.FormatBool(*r.IgnoreThrottled))
	}

	if r.IgnoreUnavailable != nil {
		set("ignore_unavailable", strconv.FormatBool(*r.IgnoreUnavailable))
	}

	if r.Lenient != nil {
		set("lenient", strconv.FormatBool(*r.Lenient))
	}

	if r.MaxConcurrentShardRequests != 0 {
		set("max_concurrent_shard_requests", strconv.Itoa(r.MaxConcurrentShardRequests))
	}

	if r.PreFilterShardSize != 0 {
		set("pre_filter_shard_size", strconv.Itoa(r.PreFilterShardSize))
	}

	if r.Preference != "" {
		set("preference", r.Preference)
	}

	if r.Q != "" {
		set("q", r.Q)
	}

	if r.RequestCache != nil {
		set("request_cache", strconv.FormatBool(*r.RequestCache))
	}

	if r.RestTotalHitsAsInt != nil {
		set("rest_total_hits_as_int", strconv.FormatBool(*r.RestTotalHitsAsInt))
	}

	if len(r.Routing) > 0 {
		set("routing", strings.Join(r.Routing, ","))
	}

	if r.Scroll != 0 {
		set("scroll", formatDuration(r.Scroll))
	}

	if r.SearchType != "" {
		set("search_type", r.SearchType)
	}

	if r.SeqNoPrimaryTerm != nil {
		set("seq_no_primary_term", strconv.FormatBool(*r.SeqNoPrimaryTerm))
	}

	if r.Size != 0 {
		set("size", strconv.Itoa(r.Size))
	}

	if len(r.Sort) > 0 {
		set("sort", strings.Join(r.Sort, ","))
	}

	if len(r.Stats) > 0 {
		set("stats", strings.Join(r.Stats, ","))
	}

	if len(r.StoredFields) > 0 {
		set("stored_fields", strings.Join(r.StoredFields, ","))
	}

	if r.SuggestField != "" {
		set("suggest_field", r.SuggestField)
	}

	if r.SuggestMode != "" {
		set("suggest_mode", r.SuggestMode)
	}

	if r.SuggestSize != 0 {
		set("suggest_size", strconv.Itoa(r.SuggestSize))
	}

	if r.SuggestText != "" {
		set("suggest_text", r.SuggestText)
	}

	if r.TerminateAfter != 0 {
		set("terminate_after", strconv.Itoa(r.TerminateAfter))
	}

	if r.TrackScores != nil {
		set("track_scores", strconv.FormatBool(*r.TrackScores))
	}

	if r.TrackTotalHits != nil {
		set("track_total_hits", strconv.FormatBool(*r.TrackTotalHits))
	}

	if r.TypedKeys != nil {
		set("typed_keys", strconv.FormatBool(*r.TypedKeys))
	}

	if r.Version != nil {
		set("version", strconv.FormatBool(*r.Version))
	}

	return params
}

// SearchModelsResp represents the response for the knn.search_models operation.
//
// Use an OpenSearch query to search for models in the index.
//
// Available: >= 1.0.0.
//
// See: https://docs.opensearch.org/latest/vector-search/api/knn/#search-for-a-model
type SearchModelsResp struct {
	opensearchapi.SearchResultJSONValue

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r SearchModelsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r SearchModelsResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}
