// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package list

import (
	"bytes"
	"io"
	"net/http"
	"strconv"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v5"
	"github.com/opensearch-project/opensearch-go/v5/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v5/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v5/internal/path"
	"github.com/opensearch-project/opensearch-go/v5/opensearchapi"
)

// IndicesReq represents the request for the list.indices operation.
//
// Returns paginated information about indexes including number of primaries and replicas, document counts, disk size.
//
// GET /_list/indices
//
// Available: >= 2.18.0.
//
// See: https://opensearch.org/docs/latest/api-reference/list/list-indices/
type IndicesReq struct {
	// Indices specifies the list of path segments for the request URL.
	Indices []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *IndicesParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.ListIndicesPath{
		Indices: r.Indices,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	} else {
		// This operation defaults the format query param (the typed Resp
		// only decodes that format). The default lives in Params.get(), so
		// build it from a zero-value Params when the caller passes none.
		params = IndicesParams{}.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// IndicesParams represents query parameters for the IndicesReq.
type IndicesParams struct {
	opensearchapi.TimeoutParams
	opensearchapi.DebugParams
	// The unit used to display byte values.
	Bytes string

	// The type of index that wildcard patterns can match.
	ExpandWildcards []string

	// The health status used to limit returned indexes. By default, the
	// response includes indexes of any health status.
	Health string

	// If `true`, the response includes information from segments that are not
	// loaded into memory.
	//
	// Default: false.
	IncludeUnloadedSegments *bool

	// Return local information, do not retrieve the state from cluster-manager
	// node.
	//
	// Default: false.
	Local *bool

	// Token to retrieve next page of indexes.
	NextToken string

	// If `true`, the response only includes information from primary shards.
	//
	// Default: false.
	Pri *bool

	// Maximum number of indexes to be displayed in a page.
	Size int

	// Defines order in which indexes will be displayed. Accepted values are
	// `asc` and `desc`. If `desc`, most recently created indexes would be
	// displayed first.
	Sort string

	// The unit used to display time values.
	Time opensearchapi.TimeUnit
}

func (r IndicesParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)
	if r.Format == "" {
		set("format", "json")
	}

	if r.Bytes != "" {
		set("bytes", r.Bytes)
	}

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	if r.Health != "" {
		set("health", r.Health)
	}

	if r.IncludeUnloadedSegments != nil {
		set("include_unloaded_segments", strconv.FormatBool(*r.IncludeUnloadedSegments))
	}

	if r.Local != nil {
		set("local", strconv.FormatBool(*r.Local))
	}

	if r.NextToken != "" {
		set("next_token", r.NextToken)
	}

	if r.Pri != nil {
		set("pri", strconv.FormatBool(*r.Pri))
	}

	if r.Size != 0 {
		set("size", strconv.Itoa(r.Size))
	}

	if r.Sort != "" {
		set("sort", r.Sort)
	}

	if r.Time != "" {
		set("time", string(r.Time))
	}

	return params
}

// IndicesResp represents the response for the list.indices operation.
//
// Returns paginated information about indexes including number of primaries and replicas, document counts, disk size.
//
// Available: >= 2.18.0.
//
// See: https://opensearch.org/docs/latest/api-reference/list/list-indices/
type IndicesResp struct {
	Indices   []opensearchapi.CatIndicesRecord `json:"indices,omitempty"`
	NextToken *string                          `json:"next_token"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r IndicesResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}
