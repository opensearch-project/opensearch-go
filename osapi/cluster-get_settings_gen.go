// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"encoding/json"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// ClusterGetSettingsReq represents the request for the cluster.get_settings operation.
//
// Returns cluster settings.
//
// GET /_cluster/settings
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-settings/
type ClusterGetSettingsReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params ClusterGetSettingsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r ClusterGetSettingsReq) GetRequest() (*http.Request, error) {
	path, err := ospath.ClusterGetSettingsPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// ClusterGetSettingsParams represents query parameters for the ClusterGetSettingsReq.
type ClusterGetSettingsParams struct {
	// The amount of time to wait for a response from the cluster manager node.
	// For more information about supported time units, see [Common
	// parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// Whether to return settings in the flat form, which can improve
	// readability, especially for heavily nested settings. For example, the
	// flat form of `"cluster": { "max_shards_per_node": 500 }` is
	// `"cluster.max_shards_per_node": "500"`.
	//
	// Default: false.
	FlatSettings bool

	// When `true`, returns default cluster settings from the local node.
	//
	// Default: false.
	IncludeDefaults bool

	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	Timeout time.Duration

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r ClusterGetSettingsParams) get() map[string]string {
	params := make(map[string]string, 5)

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.FlatSettings {
		params["flat_settings"] = "true"
	}

	if r.IncludeDefaults {
		params["include_defaults"] = "true"
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if r.Timeout != 0 {
		params["timeout"] = formatDuration(r.Timeout)
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// ClusterGetSettingsResp represents the response for the cluster.get_settings operation.
//
// Returns cluster settings.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-settings/
type ClusterGetSettingsResp struct {
	Defaults   map[string]json.RawMessage `json:"defaults,omitempty"`
	Persistent map[string]json.RawMessage `json:"persistent"`
	Transient  map[string]json.RawMessage `json:"transient"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r ClusterGetSettingsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c clusterClient) GetSettings(ctx context.Context, req *ClusterGetSettingsReq) (*ClusterGetSettingsResp, error) {
	if req == nil {
		req = &ClusterGetSettingsReq{}
	}

	var (
		data ClusterGetSettingsResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
