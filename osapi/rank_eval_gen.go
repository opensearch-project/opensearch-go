// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// RankEvalReq represents the request for the rank_eval operation.
//
// Allows to evaluate the quality of ranked search results over a set of typical search queries.
//
// Path: /_rank_eval
//
// Methods: GET, POST
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/rank-eval/
type RankEvalReq struct {
	// Index specifies the list of path segments for the request URL.
	Index []string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params RankEvalParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r RankEvalReq) GetRequest() (*http.Request, error) {
	path, err := ospath.RankEvalPath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}
	method := http.MethodGet
	if r.Body != nil {
		method = http.MethodPost
	}

	return opensearch.BuildRequest(
		method,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// RankEvalParams represents query parameters for the RankEvalReq.
type RankEvalParams struct {
	// If `false`, the request returns an error if any wildcard expression,
	// index alias, or `_all` value targets only missing or closed indexes.
	// This behavior applies even if the request targets other open indexes.
	// For example, a request targeting `foo*,bar*` returns an error if an
	// index starts with `foo` but no index starts with `bar`.
	AllowNoIndices bool

	// Whether to expand wildcard expression to concrete indexes that are open,
	// closed or both.
	ExpandWildcards []string

	// If `true`, missing or closed indexes are not included in the response.
	IgnoreUnavailable bool

	// Search operation type
	SearchType string

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r RankEvalParams) get() map[string]string {
	params := make(map[string]string, 4)

	if r.AllowNoIndices {
		params["allow_no_indices"] = "true"
	}

	if len(r.ExpandWildcards) > 0 {
		params["expand_wildcards"] = strings.Join(r.ExpandWildcards, ",")
	}

	if r.IgnoreUnavailable {
		params["ignore_unavailable"] = "true"
	}

	if r.SearchType != "" {
		params["search_type"] = r.SearchType
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// RankEvalResp represents the response for the rank_eval operation.
//
// Allows to evaluate the quality of ranked search results over a set of typical search queries.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/rank-eval/
type RankEvalResp struct {
	// The details section contains one entry for every query in the original
	// requests section, keyed by the search request id
	Details map[string]RankEvalMetricDetail `json:"details"`

	Failures map[string]json.RawMessage `json:"failures"`

	// The overall evaluation quality calculated by the defined metric
	MetricScore float64 `json:"metric_score"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r RankEvalResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RankEvalMetricDetail is a typed component of the rank_eval response.
type RankEvalMetricDetail struct {
	// The `hits` section provides a grouping of the search results with their
	// supplied ratings.
	Hits []RankEvalHitItem `json:"hits"`

	// The `metric_details` section provides additional information about the
	// calculated quality metric indicating the number of relevant retrieved
	// documents. The content varies for each metric but allows for better
	// interpretation of the results.
	MetricDetails map[string]map[string]json.RawMessage `json:"metric_details"`

	// The `metric_score`, found in the `metric_details` section, shows the
	// contribution of this query to the global quality metric score.
	MetricScore float64 `json:"metric_score"`

	// The `unrated_docs` section contains an `_index` and `_id` entry for each
	// document that didn't have a `ratings` value. This can be used to ask the
	// user to supply ratings for these documents.
	UnratedDocs []RankEvalUnratedDocument `json:"unrated_docs"`
}

// RankEvalHitItem is a typed component of the rank_eval response.
type RankEvalHitItem struct {
	Hit    RankEvalHit `json:"hit"`
	Rating *int        `json:"rating,omitempty"`
}

// RankEvalHit is a typed component of the rank_eval response.
type RankEvalHit struct {
	// The unique identifier for a resource.
	ID string `json:"_id"`

	Index string  `json:"_index"`
	Score float64 `json:"_score"`

	// The type of document or resource.
	Type *string `json:"_type,omitempty"`
}

// RankEvalUnratedDocument is a typed component of the rank_eval response.
type RankEvalUnratedDocument struct {
	// The unique identifier for a resource.
	ID string `json:"_id"`

	Index string `json:"_index"`
}

func (c Client) RankEval(ctx context.Context, req *RankEvalReq) (*RankEvalResp, error) {
	if req == nil {
		req = &RankEvalReq{}
	}

	var (
		data RankEvalResp
		err  error
	)
	if data.response, err = do(ctx, &c, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
