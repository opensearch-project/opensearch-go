// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IndicesUpgradeReq represents the request for the indices.upgrade operation.
//
// The `_upgrade` API is no longer useful and will be removed.
//
// POST /_upgrade
//
// Deprecated: since 1.0.0. Available >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesUpgradeReq struct {
	// Index specifies the list of path segments for the request URL.
	Index []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params IndicesUpgradeParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesUpgradeReq) GetRequest() (*http.Request, error) {
	path, err := ospath.IndicesUpgradePath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// IndicesUpgradeParams represents query parameters for the IndicesUpgradeReq.
type IndicesUpgradeParams struct {
	// Whether to ignore if a wildcard indexes expression resolves into no
	// concrete indexes. (This includes `_all` string or when no indexes have
	// been specified).
	AllowNoIndices bool

	// Whether to expand wildcard expression to concrete indexes that are open,
	// closed or both.
	ExpandWildcards []string

	// Whether specified concrete indexes should be ignored when unavailable
	// (missing or closed).
	IgnoreUnavailable bool

	// If `true`, only ancient (an older Lucene major release) segments will be
	// upgraded.
	OnlyAncientSegments bool

	// Should this request wait until the operation has completed before
	// returning.
	//
	// Available: >= 2.7.0.
	//
	// Default: false.
	WaitForCompletion bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r IndicesUpgradeParams) get() map[string]string {
	params := make(map[string]string, 5)

	if r.AllowNoIndices {
		params["allow_no_indices"] = "true"
	}

	if len(r.ExpandWildcards) > 0 {
		params["expand_wildcards"] = strings.Join(r.ExpandWildcards, ",")
	}

	if r.IgnoreUnavailable {
		params["ignore_unavailable"] = "true"
	}

	if r.OnlyAncientSegments {
		params["only_ancient_segments"] = "true"
	}

	if r.WaitForCompletion {
		params["wait_for_completion"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// IndicesUpgradeResp represents the response for the indices.upgrade operation.
//
// The `_upgrade` API is no longer useful and will be removed.
//
// Deprecated: since 1.0.0. Available >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesUpgradeResp struct {
	ShardsOperationResponseBase
	UpgradedIndices map[string]IndicesUpgradeVersionStatus `json:"upgraded_indices,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesUpgradeResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c indicesClient) Upgrade(ctx context.Context, req *IndicesUpgradeReq) (*IndicesUpgradeResp, error) {
	if req == nil {
		req = &IndicesUpgradeReq{}
	}

	var (
		data IndicesUpgradeResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
