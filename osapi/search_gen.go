// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strconv"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// SearchReq represents the request for the search operation.
//
// Returns results matching a query.
//
// Path: /_search
//
// Methods: GET, POST
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/search/
type SearchReq struct {
	// Index specifies the list of path segments for the request URL.
	Index []string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params SearchParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r SearchReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SearchPath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}
	method := http.MethodGet
	if r.Body != nil {
		method = http.MethodPost
	}

	return opensearch.BuildRequest(
		method,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// SearchParams represents query parameters for the SearchReq.
type SearchParams struct {
	// Indicates which source fields are returned for matching documents. These
	// fields are returned in the `hits._source` property of the search
	// response. Valid values are: `true` to return the entire document source;
	// `false` to not return the document source; `<string>` to return the
	// source fields that are specified as a comma-separated list (supports
	// wildcard (`*`) patterns).
	Source string

	// A comma-separated list of source fields to exclude from the response.
	// You can also use this parameter to exclude fields from the subset
	// specified in `_source_includes` query parameter. If the `_source`
	// parameter is `false`, this parameter is ignored.
	SourceExcludes []string

	// A comma-separated list of source fields to include in the response. If
	// this parameter is specified, only these source fields are returned. You
	// can exclude fields from this subset using the `_source_excludes` query
	// parameter. If the `_source` parameter is `false`, this parameter is
	// ignored.
	SourceIncludes []string

	// If `false`, the request returns an error if any wildcard expression,
	// index alias, or `_all` value targets only missing or closed indexes.
	// This behavior applies even if the request targets other open indexes.
	// For example, a request targeting `foo*,bar*` returns an error if an
	// index starts with `foo` but no index starts with `bar`.
	AllowNoIndices bool

	// If `true`, returns partial results if there are shard request timeouts
	// or shard failures. If `false`, returns an error with no partial results.
	//
	// Default: true.
	AllowPartialSearchResults bool

	// If `true`, wildcard and prefix queries are analyzed. This parameter can
	// only be used when the q query string parameter is specified.
	//
	// Default: false.
	AnalyzeWildcard bool

	// Analyzer to use for the query string. This parameter can only be used
	// when the q query string parameter is specified.
	Analyzer string

	// The number of shard results that should be reduced at once on the
	// coordinating node. This value should be used as a protection mechanism
	// to reduce the memory overhead per search request if the potential number
	// of shards in the request can be large.
	//
	// Default: 512.
	BatchedReduceSize int

	// The time after which the search request will be canceled. Request-level
	// parameter takes precedence over `cancel_after_time_interval` cluster
	// setting.
	CancelAfterTimeInterval time.Duration

	// If `true`, network round-trips between the coordinating node and the
	// remote clusters are minimized when executing cross-cluster search (CCS)
	// requests.
	//
	// Default: true.
	CcsMinimizeRoundtrips bool

	// The default operator for query string query: AND or OR. This parameter
	// can only be used when the `q` query string parameter is specified.
	DefaultOperator string

	// Field to use as default where no field prefix is given in the query
	// string. This parameter can only be used when the q query string
	// parameter is specified.
	Df string

	// A comma-separated list of fields to return as the docvalue
	// representation for each hit.
	DocvalueFields []string

	// Type of index that wildcard patterns can match. If the request can
	// target data streams, this argument determines whether wildcard
	// expressions match hidden data streams. Supports comma-separated values,
	// such as `open,hidden`.
	ExpandWildcards []string

	// If `true`, returns detailed information about score computation as part
	// of a hit.
	Explain bool

	// Starting document offset. Needs to be non-negative. By default, you
	// cannot page through more than 10,000 hits using the `from` and `size`
	// parameters. To page through more hits, use the `search_after` parameter.
	//
	// Default: 0.
	From int

	// If `true`, concrete, expanded or aliased indexes will be ignored when
	// frozen.
	IgnoreThrottled bool

	// If `false`, the request returns an error if it targets a missing or
	// closed index.
	IgnoreUnavailable bool

	// Indicates whether `hit.matched_queries` should be rendered as a map that
	// includes the name of the matched query associated with its score (true)
	// or as an array containing the name of the matched queries (false)
	//
	// Default: false.
	IncludeNamedQueriesScore bool

	// If `true`, format-based query failures (such as providing text to a
	// numeric field) in the query string will be ignored. This parameter can
	// only be used when the `q` query string parameter is specified.
	Lenient bool

	// Defines the number of concurrent shard requests per node this search
	// executes concurrently. This value should be used to limit the impact of
	// the search on the cluster in order to limit the number of concurrent
	// shard requests.
	//
	// Default: 5.
	MaxConcurrentShardRequests int

	// Indicates whether to return phase-level `took` time values in the
	// response.
	//
	// Default: false.
	PhaseTook bool

	// Defines a threshold that enforces a pre-filter roundtrip to prefilter
	// search shards based on query rewriting if the number of shards the
	// search request expands to exceeds the threshold. This filter roundtrip
	// can limit the number of shards significantly if for instance a shard can
	// not match any documents based on its rewrite method (if date filters are
	// mandatory to match but the shard bounds and the query are disjoint).
	// When unspecified, the pre-filter phase is executed if any of these
	// conditions is met: the request targets more than 128 shards; the request
	// targets one or more read-only index; the primary sort of the query
	// targets an indexed field.
	PreFilterShardSize int

	// Nodes and shards used for the search. By default, OpenSearch selects
	// from eligible nodes and shards using adaptive replica selection,
	// accounting for allocation awareness. Valid values are: `_only_local` to
	// run the search only on shards on the local node; `_local` to, if
	// possible, run the search on shards on the local node, or if not, select
	// shards using the default method; `_only_nodes:<node-id>,<node-id>` to
	// run the search on only the specified nodes IDs, where, if suitable
	// shards exist on more than one selected node, use shards on those nodes
	// using the default method, or if none of the specified nodes are
	// available, select shards from any available node using the default
	// method; `_prefer_nodes:<node-id>,<node-id>` to if possible, run the
	// search on the specified nodes IDs, or if not, select shards using the
	// default method; `_shards:<shard>,<shard>` to run the search only on the
	// specified shards; `<custom-string>` (any string that does not start with
	// `_`) to route searches with the same `<custom-string>` to the same
	// shards in the same order.
	//
	// Default: random.
	Preference string

	// Query in the Lucene query string syntax using query parameter search.
	// Query parameter searches do not support the full OpenSearch Query DSL
	// but are handy for testing.
	Q string

	// If `true`, the caching of search results is enabled for requests where
	// `size` is `0`. Defaults to index level settings.
	RequestCache bool

	// Indicates whether `hits.total` should be rendered as an integer or an
	// object in the rest search response.
	//
	// Default: false.
	RestTotalHitsAsInt bool

	// Custom value used to route operations to a specific shard.
	Routing []string

	// Period to retain the search context for scrolling. See Scroll search
	// results. By default, this value cannot exceed `1d` (24 hours). You can
	// change this limit using the `search.max_keep_alive` cluster-level
	// setting.
	Scroll time.Duration

	// Customizable sequence of processing stages applied to search queries.
	SearchPipeline string

	// How distributed term frequencies are calculated for relevance scoring.
	SearchType string

	// If `true`, returns sequence number and primary term of the last
	// modification of each hit.
	SeqNoPrimaryTerm bool

	// Defines the number of hits to return. By default, you cannot page
	// through more than 10,000 hits using the `from` and `size` parameters. To
	// page through more hits, use the `search_after` parameter.
	//
	// Default: 10.
	Size int

	// A comma-separated list of <field>:<direction> pairs.
	Sort []string

	// Specific `tag` of the request for logging and statistical purposes.
	Stats []string

	// A comma-separated list of stored fields to return as part of a hit. If
	// no fields are specified, no stored fields are included in the response.
	// If this field is specified, the `_source` parameter defaults to `false`.
	// You can pass `_source: true` to return both source fields and stored
	// fields in the search response.
	StoredFields []string

	// Specifies which field to use for suggestions.
	SuggestField string

	// Specifies the suggest mode. This parameter can only be used when the
	// `suggest_field` and `suggest_text` query string parameters are
	// specified.
	SuggestMode string

	// Number of suggestions to return. This parameter can only be used when
	// the `suggest_field` and `suggest_text` query string parameters are
	// specified.
	SuggestSize int

	// The source text for which the suggestions should be returned. This
	// parameter can only be used when the `suggest_field` and `suggest_text`
	// query string parameters are specified.
	SuggestText string

	// Maximum number of documents to collect for each shard. If a query
	// reaches this limit, OpenSearch terminates the query early. OpenSearch
	// collects documents before sorting. Use with caution. OpenSearch applies
	// this parameter to each shard handling the request. When possible, let
	// OpenSearch perform early termination automatically. Avoid specifying
	// this parameter for requests that target data streams with backing
	// indexes across multiple data tiers. If set to `0` (default), the query
	// does not terminate early.
	TerminateAfter int

	// Specifies the period of time to wait for a response from each shard. If
	// no response is received before the timeout expires, the request fails
	// and returns an error.
	Timeout time.Duration

	// If `true`, calculate and return document scores, even if the scores are
	// not used for sorting.
	TrackScores bool

	// Number of hits matching the query to count accurately. If `true`, the
	// exact number of hits is returned at the cost of some performance. If
	// `false`, the response does not include the total number of hits matching
	// the query.
	TrackTotalHits string

	// If `true`, aggregation and suggester names are be prefixed by their
	// respective types in the response.
	TypedKeys bool

	// Enables or disables verbose mode for the search pipeline. When verbose
	// mode is enabled, detailed information about each processor in the search
	// pipeline is included in the search response. This includes the processor
	// name, execution status, input, output, and time taken for processing.
	// This parameter is primarily intended for debugging purposes, allowing
	// users to track how data flows and transforms through the search
	// pipeline.
	//
	// Available: >= 3.0.0.
	VerbosePipeline bool

	// If `true`, returns document version as part of a hit.
	Version bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r SearchParams) get() map[string]string {
	params := make(map[string]string, 47)

	if r.Source != "" {
		params["_source"] = r.Source
	}

	if len(r.SourceExcludes) > 0 {
		params["_source_excludes"] = strings.Join(r.SourceExcludes, ",")
	}

	if len(r.SourceIncludes) > 0 {
		params["_source_includes"] = strings.Join(r.SourceIncludes, ",")
	}

	if r.AllowNoIndices {
		params["allow_no_indices"] = "true"
	}

	if r.AllowPartialSearchResults {
		params["allow_partial_search_results"] = "true"
	}

	if r.AnalyzeWildcard {
		params["analyze_wildcard"] = "true"
	}

	if r.Analyzer != "" {
		params["analyzer"] = r.Analyzer
	}

	if r.BatchedReduceSize != 0 {
		params["batched_reduce_size"] = strconv.Itoa(r.BatchedReduceSize)
	}

	if r.CancelAfterTimeInterval != 0 {
		params["cancel_after_time_interval"] = formatDuration(r.CancelAfterTimeInterval)
	}

	if r.CcsMinimizeRoundtrips {
		params["ccs_minimize_roundtrips"] = "true"
	}

	if r.DefaultOperator != "" {
		params["default_operator"] = r.DefaultOperator
	}

	if r.Df != "" {
		params["df"] = r.Df
	}

	if len(r.DocvalueFields) > 0 {
		params["docvalue_fields"] = strings.Join(r.DocvalueFields, ",")
	}

	if len(r.ExpandWildcards) > 0 {
		params["expand_wildcards"] = strings.Join(r.ExpandWildcards, ",")
	}

	if r.Explain {
		params["explain"] = "true"
	}

	if r.From != 0 {
		params["from"] = strconv.Itoa(r.From)
	}

	if r.IgnoreThrottled {
		params["ignore_throttled"] = "true"
	}

	if r.IgnoreUnavailable {
		params["ignore_unavailable"] = "true"
	}

	if r.IncludeNamedQueriesScore {
		params["include_named_queries_score"] = "true"
	}

	if r.Lenient {
		params["lenient"] = "true"
	}

	if r.MaxConcurrentShardRequests != 0 {
		params["max_concurrent_shard_requests"] = strconv.Itoa(r.MaxConcurrentShardRequests)
	}

	if r.PhaseTook {
		params["phase_took"] = "true"
	}

	if r.PreFilterShardSize != 0 {
		params["pre_filter_shard_size"] = strconv.Itoa(r.PreFilterShardSize)
	}

	if r.Preference != "" {
		params["preference"] = r.Preference
	}

	if r.Q != "" {
		params["q"] = r.Q
	}

	if r.RequestCache {
		params["request_cache"] = "true"
	}

	if r.RestTotalHitsAsInt {
		params["rest_total_hits_as_int"] = "true"
	}

	if len(r.Routing) > 0 {
		params["routing"] = strings.Join(r.Routing, ",")
	}

	if r.Scroll != 0 {
		params["scroll"] = formatDuration(r.Scroll)
	}

	if r.SearchPipeline != "" {
		params["search_pipeline"] = r.SearchPipeline
	}

	if r.SearchType != "" {
		params["search_type"] = r.SearchType
	}

	if r.SeqNoPrimaryTerm {
		params["seq_no_primary_term"] = "true"
	}

	if r.Size != 0 {
		params["size"] = strconv.Itoa(r.Size)
	}

	if len(r.Sort) > 0 {
		params["sort"] = strings.Join(r.Sort, ",")
	}

	if len(r.Stats) > 0 {
		params["stats"] = strings.Join(r.Stats, ",")
	}

	if len(r.StoredFields) > 0 {
		params["stored_fields"] = strings.Join(r.StoredFields, ",")
	}

	if r.SuggestField != "" {
		params["suggest_field"] = r.SuggestField
	}

	if r.SuggestMode != "" {
		params["suggest_mode"] = r.SuggestMode
	}

	if r.SuggestSize != 0 {
		params["suggest_size"] = strconv.Itoa(r.SuggestSize)
	}

	if r.SuggestText != "" {
		params["suggest_text"] = r.SuggestText
	}

	if r.TerminateAfter != 0 {
		params["terminate_after"] = strconv.Itoa(r.TerminateAfter)
	}

	if r.Timeout != 0 {
		params["timeout"] = formatDuration(r.Timeout)
	}

	if r.TrackScores {
		params["track_scores"] = "true"
	}

	if r.TrackTotalHits != "" {
		params["track_total_hits"] = r.TrackTotalHits
	}

	if r.TypedKeys {
		params["typed_keys"] = "true"
	}

	if r.VerbosePipeline {
		params["verbose_pipeline"] = "true"
	}

	if r.Version {
		params["version"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// SearchResp represents the response for the search operation.
//
// Returns results matching a query.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/search/
type SearchResp struct {
	Clusters        *ClusterStatistics                             `json:"_clusters,omitempty"`
	ScrollID        *string                                        `json:"_scroll_id,omitempty"`
	Shards          ShardStatistics                                `json:"_shards"`
	Aggregations    map[string]SearchResponseBodyAggregationsValue `json:"aggregations,omitempty"`
	Fields          map[string]json.RawMessage                     `json:"fields,omitempty"`
	Hits            SearchHitsMetadata                             `json:"hits"`
	NumReducePhases *int                                           `json:"num_reduce_phases,omitempty"`

	// The time taken by different phases of the search.
	PhaseTook *PhaseTook `json:"phase_took,omitempty"`

	// The unique identifier for a resource.
	PitID *string `json:"pit_id,omitempty"`

	// Available: >= 3.0.0.
	ProcessorResults []SearchProcessorExecutionDetail `json:"processor_results,omitempty"`

	Profile         *SearchProfile                                  `json:"profile,omitempty"`
	Suggest         map[string][]SearchResponseBodySuggestValueItem `json:"suggest,omitempty"`
	TerminatedEarly *bool                                           `json:"terminated_early,omitempty"`
	TimedOut        bool                                            `json:"timed_out"`
	Took            int64                                           `json:"took"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r SearchResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c Client) Search(ctx context.Context, req *SearchReq) (*SearchResp, error) {
	if req == nil {
		req = &SearchReq{}
	}

	var (
		data SearchResp
		err  error
	)
	if data.response, err = do(ctx, &c, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
