// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"encoding/json"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// CatAllocationReq represents the request for the cat.allocation operation.
//
// Provides a snapshot of how many shards are allocated to each data node and how much disk space they are using.
//
// GET /_cat/allocation
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-allocation/
type CatAllocationReq struct {
	// NodeID specifies the list of path segments for the request URL.
	NodeID []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params CatAllocationParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r CatAllocationReq) GetRequest() (*http.Request, error) {
	path, err := ospath.CatAllocationPath{
		NodeID: r.NodeID,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// CatAllocationParams represents query parameters for the CatAllocationReq.
type CatAllocationParams struct {
	// The units used to display byte values.
	Bytes string

	// A timeout for connection to the cluster manager node.
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// A short version of the HTTP `Accept` header, such as `json` or `yaml`.
	Format string

	// A comma-separated list of column names to display.
	H []string

	// Returns help information.
	//
	// Default: false.
	Help bool

	// Returns local information but does not retrieve the state from
	// cluster-manager node.
	//
	// Default: false.
	Local bool

	// A timeout for connection to the cluster manager node.
	//
	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	// A comma-separated list of column names or column aliases to sort by.
	S []string

	// Enables verbose mode, which displays column headers.
	//
	// Default: false.
	V bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r CatAllocationParams) get() map[string]string {
	params := make(map[string]string, 9)

	if r.Bytes != "" {
		params["bytes"] = r.Bytes
	}

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.Format != "" {
		params["format"] = r.Format
	}

	if len(r.H) > 0 {
		params["h"] = strings.Join(r.H, ",")
	}

	if r.Help {
		params["help"] = "true"
	}

	if r.Local {
		params["local"] = "true"
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if len(r.S) > 0 {
		params["s"] = strings.Join(r.S, ",")
	}

	if r.V {
		params["v"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// CatAllocationResp represents the response for the cat.allocation operation.
//
// Provides a snapshot of how many shards are allocated to each data node and how much disk space they are using.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-allocation/
type CatAllocationResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r CatAllocationResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// CatAllocationRecord is a typed component of the cat.allocation response.
type CatAllocationRecord struct {
	// Free disk space available to OpenSearch. OpenSearch retrieves this
	// metric from the node's operating system. Disk-based shard allocation
	// uses this metric to assign shards to nodes based on available disk
	// space.
	DiskAvail *json.RawMessage `json:"disk.avail,omitempty"`

	// Disk space used by the node's shards. Does not include disk space for
	// the translog or unassigned shards. IMPORTANT: This metric double-counts
	// disk space for hard-linked files, such as those created when shrinking,
	// splitting, or cloning an index.
	DiskIndices *json.RawMessage `json:"disk.indices,omitempty"`

	// Total percentage of disk space in use. Calculated as `disk.used /
	// disk.total`.
	DiskPercent *string `json:"disk.percent,omitempty"`

	// Total disk space for the node, including in-use and available space.
	DiskTotal *json.RawMessage `json:"disk.total,omitempty"`

	// Total disk space in use. OpenSearch retrieves this metric from the
	// node's operating system (OS). The metric includes disk space for:
	// OpenSearch, including the translog and unassigned shards; the node's
	// operating system; any other applications or files on the node. Unlike
	// `disk.indices`, this metric does not double-count disk space for
	// hard-linked files.
	DiskUsed *json.RawMessage `json:"disk.used,omitempty"`

	// Network host for the node. Set using the `network.host` setting.
	Host *string `json:"host,omitempty"`

	// IP address and port for the node.
	IP *string `json:"ip,omitempty"`

	// Name for the node. Set using the `node.name` setting.
	Node *string `json:"node,omitempty"`

	// Number of primary and replica shards assigned to the node.
	Shards *string `json:"shards,omitempty"`
}

func (c catClient) Allocation(ctx context.Context, req *CatAllocationReq) (*CatAllocationResp, error) {
	if req == nil {
		req = &CatAllocationReq{}
	}

	var (
		data CatAllocationResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
