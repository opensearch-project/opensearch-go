// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"io"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// SnapshotCloneReq represents the request for the snapshot.clone operation.
//
// Creates a clone of all or part of a snapshot in the same repository as the original snapshot.
//
// PUT /_snapshot/{repository}/{snapshot}/_clone/{target_snapshot}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type SnapshotCloneReq struct {
	// Repository specifies the path segment for the request URL.
	Repository string

	// Snapshot specifies the path segment for the request URL.
	Snapshot string

	// TargetSnapshot specifies the path segment for the request URL.
	TargetSnapshot string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params SnapshotCloneParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r SnapshotCloneReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SnapshotClonePath{
		Repository:     r.Repository,
		Snapshot:       r.Snapshot,
		TargetSnapshot: r.TargetSnapshot,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPut,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// SnapshotCloneParams represents query parameters for the SnapshotCloneReq.
type SnapshotCloneParams struct {
	// The amount of time to wait for a response from the cluster manager node.
	// For more information about supported time units, see [Common
	// parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// Explicit operation timeout for connection to cluster-manager node
	//
	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r SnapshotCloneParams) get() map[string]string {
	params := make(map[string]string, 2)

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// SnapshotCloneResp represents the response for the snapshot.clone operation.
//
// Creates a clone of all or part of a snapshot in the same repository as the original snapshot.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type SnapshotCloneResp struct {
	// For a successful response, this value is always true. On failure, an
	// exception is returned instead.
	Acknowledged bool `json:"acknowledged"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r SnapshotCloneResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c snapshotClient) Clone(ctx context.Context, req SnapshotCloneReq) (*SnapshotCloneResp, error) {
	var (
		data SnapshotCloneResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
