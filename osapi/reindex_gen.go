// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"io"
	"net/http"
	"strconv"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// ReindexReq represents the request for the reindex operation.
//
// Allows to copy documents from one index to another, optionally filtering the source
// documents by a query, changing the destination index settings, or fetching the
// documents from a remote cluster.
//
// POST /_reindex
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/reindex-data/
type ReindexReq struct {

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params ReindexParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r ReindexReq) GetRequest() (*http.Request, error) {
	path, err := ospath.ReindexPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// ReindexParams represents query parameters for the ReindexReq.
type ReindexParams struct {
	// Maximum number of documents to process. By default, all documents.
	MaxDocs int

	// If `true`, the request refreshes affected shards to make this operation
	// visible to search.
	Refresh string

	// The throttle for this request in sub-requests per second. Defaults to no
	// throttle.
	//
	// Default: 0.
	RequestsPerSecond int

	RequireAlias bool

	// Specifies how long a consistent view of the index should be maintained
	// for scrolled search.
	Scroll time.Duration

	// The number of slices this task should be divided into. Defaults to 1
	// slice, meaning the task isn't sliced into subtasks.
	Slices string

	// Period each indexing waits for automatic index creation, dynamic mapping
	// updates, and waiting for active shards.
	Timeout time.Duration

	// The number of shard copies that must be active before proceeding with
	// the operation. Set to `all` or any positive integer up to the total
	// number of shards in the index (`number_of_replicas+1`).
	WaitForActiveShards string

	// If `true`, the request blocks until the operation is complete.
	//
	// Default: true.
	WaitForCompletion bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r ReindexParams) get() map[string]string {
	params := make(map[string]string, 9)

	if r.MaxDocs != 0 {
		params["max_docs"] = strconv.Itoa(r.MaxDocs)
	}

	if r.Refresh != "" {
		params["refresh"] = r.Refresh
	}

	if r.RequestsPerSecond != 0 {
		params["requests_per_second"] = strconv.Itoa(r.RequestsPerSecond)
	}

	if r.RequireAlias {
		params["require_alias"] = "true"
	}

	if r.Scroll != 0 {
		params["scroll"] = formatDuration(r.Scroll)
	}

	if r.Slices != "" {
		params["slices"] = r.Slices
	}

	if r.Timeout != 0 {
		params["timeout"] = formatDuration(r.Timeout)
	}

	if r.WaitForActiveShards != "" {
		params["wait_for_active_shards"] = r.WaitForActiveShards
	}

	if r.WaitForCompletion {
		params["wait_for_completion"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// ReindexResp represents the response for the reindex operation.
//
// Allows to copy documents from one index to another, optionally filtering the source
// documents by a query, changing the destination index settings, or fetching the
// documents from a remote cluster.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/reindex-data/
type ReindexResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r ReindexResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// ReindexResponseBodyObject is a typed component of the reindex response.
type ReindexResponseBodyObject struct {
	// The unique identifier of a task.
	Task *string `json:"task,omitempty"`
}

func (c Client) Reindex(ctx context.Context, req *ReindexReq) (*ReindexResp, error) {
	if req == nil {
		req = &ReindexReq{}
	}

	var (
		data ReindexResp
		err  error
	)
	if data.response, err = do(ctx, &c, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
