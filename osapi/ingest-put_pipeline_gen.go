// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"io"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IngestPutPipelineReq represents the request for the ingest.put_pipeline operation.
//
// Creates or updates an ingest pipeline.
//
// PUT /_ingest/pipeline/{id}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/ingest-apis/create-update-ingest/
type IngestPutPipelineReq struct {
	// ID specifies the path segment for the request URL.
	ID string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params IngestPutPipelineParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IngestPutPipelineReq) GetRequest() (*http.Request, error) {
	path, err := ospath.IngestPutPipelinePath{
		ID: r.ID,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPut,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// IngestPutPipelineParams represents query parameters for the IngestPutPipelineReq.
type IngestPutPipelineParams struct {
	// The amount of time allowed to establish a connection to the cluster
	// manager node.
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// Period to wait for a connection to the cluster-manager node. If no
	// response is received before the timeout expires, the request fails and
	// returns an error.
	//
	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	// The amount of time to wait for a response.
	Timeout time.Duration

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r IngestPutPipelineParams) get() map[string]string {
	params := make(map[string]string, 3)

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if r.Timeout != 0 {
		params["timeout"] = formatDuration(r.Timeout)
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// IngestPutPipelineResp represents the response for the ingest.put_pipeline operation.
//
// Creates or updates an ingest pipeline.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/ingest-apis/create-update-ingest/
type IngestPutPipelineResp struct {
	// For a successful response, this value is always true. On failure, an
	// exception is returned instead.
	Acknowledged bool `json:"acknowledged"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IngestPutPipelineResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c ingestClient) PutPipeline(ctx context.Context, req IngestPutPipelineReq) (*IngestPutPipelineResp, error) {
	var (
		data IngestPutPipelineResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
