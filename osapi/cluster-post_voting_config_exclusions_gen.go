// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// ClusterPostVotingConfigExclusionsReq represents the request for the cluster.post_voting_config_exclusions operation.
//
// Updates the cluster voting configuration by excluding certain node IDs or names.
//
// POST /_cluster/voting_config_exclusions
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type ClusterPostVotingConfigExclusionsReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params ClusterPostVotingConfigExclusionsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r ClusterPostVotingConfigExclusionsReq) GetRequest() (*http.Request, error) {
	path, err := ospath.ClusterPostVotingConfigExclusionsPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// ClusterPostVotingConfigExclusionsParams represents query parameters for the ClusterPostVotingConfigExclusionsReq.
type ClusterPostVotingConfigExclusionsParams struct {
	// A comma-separated list of node IDs to exclude from the voting
	// configuration. When using this setting, you cannot also specify
	// `node_names`. Either `node_ids` or `node_names` are required to receive
	// a valid response.
	NodeIds []string

	// A comma-separated list of node names to exclude from the voting
	// configuration. When using this setting, you cannot also specify
	// `node_ids`. Either `node_ids` or `node_names` are required to receive a
	// valid response.
	NodeNames []string

	// When adding a voting configuration exclusion, the API waits for the
	// specified nodes to be excluded from the voting configuration before
	// returning a response. If the timeout expires before the appropriate
	// condition is satisfied, the request fails and returns an error.
	Timeout time.Duration

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r ClusterPostVotingConfigExclusionsParams) get() map[string]string {
	params := make(map[string]string, 3)

	if len(r.NodeIds) > 0 {
		params["node_ids"] = strings.Join(r.NodeIds, ",")
	}

	if len(r.NodeNames) > 0 {
		params["node_names"] = strings.Join(r.NodeNames, ",")
	}

	if r.Timeout != 0 {
		params["timeout"] = formatDuration(r.Timeout)
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// ClusterPostVotingConfigExclusionsResp represents the response for the cluster.post_voting_config_exclusions operation.
//
// Updates the cluster voting configuration by excluding certain node IDs or names.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type ClusterPostVotingConfigExclusionsResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r ClusterPostVotingConfigExclusionsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c clusterClient) PostVotingConfigExclusions(ctx context.Context, req *ClusterPostVotingConfigExclusionsReq) (*ClusterPostVotingConfigExclusionsResp, error) {
	if req == nil {
		req = &ClusterPostVotingConfigExclusionsReq{}
	}

	var (
		data ClusterPostVotingConfigExclusionsResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
