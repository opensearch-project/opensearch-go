// Code generated by cmd/osgen; DO NOT EDIT.

package transforms

import (
	"net/http"
	"strconv"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
	"github.com/opensearch-project/opensearch-go/v4/osapi"
)

// SearchReq represents the request for the transforms.search operation.
//
// Returns the details of all transform jobs.
//
// GET /_plugins/_transform
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/index-transforms/transforms-apis/#get-a-transform-jobs-details
type SearchReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params SearchParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r SearchReq) GetRequest() (*http.Request, error) {
	path, err := ospath.TransformsSearchPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// SearchParams represents query parameters for the SearchReq.
type SearchParams struct {
	// The starting transform to return. Default is `0`.
	From int

	// The search term to use to filter results.
	Search string

	// Specifies the number of transforms to return. Default is `10`.
	Size int

	// Specifies the direction to sort results in. Can be `ASC` or `DESC`.
	// Default is `ASC`.
	SortDirection string

	// The field to sort results with.
	SortField string

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r SearchParams) get() map[string]string {
	params := make(map[string]string, 5)

	if r.From != 0 {
		params["from"] = strconv.Itoa(r.From)
	}

	if r.Search != "" {
		params["search"] = r.Search
	}

	if r.Size != 0 {
		params["size"] = strconv.Itoa(r.Size)
	}

	if r.SortDirection != "" {
		params["sortDirection"] = r.SortDirection
	}

	if r.SortField != "" {
		params["sortField"] = r.SortField
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// SearchResp represents the response for the transforms.search operation.
//
// Returns the details of all transform jobs.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/index-transforms/transforms-apis/#get-a-transform-jobs-details
type SearchResp struct {
	TotalTransforms *float64                            `json:"total_transforms,omitempty"`
	Transforms      []osapi.TransformsTransformResponse `json:"transforms,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r SearchResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
