// Code generated by cmd/osgen; DO NOT EDIT.

package notifications

import (
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// CreateConfigReq represents the request for the notifications.create_config operation.
//
// Create channel configuration.
//
// POST /_plugins/_notifications/configs
//
// Available: >= 2.0.0.
//
// See: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#create-channel-configuration
type CreateConfigReq struct {

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params CreateConfigParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r CreateConfigReq) GetRequest() (*http.Request, error) {
	path, err := ospath.NotificationsCreateConfigPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// CreateConfigParams represents query parameters for the CreateConfigReq.
type CreateConfigParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r CreateConfigParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// CreateConfigResp represents the response for the notifications.create_config operation.
//
// Create channel configuration.
//
// Available: >= 2.0.0.
//
// See: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#create-channel-configuration
type CreateConfigResp struct {
	ConfigID *string `json:"config_id,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r CreateConfigResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
