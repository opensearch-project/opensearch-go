// Code generated by cmd/osgen; DO NOT EDIT.

package replication

import (
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// PauseReq represents the request for the replication.pause operation.
//
// Pauses the replication of the leader index.
//
// POST /_plugins/_replication/{index}/_pause
//
// Available: >= 1.1.0.
//
// See: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#pause-replication
type PauseReq struct {
	// Index specifies the path segment for the request URL.
	Index string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params PauseParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r PauseReq) GetRequest() (*http.Request, error) {
	path, err := ospath.ReplicationPausePath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// PauseParams represents query parameters for the PauseReq.
type PauseParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r PauseParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// PauseResp represents the response for the replication.pause operation.
//
// Pauses the replication of the leader index.
//
// Available: >= 1.1.0.
//
// See: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#pause-replication
type PauseResp struct {
	// For a successful response, this value is always true. On failure, an
	// exception is returned instead.
	Acknowledged bool `json:"acknowledged"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r PauseResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
