// Code generated by cmd/osgen; DO NOT EDIT.

package ppl

import (
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// GetStatsReq represents the request for the ppl.get_stats operation.
//
// Retrieves performance metrics for the PPL plugin.
//
// GET /_plugins/_ppl/stats
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/search-plugins/sql/monitoring/
type GetStatsReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params GetStatsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r GetStatsReq) GetRequest() (*http.Request, error) {
	path, err := ospath.PplGetStatsPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// GetStatsParams represents query parameters for the GetStatsReq.
type GetStatsParams struct {
	// Specifies the response format (JSON, YAML).
	Format string

	// Whether to escape special characters in the results.
	//
	// Default: true.
	Sanitize bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r GetStatsParams) get() map[string]string {
	params := make(map[string]string, 2)

	if r.Format != "" {
		params["format"] = r.Format
	}

	if r.Sanitize {
		params["sanitize"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// GetStatsResp represents the response for the ppl.get_stats operation.
//
// Retrieves performance metrics for the PPL plugin.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/search-plugins/sql/monitoring/
type GetStatsResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r GetStatsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
