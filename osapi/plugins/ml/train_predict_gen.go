// Code generated by cmd/osgen; DO NOT EDIT.

package ml

import (
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
	"github.com/opensearch-project/opensearch-go/v4/osapi"
)

// TrainPredictReq represents the request for the ml.train_predict operation.
//
// Trains a model and predicts against the same training dataset.
//
// POST /_plugins/_ml/_train_predict/{algorithm_name}
//
// Available: >= 1.3.0.
type TrainPredictReq struct {
	// AlgorithmName specifies the path segment for the request URL.
	AlgorithmName string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params TrainPredictParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r TrainPredictReq) GetRequest() (*http.Request, error) {
	path, err := ospath.MlTrainPredictPath{
		AlgorithmName: r.AlgorithmName,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// TrainPredictParams represents query parameters for the TrainPredictReq.
type TrainPredictParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r TrainPredictParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// TrainPredictResp represents the response for the ml.train_predict operation.
//
// Trains a model and predicts against the same training dataset.
//
// Available: >= 1.3.0.
type TrainPredictResp struct {
	PredictionResult *osapi.MlPredictionResult `json:"prediction_result,omitempty"`

	// The status.
	Status string `json:"status"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r TrainPredictResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
