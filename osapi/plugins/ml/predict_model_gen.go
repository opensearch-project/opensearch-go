// Code generated by cmd/osgen; DO NOT EDIT.

package ml

import (
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
	"github.com/opensearch-project/opensearch-go/v4/osapi"
)

// PredictModelReq represents the request for the ml.predict_model operation.
//
// Predicts a model.
//
// POST /_plugins/_ml/models/{model_id}/_predict
//
// Available: >= 2.12.0.
type PredictModelReq struct {
	// ModelID specifies the path segment for the request URL.
	ModelID string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params PredictModelParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r PredictModelReq) GetRequest() (*http.Request, error) {
	path, err := ospath.MlPredictModelPath{
		ModelID: r.ModelID,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// PredictModelParams represents query parameters for the PredictModelReq.
type PredictModelParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r PredictModelParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// PredictModelResp represents the response for the ml.predict_model operation.
//
// Predicts a model.
//
// Available: >= 2.12.0.
type PredictModelResp struct {
	InferenceResults []osapi.MlPredictModelResult `json:"inference_results,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r PredictModelResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
