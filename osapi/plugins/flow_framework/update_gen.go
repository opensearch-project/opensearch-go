// Code generated by cmd/osgen; DO NOT EDIT.

package flow_framework

import (
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// UpdateReq represents the request for the flow_framework.update operation.
//
// Updates a workflow template that has not been provisioned.
//
// PUT /_plugins/_flow_framework/workflow/{workflow_id}
//
// Available: >= 2.12.0.
//
// See: https://opensearch.org/docs/latest/automating-configurations/api/create-workflow/
type UpdateReq struct {
	// WorkflowID specifies the path segment for the request URL.
	WorkflowID string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params UpdateParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r UpdateReq) GetRequest() (*http.Request, error) {
	path, err := ospath.FlowFrameworkUpdatePath{
		WorkflowID: r.WorkflowID,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPut,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// UpdateParams represents query parameters for the UpdateReq.
type UpdateParams struct {
	// Default: false.
	Provision bool

	// Available: >= 2.17.0.
	// Default: false.
	Reprovision bool

	// Default: false.
	UpdateFields bool

	// Specifies the workflow template to use.
	UseCase string

	// Default: all.
	Validation string

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r UpdateParams) get() map[string]string {
	params := make(map[string]string, 5)

	if r.Provision {
		params["provision"] = "true"
	}

	if r.Reprovision {
		params["reprovision"] = "true"
	}

	if r.UpdateFields {
		params["update_fields"] = "true"
	}

	if r.UseCase != "" {
		params["use_case"] = r.UseCase
	}

	if r.Validation != "" {
		params["validation"] = r.Validation
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// UpdateResp represents the response for the flow_framework.update operation.
//
// Updates a workflow template that has not been provisioned.
//
// Available: >= 2.12.0.
//
// See: https://opensearch.org/docs/latest/automating-configurations/api/create-workflow/
type UpdateResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r UpdateResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
