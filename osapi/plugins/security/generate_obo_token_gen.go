// Code generated by cmd/osgen; DO NOT EDIT.

package security

import (
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// GenerateOboTokenReq represents the request for the security.generate_obo_token operation.
//
// Generates a `On-Behalf-Of` token for the current user.
//
// POST /_plugins/_security/api/generateonbehalfoftoken
//
// Available: >= 2.12.0.
//
// Not available on: amazon-managed, amazon-serverless.
//
// See: https://opensearch.org/docs/latest/security/access-control/authentication-tokens/#api-endpoint
type GenerateOboTokenReq struct {

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params GenerateOboTokenParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r GenerateOboTokenReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SecurityGenerateOboTokenPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// GenerateOboTokenParams represents query parameters for the GenerateOboTokenReq.
type GenerateOboTokenParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r GenerateOboTokenParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// GenerateOboTokenResp represents the response for the security.generate_obo_token operation.
//
// Generates a `On-Behalf-Of` token for the current user.
//
// Available: >= 2.12.0.
//
// See: https://opensearch.org/docs/latest/security/access-control/authentication-tokens/#api-endpoint
type GenerateOboTokenResp struct {
	// The generated OBO token.
	AuthenticationToken *string `json:"authenticationToken,omitempty"`

	// The duration of the token.
	DurationSeconds *string `json:"durationSeconds,omitempty"`

	// The name of the entity requesting token.
	User *string `json:"user,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r GenerateOboTokenResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
