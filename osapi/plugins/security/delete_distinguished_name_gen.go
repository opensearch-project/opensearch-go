// Code generated by cmd/osgen; DO NOT EDIT.

package security

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// DeleteDistinguishedNameReq represents the request for the security.delete_distinguished_name operation.
//
// Deletes all distinguished names in the specified cluster or node allowlist. Requires super admin or REST API permissions.
//
// DELETE /_plugins/_security/api/nodesdn/{cluster_name}
//
// Available: >= 1.0.0.
//
// Not available on: amazon-managed, amazon-serverless.
//
// See: https://opensearch.org/docs/latest/security/access-control/api/#delete-distinguished-names
type DeleteDistinguishedNameReq struct {
	// ClusterName specifies the path segment for the request URL.
	ClusterName string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params DeleteDistinguishedNameParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r DeleteDistinguishedNameReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SecurityDeleteDistinguishedNamePath{
		ClusterName: r.ClusterName,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodDelete,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// DeleteDistinguishedNameParams represents query parameters for the DeleteDistinguishedNameReq.
type DeleteDistinguishedNameParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r DeleteDistinguishedNameParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// DeleteDistinguishedNameResp represents the response for the security.delete_distinguished_name operation.
//
// Deletes all distinguished names in the specified cluster or node allowlist. Requires super admin or REST API permissions.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/security/access-control/api/#delete-distinguished-names
type DeleteDistinguishedNameResp struct {
	// The nessage returned as part of an `OK` response.
	Message *string `json:"message,omitempty"`

	Status *json.RawMessage `json:"status,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r DeleteDistinguishedNameResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
