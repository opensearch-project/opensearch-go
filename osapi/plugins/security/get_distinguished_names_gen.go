// Code generated by cmd/osgen; DO NOT EDIT.

package security

import (
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// GetDistinguishedNamesReq represents the request for the security.get_distinguished_names operation.
//
// Retrieves all node distinguished names. Requires super admin or REST API permissions.
//
// GET /_plugins/_security/api/nodesdn
//
// Available: >= 1.0.0.
//
// Not available on: amazon-managed, amazon-serverless.
//
// See: https://opensearch.org/docs/latest/security/access-control/api/#get-distinguished-names
type GetDistinguishedNamesReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params GetDistinguishedNamesParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r GetDistinguishedNamesReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SecurityGetDistinguishedNamesPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// GetDistinguishedNamesParams represents query parameters for the GetDistinguishedNamesReq.
type GetDistinguishedNamesParams struct {
	// Whether to include or exclude any static node's DN settings from the
	// final result.
	ShowAll bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r GetDistinguishedNamesParams) get() map[string]string {
	params := make(map[string]string, 1)

	if r.ShowAll {
		params["show_all"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// GetDistinguishedNamesResp represents the response for the security.get_distinguished_names operation.
//
// Retrieves all node distinguished names. Requires super admin or REST API permissions.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/security/access-control/api/#get-distinguished-names
type GetDistinguishedNamesResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r GetDistinguishedNamesResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
