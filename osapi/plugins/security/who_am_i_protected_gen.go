// Code generated by cmd/osgen; DO NOT EDIT.

package security

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// WhoAmIProtectedReq represents the request for the security.who_am_i_protected operation.
//
// Gets the identity information for the user currently logged in. To use this operation, you must have access to this endpoint when authorization at REST layer is enabled.
//
// GET /_plugins/_security/whoamiprotected
//
// Available: >= 2.11.0.
//
// Not available on: amazon-managed, amazon-serverless.
type WhoAmIProtectedReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params WhoAmIProtectedParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r WhoAmIProtectedReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SecurityWhoAmIProtectedPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// WhoAmIProtectedParams represents query parameters for the WhoAmIProtectedReq.
type WhoAmIProtectedParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r WhoAmIProtectedParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// WhoAmIProtectedResp represents the response for the security.who_am_i_protected operation.
//
// Gets the identity information for the user currently logged in. To use this operation, you must have access to this endpoint when authorization at REST layer is enabled.
//
// Available: >= 2.11.0.
type WhoAmIProtectedResp struct {
	Dn                       *json.RawMessage `json:"dn,omitempty"`
	IsAdmin                  *bool            `json:"is_admin,omitempty"`
	IsNodeCertificateRequest *bool            `json:"is_node_certificate_request,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r WhoAmIProtectedResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
