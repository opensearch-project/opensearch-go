// Code generated by cmd/osgen; DO NOT EDIT.

package security

import (
	"encoding/json"
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// PatchActionGroupReq represents the request for the security.patch_action_group operation.
//
// Updates the individual attributes of an action group.
//
// PATCH /_plugins/_security/api/actiongroups/{action_group}
//
// Available: >= 1.0.0.
//
// Not available on: amazon-managed, amazon-serverless.
//
// See: https://opensearch.org/docs/latest/security/access-control/api/#patch-action-group
type PatchActionGroupReq struct {
	// ActionGroup specifies the path segment for the request URL.
	ActionGroup string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params PatchActionGroupParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r PatchActionGroupReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SecurityPatchActionGroupPath{
		ActionGroup: r.ActionGroup,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPatch,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// PatchActionGroupParams represents query parameters for the PatchActionGroupReq.
type PatchActionGroupParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r PatchActionGroupParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// PatchActionGroupResp represents the response for the security.patch_action_group operation.
//
// Updates the individual attributes of an action group.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/security/access-control/api/#patch-action-group
type PatchActionGroupResp struct {
	// The nessage returned as part of an `OK` response.
	Message *string `json:"message,omitempty"`

	Status *json.RawMessage `json:"status,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r PatchActionGroupResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
