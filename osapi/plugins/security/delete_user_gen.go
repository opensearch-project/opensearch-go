// Code generated by cmd/osgen; DO NOT EDIT.

package security

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// DeleteUserReq represents the request for the security.delete_user operation.
//
// Deletes the specified internal user.
//
// DELETE /_plugins/_security/api/internalusers/{username}
//
// Available: >= 1.0.0.
//
// Not available on: amazon-managed, amazon-serverless.
//
// See: https://opensearch.org/docs/latest/security/access-control/api/#delete-user
type DeleteUserReq struct {
	// Username specifies the path segment for the request URL.
	Username string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params DeleteUserParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r DeleteUserReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SecurityDeleteUserPath{
		Username: r.Username,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodDelete,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// DeleteUserParams represents query parameters for the DeleteUserReq.
type DeleteUserParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r DeleteUserParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// DeleteUserResp represents the response for the security.delete_user operation.
//
// Deletes the specified internal user.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/security/access-control/api/#delete-user
type DeleteUserResp struct {
	// The nessage returned as part of an `OK` response.
	Message *string `json:"message,omitempty"`

	Status *json.RawMessage `json:"status,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r DeleteUserResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
