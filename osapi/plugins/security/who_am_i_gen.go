// Code generated by cmd/osgen; DO NOT EDIT.

package security

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// WhoAmIReq represents the request for the security.who_am_i operation.
//
// Gets the identity information for the user currently logged in.
//
// Path: /_plugins/_security/whoami
//
// Methods: GET, POST
//
// Available: >= 2.0.0.
//
// Not available on: amazon-managed, amazon-serverless.
type WhoAmIReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params WhoAmIParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r WhoAmIReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SecurityWhoAmIPath{}.Build()
	if err != nil {
		return nil, err
	}
	method := http.MethodGet

	return opensearch.BuildRequest(
		method,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// WhoAmIParams represents query parameters for the WhoAmIReq.
type WhoAmIParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r WhoAmIParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// WhoAmIResp represents the response for the security.who_am_i operation.
//
// Gets the identity information for the user currently logged in.
//
// Available: >= 2.0.0.
type WhoAmIResp struct {
	Dn                       *json.RawMessage `json:"dn,omitempty"`
	IsAdmin                  *bool            `json:"is_admin,omitempty"`
	IsNodeCertificateRequest *bool            `json:"is_node_certificate_request,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r WhoAmIResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
