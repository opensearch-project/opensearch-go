// Code generated by cmd/osgen; DO NOT EDIT.

package security

import (
	"encoding/json"
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// PatchTenantsReq represents the request for the security.patch_tenants operation.
//
// Adds, deletes, or modifies multiple tenants in a single request.
//
// PATCH /_plugins/_security/api/tenants
//
// Available: >= 1.0.0.
//
// Not available on: amazon-managed, amazon-serverless.
//
// See: https://opensearch.org/docs/latest/security/access-control/api/#patch-tenants
type PatchTenantsReq struct {

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params PatchTenantsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r PatchTenantsReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SecurityPatchTenantsPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPatch,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// PatchTenantsParams represents query parameters for the PatchTenantsReq.
type PatchTenantsParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r PatchTenantsParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// PatchTenantsResp represents the response for the security.patch_tenants operation.
//
// Adds, deletes, or modifies multiple tenants in a single request.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/security/access-control/api/#patch-tenants
type PatchTenantsResp struct {
	// The nessage returned as part of an `OK` response.
	Message *string `json:"message,omitempty"`

	Status *json.RawMessage `json:"status,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r PatchTenantsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
