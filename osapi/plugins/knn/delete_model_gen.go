// Code generated by cmd/osgen; DO NOT EDIT.

package knn

import (
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// DeleteModelReq represents the request for the knn.delete_model operation.
//
// Used to delete a particular model in the cluster.
//
// DELETE /_plugins/_knn/models/{model_id}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/search-plugins/knn/api/#delete-model
type DeleteModelReq struct {
	// ModelID specifies the path segment for the request URL.
	ModelID string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params DeleteModelParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r DeleteModelReq) GetRequest() (*http.Request, error) {
	path, err := ospath.KnnDeleteModelPath{
		ModelID: r.ModelID,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodDelete,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// DeleteModelParams represents query parameters for the DeleteModelReq.
type DeleteModelParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r DeleteModelParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// DeleteModelResp represents the response for the knn.delete_model operation.
//
// Used to delete a particular model in the cluster.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/search-plugins/knn/api/#delete-model
type DeleteModelResp struct {
	ModelID string `json:"model_id"`
	Result  string `json:"result"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r DeleteModelResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
