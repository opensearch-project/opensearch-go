// Code generated by cmd/osgen; DO NOT EDIT.

package geospatial

import (
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// GeojsonUploadPostReq represents the request for the geospatial.geojson_upload_post operation.
//
// Use an OpenSearch query to upload `GeoJSON`, operation will fail if index exists.
// - When type is `geo_point`, only Point geometry is allowed
// - When type is `geo_shape`, all geometry types are allowed (Point, MultiPoint, LineString, MultiLineString, Polygon, MultiPolygon, GeometryCollection, Envelope).
//
// POST /_plugins/geospatial/geojson/_upload
//
// Available: >= 2.11.0.
type GeojsonUploadPostReq struct {

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params GeojsonUploadPostParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r GeojsonUploadPostReq) GetRequest() (*http.Request, error) {
	path, err := ospath.GeospatialGeojsonUploadPostPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// GeojsonUploadPostParams represents query parameters for the GeojsonUploadPostReq.
type GeojsonUploadPostParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r GeojsonUploadPostParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// GeojsonUploadPostResp represents the response for the geospatial.geojson_upload_post operation.
//
// Use an OpenSearch query to upload `GeoJSON`, operation will fail if index exists.
// - When type is `geo_point`, only Point geometry is allowed
// - When type is `geo_shape`, all geometry types are allowed (Point, MultiPoint, LineString, MultiLineString, Polygon, MultiPolygon, GeometryCollection, Envelope).
//
// Available: >= 2.11.0.
type GeojsonUploadPostResp struct {
	// Whether there were any errors.
	Errors bool `json:"errors"`

	// Number of features that failed to upload.
	Failure int `json:"failure"`

	// Number of features successfully uploaded.
	Success int `json:"success"`

	// The time unit for milliseconds.
	Took int64 `json:"took"`

	// Total number of features processed.
	Total int `json:"total"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r GeojsonUploadPostResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
