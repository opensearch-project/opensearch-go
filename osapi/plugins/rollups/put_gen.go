// Code generated by cmd/osgen; DO NOT EDIT.

package rollups

import (
	"io"
	"net/http"
	"strconv"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// PutReq represents the request for the rollups.put operation.
//
// Creates or updates an index rollup job configuration.
//
// PUT /_plugins/_rollup/jobs/{id}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#create-or-update-an-index-rollup-job
type PutReq struct {
	// ID specifies the path segment for the request URL.
	ID string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params PutParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r PutReq) GetRequest() (*http.Request, error) {
	path, err := ospath.RollupsPutPath{
		ID: r.ID,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPut,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// PutParams represents query parameters for the PutReq.
type PutParams struct {
	// Only performs the operation if the document has the specified primary
	// term.
	IfPrimaryTerm int

	// Only performs the operation if the document has the specified sequence
	// number.
	IfSeqNo int

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r PutParams) get() map[string]string {
	params := make(map[string]string, 2)

	if r.IfPrimaryTerm != 0 {
		params["if_primary_term"] = strconv.Itoa(r.IfPrimaryTerm)
	}

	if r.IfSeqNo != 0 {
		params["if_seq_no"] = strconv.Itoa(r.IfSeqNo)
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// PutResp represents the response for the rollups.put operation.
//
// Creates or updates an index rollup job configuration.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#create-or-update-an-index-rollup-job
type PutResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r PutResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
