// Code generated by cmd/osgen; DO NOT EDIT.

package sql

import (
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// CloseReq represents the request for the sql.close operation.
//
// Closes an open cursor to free server-side resources.
//
// POST /_plugins/_sql/close
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
type CloseReq struct {

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params CloseParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r CloseReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SQLClosePath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// CloseParams represents query parameters for the CloseReq.
type CloseParams struct {
	// Specifies the response format (JSON or YAML).
	Format string

	// Whether to escape special characters in the results.
	//
	// Default: true.
	Sanitize bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r CloseParams) get() map[string]string {
	params := make(map[string]string, 2)

	if r.Format != "" {
		params["format"] = r.Format
	}

	if r.Sanitize {
		params["sanitize"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// CloseResp represents the response for the sql.close operation.
//
// Closes an open cursor to free server-side resources.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
type CloseResp struct {
	// Whether the cursor was closed successfully.
	Succeeded *bool `json:"succeeded,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r CloseResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}
