// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package security_analytics

import (
	"bytes"
	"io"
	"net/http"
	"strconv"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
	"github.com/opensearch-project/opensearch-go/v4/osapi"
)

// GetFindingsReq represents the request for the security_analytics.get_findings operation.
//
// Retrieve findings related to a specific detector type or detector ID.
//
// GET /_plugins/_security_analytics/findings/_search
//
// Available: >= 2.4.0.
//
// See: https://docs.opensearch.org/docs/latest/security-analytics/api-tools/alert-finding-api/#get-findings
type GetFindingsReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *GetFindingsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r GetFindingsReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.SecurityAnalyticsGetFindingsPath{}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// GetFindingsParams represents query parameters for the GetFindingsReq.
type GetFindingsParams struct {
	osapi.TimeoutParams
	osapi.DebugParams
	// The detection type that dictates the retrieval type for the findings.
	// When the detection type is `threat`, it fetches threat intelligence
	// feeds. When the detection type is `rule`, findings are fetched based on
	// the detector’s rule. Optional.
	//
	// Default: rule.
	DetectionType string

	// The type of detector used to fetch alerts. Optional when the
	// `detector_id` is specified. Otherwise required.
	DetectorType string

	// The ID of the detector used to fetch alerts. Optional when the
	// `detectorType` is specified. Otherwise required.
	DetectorID string

	// The end timestamp (in ms) of the time window in which you want to
	// retrieve findings. Optional.
	EndTime string

	// The comma-separated id list of findings for which you want retrieve
	// details. Optional.
	FindingIds string

	// Used to sort by whether the field `missing` exists or not in the
	// documents associated with the finding. Optional.
	Missing string

	// The finding attribute you want returned in the search. To search in a
	// specific index, specify the index name in the request path. For example,
	// to search findings in the indexABC index, use `searchString=indexABC’.
	// Optional.
	SearchString string

	// The rule severity for which retrieve findings. Severity can be
	// `critical`, `high`, `medium`, or `low`. Optional.
	Severity string

	// The maximum number of results returned in the response. Optional.
	//
	// Default: 20.
	Size int

	// The order used to sort the list of findings. Possible values are `asc`
	// or `desc`. Optional.
	//
	// Default: asc.
	SortOrder string

	// The string used by the Alerting plugin to sort the findings. Optional.
	//
	// Default: timestamp.
	SortString string

	// The pagination index. Optional.
	//
	// Default: 0.
	StartIndex int

	// The beginning timestamp (in ms) of the time window in which you want to
	// retrieve findings. Optional.
	StartTime int
}

func (r GetFindingsParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.DetectionType != "" {
		set("detectionType", r.DetectionType)
	}

	if r.DetectorType != "" {
		set("detectorType", r.DetectorType)
	}

	if r.DetectorID != "" {
		set("detector_id", r.DetectorID)
	}

	if r.EndTime != "" {
		set("endTime", r.EndTime)
	}

	if r.FindingIds != "" {
		set("findingIds", r.FindingIds)
	}

	if r.Missing != "" {
		set("missing", r.Missing)
	}

	if r.SearchString != "" {
		set("searchString", r.SearchString)
	}

	if r.Severity != "" {
		set("severity", r.Severity)
	}

	if r.Size != 0 {
		set("size", strconv.Itoa(r.Size))
	}

	if r.SortOrder != "" {
		set("sortOrder", r.SortOrder)
	}

	if r.SortString != "" {
		set("sortString", r.SortString)
	}

	if r.StartIndex != 0 {
		set("startIndex", strconv.Itoa(r.StartIndex))
	}

	if r.StartTime != 0 {
		set("startTime", strconv.Itoa(r.StartTime))
	}

	return params
}

// GetFindingsResp represents the response for the security_analytics.get_findings operation.
//
// Retrieve findings related to a specific detector type or detector ID.
//
// Available: >= 2.4.0.
//
// See: https://docs.opensearch.org/docs/latest/security-analytics/api-tools/alert-finding-api/#get-findings
type GetFindingsResp struct {
	Findings      []SecurityAnalyticsFindingsFinding `json:"findings"`
	TotalFindings int64                              `json:"total_findings"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r GetFindingsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r GetFindingsResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// SecurityAnalyticsFindingsFinding is a typed component of the security_analytics.get_findings operation.
type SecurityAnalyticsFindingsFinding struct {
	DetectorId    *string                             `json:"detectorId,omitempty"`
	DocumentList  []SecurityAnalyticsFindingsDocument `json:"document_list,omitempty"`
	ID            *string                             `json:"id,omitempty"`
	Index         *string                             `json:"index,omitempty"`
	Queries       []SecurityAnalyticsFindingsQuery    `json:"queries,omitempty"`
	RelatedDocIds []string                            `json:"related_doc_ids,omitempty"`
	Timestamp     *int64                              `json:"timestamp,omitempty"`
}

// SecurityAnalyticsFindingsDocument is a typed component of the security_analytics.get_findings operation.
type SecurityAnalyticsFindingsDocument struct {
	Document *string `json:"document,omitempty"`
	Found    *bool   `json:"found,omitempty"`
	ID       *string `json:"id,omitempty"`
	Index    *string `json:"index,omitempty"`
}

// SecurityAnalyticsFindingsQuery is a typed component of the security_analytics.get_findings operation.
type SecurityAnalyticsFindingsQuery struct {
	Fields          []string `json:"fields,omitempty"`
	ID              *string  `json:"id,omitempty"`
	Name            *string  `json:"name,omitempty"`
	Query           *string  `json:"query,omitempty"`
	QueryFieldNames []string `json:"query_field_names,omitempty"`
	Tags            []string `json:"tags,omitempty"`
}
