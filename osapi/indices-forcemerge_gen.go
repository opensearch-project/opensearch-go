// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"bytes"
	"context"
	"io"
	"net/http"
	"strconv"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IndicesForcemergeReq represents the request for the indices.forcemerge operation.
//
// Performs the force merge operation on one or more indexes.
//
// POST /_forcemerge
//
// Available: >= 1.0.0.
//
// Not available on: amazon-managed, amazon-serverless.
//
// See: https://opensearch.org/docs/latest
type IndicesForcemergeReq struct {
	// Index specifies the list of path segments for the request URL.
	Index []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *IndicesForcemergeParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesForcemergeReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.IndicesForcemergePath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// IndicesForcemergeParams represents query parameters for the IndicesForcemergeReq.
type IndicesForcemergeParams struct {
	TimeoutParams
	DebugParams
	// Whether to ignore if a wildcard indexes expression resolves into no
	// concrete indexes. (This includes `_all` string or when no indexes have
	// been specified)
	AllowNoIndices *bool

	// Whether to expand wildcard expression to concrete indexes that are open,
	// closed or both.
	ExpandWildcards []string

	// Specify whether the index should be flushed after performing the
	// operation.
	//
	// Default: true.
	Flush *bool

	// Whether specified concrete indexes should be ignored when unavailable
	// (missing or closed)
	IgnoreUnavailable *bool

	// The number of larger segments into which smaller segments are merged.
	// Set this parameter to 1 to merge all segments into one segment. The
	// default behavior is to perform the merge as necessary.
	MaxNumSegments int

	// Specify whether the operation should only expunge deleted documents
	OnlyExpungeDeletes *bool

	// Specify whether the operation should only perform on primary shards.
	// Defaults to false.
	//
	// Available: >= 2.13.0.
	//
	// Default: false.
	PrimaryOnly *bool

	// Should the request wait until the force merge is completed.
	//
	// Available: >= 2.7.0.
	//
	// Default: true.
	WaitForCompletion *bool
}

func (r IndicesForcemergeParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.AllowNoIndices != nil {
		set("allow_no_indices", strconv.FormatBool(*r.AllowNoIndices))
	}

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	if r.Flush != nil {
		set("flush", strconv.FormatBool(*r.Flush))
	}

	if r.IgnoreUnavailable != nil {
		set("ignore_unavailable", strconv.FormatBool(*r.IgnoreUnavailable))
	}

	if r.MaxNumSegments != 0 {
		set("max_num_segments", strconv.Itoa(r.MaxNumSegments))
	}

	if r.OnlyExpungeDeletes != nil {
		set("only_expunge_deletes", strconv.FormatBool(*r.OnlyExpungeDeletes))
	}

	if r.PrimaryOnly != nil {
		set("primary_only", strconv.FormatBool(*r.PrimaryOnly))
	}

	if r.WaitForCompletion != nil {
		set("wait_for_completion", strconv.FormatBool(*r.WaitForCompletion))
	}

	return params
}

// IndicesForcemergeResp represents the response for the indices.forcemerge operation.
//
// Performs the force merge operation on one or more indexes.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesForcemergeResp struct {
	ShardsOperationResponseBase

	// Task contains a task id returned when `wait_for_completion=false`, you
	// can use the `task_id` to get the status of the task at _tasks/<task_id>.
	Task *string `json:"task,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesForcemergeResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r IndicesForcemergeResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// Forcemerge performs the force merge operation on one or more indexes.
//
// POST /_forcemerge
//
// Available: >= 1.0.0.
//
// Not available on: amazon-managed, amazon-serverless.
//
// See: https://opensearch.org/docs/latest
func (c indicesClient) Forcemerge(ctx context.Context, req *IndicesForcemergeReq) (*IndicesForcemergeResp, error) {
	if req == nil {
		req = &IndicesForcemergeReq{}
	}

	var (
		data IndicesForcemergeResp
		err  error
	)
	if data.response, err = do(
		ctx,
		c.apiClient,
		http.MethodPost,
		req, &data,
	); err != nil {
		return &data, err
	}

	return &data, nil
}
