// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"io"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IndicesRolloverReq represents the request for the indices.rollover operation.
//
// Updates an alias to point to a new index when the existing index
// is considered to be too large or too old.
//
// POST /{alias}/_rollover
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/dashboards/im-dashboards/rollover/
type IndicesRolloverReq struct {
	// Alias specifies the path segment for the request URL.
	Alias string

	// NewIndex specifies the path segment for the request URL.
	NewIndex string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params IndicesRolloverParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesRolloverReq) GetRequest() (*http.Request, error) {
	path, err := ospath.IndicesRolloverPath{
		Alias:    r.Alias,
		NewIndex: r.NewIndex,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// IndicesRolloverParams represents query parameters for the IndicesRolloverReq.
type IndicesRolloverParams struct {
	// Operation timeout for connection to cluster-manager node.
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// If `true`, checks whether the current index satisfies the specified
	// conditions but does not perform a rollover.
	//
	// Default: false.
	DryRun bool

	// Period to wait for a connection to the cluster-manager node. If no
	// response is received before the timeout expires, the request fails and
	// returns an error.
	//
	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	// Period to wait for a response. If no response is received before the
	// timeout expires, the request fails and returns an error.
	Timeout time.Duration

	// The number of shard copies that must be active before proceeding with
	// the operation. Set to all or any positive integer up to the total number
	// of shards in the index (`number_of_replicas+1`).
	WaitForActiveShards string

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r IndicesRolloverParams) get() map[string]string {
	params := make(map[string]string, 5)

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.DryRun {
		params["dry_run"] = "true"
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if r.Timeout != 0 {
		params["timeout"] = formatDuration(r.Timeout)
	}

	if r.WaitForActiveShards != "" {
		params["wait_for_active_shards"] = r.WaitForActiveShards
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// IndicesRolloverResp represents the response for the indices.rollover operation.
//
// Updates an alias to point to a new index when the existing index
// is considered to be too large or too old.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/dashboards/im-dashboards/rollover/
type IndicesRolloverResp struct {
	AcknowledgedResponseBase
	Conditions         map[string]bool `json:"conditions"`
	DryRun             bool            `json:"dry_run"`
	NewIndex           string          `json:"new_index"`
	OldIndex           string          `json:"old_index"`
	RolledOver         bool            `json:"rolled_over"`
	ShardsAcknowledged bool            `json:"shards_acknowledged"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesRolloverResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c indicesClient) Rollover(ctx context.Context, req IndicesRolloverReq) (*IndicesRolloverResp, error) {
	var (
		data IndicesRolloverResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
