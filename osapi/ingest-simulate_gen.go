// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strings"

	"fmt"
	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IngestSimulateReq represents the request for the ingest.simulate operation.
//
// Simulates an ingest pipeline with example documents.
//
// Path: /_ingest/pipeline/_simulate
//
// Methods: GET, POST
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/ingest-apis/simulate-ingest/
type IngestSimulateReq struct {
	// ID specifies the path segment for the request URL.
	ID string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params IngestSimulateParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IngestSimulateReq) GetRequest() (*http.Request, error) {
	path, err := ospath.IngestSimulatePath{
		ID: r.ID,
	}.Build()
	if err != nil {
		return nil, err
	}
	method := http.MethodGet
	if r.Body != nil {
		method = http.MethodPost
	}

	return opensearch.BuildRequest(
		method,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// IngestSimulateParams represents query parameters for the IngestSimulateReq.
type IngestSimulateParams struct {
	// When `true`, the response includes output data for each processor in the
	// pipeline
	//
	// Default: false.
	Verbose bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r IngestSimulateParams) get() map[string]string {
	params := make(map[string]string, 1)

	if r.Verbose {
		params["verbose"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// IngestSimulateResp represents the response for the ingest.simulate operation.
//
// Simulates an ingest pipeline with example documents.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/ingest-apis/simulate-ingest/
type IngestSimulateResp struct {
	Docs []IngestSimulatePipelineSimulation `json:"docs"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IngestSimulateResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// IngestSimulatePipelineSimulation is a typed component of the ingest.simulate response.
type IngestSimulatePipelineSimulation struct {
	Doc              *IngestSimulateDocumentSimulation  `json:"doc,omitempty"`
	ProcessorResults []IngestSimulatePipelineSimulation `json:"processor_results,omitempty"`
	ProcessorType    *string                            `json:"processor_type,omitempty"`
	Status           *string                            `json:"status,omitempty"`
	Tag              *string                            `json:"tag,omitempty"`
}

// IngestSimulateDocumentSimulation is a typed component of the ingest.simulate response.
type IngestSimulateDocumentSimulation struct {
	// The unique identifier for a resource.
	ID string `json:"_id"`

	Index  string               `json:"_index"`
	Ingest IngestSimulateIngest `json:"_ingest"`

	// Value used to send the document to a specific primary shard.
	Routing *string `json:"_routing,omitempty"`

	// JSON body for the document.
	Source map[string]json.RawMessage `json:"_source"`

	// Certain APIs may return values, including numbers such as epoch
	// timestamps, as strings. This setting captures this behavior while
	// keeping the semantics of the field type. Depending on the target
	// language, code generators can keep the union or remove it and leniently
	// parse strings to the target type.
	Version *IngestSimulateDocumentSimulationVersion `json:"_version,omitempty"`

	VersionType *string `json:"_version_type,omitempty"`
}

// IngestSimulateIngest is a typed component of the ingest.simulate response.
type IngestSimulateIngest struct {
	// The name of a resource or configuration element.
	Pipeline *string `json:"pipeline,omitempty"`

	// A date and time, either as a string whose format depends on the context
	// (defaulting to ISO_8601) or the number of milliseconds since the epoch.
	// OpenSearch accepts both as an input but will generally output a string.
	// representation.
	Timestamp string `json:"timestamp"`
}

func (c ingestClient) Simulate(ctx context.Context, req IngestSimulateReq) (*IngestSimulateResp, error) {
	var (
		data IngestSimulateResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}

// Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures
// this behavior while keeping the semantics of the field type.
//
// Depending on the target language, code generators can keep the union or remove it and leniently parse
// strings to the target type.
// Use Type() to determine which branch was decoded, then call
// the corresponding accessor.
type IngestSimulateDocumentSimulationVersion struct {
	typ       IngestSimulateDocumentSimulationVersionType
	raw       json.RawMessage
	int64Val  int64
	stringVal string
}

// IngestSimulateDocumentSimulationVersionType discriminates the branches of IngestSimulateDocumentSimulationVersion.
type IngestSimulateDocumentSimulationVersionType int

const (
	IngestSimulateDocumentSimulationVersionUnknownType IngestSimulateDocumentSimulationVersionType = iota
	IngestSimulateDocumentSimulationVersionInt64Type
	IngestSimulateDocumentSimulationVersionStringType
)

// Type returns which union branch was populated during decoding.
// Returns IngestSimulateDocumentSimulationVersionUnknownType if the value has not been decoded.
func (u *IngestSimulateDocumentSimulationVersion) Type() IngestSimulateDocumentSimulationVersionType {
	return u.typ
}

// RawJSON returns the original JSON bytes for escape-hatch decoding.
func (u *IngestSimulateDocumentSimulationVersion) RawJSON() json.RawMessage { return u.raw }

// Int64 returns the int64 branch value.
func (u *IngestSimulateDocumentSimulationVersion) Int64() int64 { return u.int64Val }

// String returns the string branch value.
func (u *IngestSimulateDocumentSimulationVersion) String() string { return u.stringVal }

func (u *IngestSimulateDocumentSimulationVersion) UnmarshalJSON(data []byte) error {
	u.raw = append(u.raw[:0], data...)
	if len(data) == 0 || string(data) == "null" {
		return nil
	}
	switch {
	case data[0] >= '0' && data[0] <= '9' || data[0] == '-':
		u.typ = IngestSimulateDocumentSimulationVersionInt64Type
		return json.Unmarshal(data, &u.int64Val)
	case data[0] == '"':
		u.typ = IngestSimulateDocumentSimulationVersionStringType
		return json.Unmarshal(data, &u.stringVal)
	default:
		return fmt.Errorf("IngestSimulateDocumentSimulationVersion: unexpected JSON token: %s", data[:1])
	}
	return nil
}

func (u IngestSimulateDocumentSimulationVersion) MarshalJSON() ([]byte, error) {
	switch u.typ {
	case IngestSimulateDocumentSimulationVersionInt64Type:
		return json.Marshal(u.int64Val)
	case IngestSimulateDocumentSimulationVersionStringType:
		return json.Marshal(u.stringVal)
	default:
		if len(u.raw) > 0 {
			return u.raw, nil
		}
		return []byte("null"), nil
	}
}
