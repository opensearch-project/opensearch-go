// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// SearchPipelineGetReq represents the request for the search_pipeline.get operation.
//
// Retrieves information about a specified search pipeline.
//
// GET /_search/pipeline
//
// Available: >= 2.9.0.
type SearchPipelineGetReq struct {
	// ID specifies the path segment for the request URL.
	ID string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params SearchPipelineGetParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r SearchPipelineGetReq) GetRequest() (*http.Request, error) {
	path, err := ospath.SearchPipelineGetPath{
		ID: r.ID,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// SearchPipelineGetParams represents query parameters for the SearchPipelineGetReq.
type SearchPipelineGetParams struct {
	// operation timeout for connection to cluster-manager node.
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r SearchPipelineGetParams) get() map[string]string {
	params := make(map[string]string, 1)

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// SearchPipelineGetResp represents the response for the search_pipeline.get operation.
//
// Retrieves information about a specified search pipeline.
//
// Available: >= 2.9.0.
type SearchPipelineGetResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r SearchPipelineGetResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c searchPipelineClient) Get(ctx context.Context, req SearchPipelineGetReq) (*SearchPipelineGetResp, error) {
	var (
		data SearchPipelineGetResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
