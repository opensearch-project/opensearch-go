// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// CatFielddataReq represents the request for the cat.fielddata operation.
//
// Shows how much heap memory is currently being used by field data on every data node in the cluster.
//
// GET /_cat/fielddata
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-field-data/
type CatFielddataReq struct {
	// Fields specifies the list of path segments for the request URL.
	Fields []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params CatFielddataParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r CatFielddataReq) GetRequest() (*http.Request, error) {
	path, err := ospath.CatFielddataPath{
		Fields: r.Fields,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// CatFielddataParams represents query parameters for the CatFielddataReq.
type CatFielddataParams struct {
	// The units used to display byte values.
	Bytes string

	// A comma-separated list of fields used to limit the amount of returned
	// information.
	Fields []string

	// A short version of the `Accept` header, such as `json` or `yaml`.
	Format string

	// A comma-separated list of column names to display.
	H []string

	// Returns help information.
	//
	// Default: false.
	Help bool

	// A comma-separated list of column names or column aliases to sort by.
	S []string

	// Enables verbose mode, which displays column headers.
	//
	// Default: false.
	V bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r CatFielddataParams) get() map[string]string {
	params := make(map[string]string, 7)

	if r.Bytes != "" {
		params["bytes"] = r.Bytes
	}

	if len(r.Fields) > 0 {
		params["fields"] = strings.Join(r.Fields, ",")
	}

	if r.Format != "" {
		params["format"] = r.Format
	}

	if len(r.H) > 0 {
		params["h"] = strings.Join(r.H, ",")
	}

	if r.Help {
		params["help"] = "true"
	}

	if len(r.S) > 0 {
		params["s"] = strings.Join(r.S, ",")
	}

	if r.V {
		params["v"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// CatFielddataResp represents the response for the cat.fielddata operation.
//
// Shows how much heap memory is currently being used by field data on every data node in the cluster.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-field-data/
type CatFielddataResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r CatFielddataResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// CatFielddataRecord is a typed component of the cat.fielddata response.
type CatFielddataRecord struct {
	// field name
	Field *string `json:"field,omitempty"`

	// hostname
	Host *string `json:"host,omitempty"`

	// node id
	ID *string `json:"id,omitempty"`

	// IP address
	IP *string `json:"ip,omitempty"`

	// node name
	Node *string `json:"node,omitempty"`

	// field data usage
	Size *string `json:"size,omitempty"`
}

func (c catClient) Fielddata(ctx context.Context, req *CatFielddataReq) (*CatFielddataResp, error) {
	if req == nil {
		req = &CatFielddataReq{}
	}

	var (
		data CatFielddataResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
