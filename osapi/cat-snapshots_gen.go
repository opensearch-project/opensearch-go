// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"encoding/json"
	"net/http"
	"strings"
	"time"

	"fmt"
	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// CatSnapshotsReq represents the request for the cat.snapshots operation.
//
// Lists all of the snapshots stored in a specific repository.
//
// GET /_cat/snapshots
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-snapshots/
type CatSnapshotsReq struct {
	// Repository specifies the list of path segments for the request URL.
	Repository []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params CatSnapshotsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r CatSnapshotsReq) GetRequest() (*http.Request, error) {
	path, err := ospath.CatSnapshotsPath{
		Repository: r.Repository,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// CatSnapshotsParams represents query parameters for the CatSnapshotsReq.
type CatSnapshotsParams struct {
	// The amount of time allowed to establish a connection to the cluster
	// manager node.
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// A short version of the `Accept` header, such as `json` or `yaml`.
	Format string

	// A comma-separated list of column names to display.
	H []string

	// Returns help information.
	//
	// Default: false.
	Help bool

	// When `true`, the response does not include information from unavailable
	// snapshots.
	//
	// Default: false.
	IgnoreUnavailable bool

	// The amount of time allowed to establish a connection to the cluster
	// manager node.
	//
	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	// A comma-separated list of snapshot repositories used to limit the
	// request. Accepts wildcard expressions. `_all` returns all repositories.
	// If any repository fails during the request, OpenSearch returns an error.
	Repository []string

	// A comma-separated list of column names or column aliases to sort by.
	S []string

	// Specifies the time units, for example, `5d` or `7h`. For more
	// information, see [Supported
	// units](https://opensearch.org/docs/latest/api-reference/units/).
	Time string

	// Enables verbose mode, which displays column headers.
	//
	// Default: false.
	V bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r CatSnapshotsParams) get() map[string]string {
	params := make(map[string]string, 10)

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.Format != "" {
		params["format"] = r.Format
	}

	if len(r.H) > 0 {
		params["h"] = strings.Join(r.H, ",")
	}

	if r.Help {
		params["help"] = "true"
	}

	if r.IgnoreUnavailable {
		params["ignore_unavailable"] = "true"
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if len(r.Repository) > 0 {
		params["repository"] = strings.Join(r.Repository, ",")
	}

	if len(r.S) > 0 {
		params["s"] = strings.Join(r.S, ",")
	}

	if r.Time != "" {
		params["time"] = r.Time
	}

	if r.V {
		params["v"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// CatSnapshotsResp represents the response for the cat.snapshots operation.
//
// Lists all of the snapshots stored in a specific repository.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-snapshots/
type CatSnapshotsResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r CatSnapshotsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// CatSnapshotsRecord is a typed component of the cat.snapshots response.
type CatSnapshotsRecord struct {
	// A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s`
	// (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0`
	// without a unit and `-1` to indicate an unspecified value.
	Duration *string `json:"duration,omitempty"`

	// Certain APIs may return values, including numbers such as epoch
	// timestamps, as strings. This setting captures this behavior while
	// keeping the semantics of the field type. Depending on the target
	// language, code generators can keep the union or remove it and leniently
	// parse strings to the target type.
	EndEpoch *CatSnapshotsRecordEndEpoch `json:"end_epoch,omitempty"`

	// Time of day, expressed as HH:MM:SS.
	EndTime *string `json:"end_time,omitempty"`

	// The number of failed shards in the snapshot.
	FailedShards *string `json:"failed_shards,omitempty"`

	// The unique identifier for the snapshot.
	ID *string `json:"id,omitempty"`

	// The number of indexes in the snapshot.
	Indices *string `json:"indices,omitempty"`

	// The reason for any snapshot failures.
	Reason *string `json:"reason,omitempty"`

	// The repository name.
	Repository *string `json:"repository,omitempty"`

	// Certain APIs may return values, including numbers such as epoch
	// timestamps, as strings. This setting captures this behavior while
	// keeping the semantics of the field type. Depending on the target
	// language, code generators can keep the union or remove it and leniently
	// parse strings to the target type.
	StartEpoch *CatSnapshotsRecordStartEpoch `json:"start_epoch,omitempty"`

	// Time of day, expressed as HH:MM:SS.
	StartTime *string `json:"start_time,omitempty"`

	// The state of the snapshot process. Returned values include: `FAILED`:
	// The snapshot process failed. `INCOMPATIBLE`: The snapshot process is
	// incompatible with the current cluster version. `IN_PROGRESS`: The
	// snapshot process started but has not completed. `PARTIAL`: The snapshot
	// process completed with a partial success. `SUCCESS`: The snapshot
	// process completed with a full success.
	Status *string `json:"status,omitempty"`

	// The number of successful shards in the snapshot.
	SuccessfulShards *string `json:"successful_shards,omitempty"`

	// The total number of shards in the snapshot.
	TotalShards *string `json:"total_shards,omitempty"`
}

func (c catClient) Snapshots(ctx context.Context, req *CatSnapshotsReq) (*CatSnapshotsResp, error) {
	if req == nil {
		req = &CatSnapshotsReq{}
	}

	var (
		data CatSnapshotsResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}

// Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures
// this behavior while keeping the semantics of the field type.
//
// Depending on the target language, code generators can keep the union or remove it and leniently parse
// strings to the target type.
// Use Type() to determine which branch was decoded, then call
// the corresponding accessor.
type CatSnapshotsRecordEndEpoch struct {
	typ       CatSnapshotsRecordEndEpochType
	raw       json.RawMessage
	int64Val  int64
	stringVal string
}

// CatSnapshotsRecordEndEpochType discriminates the branches of CatSnapshotsRecordEndEpoch.
type CatSnapshotsRecordEndEpochType int

const (
	CatSnapshotsRecordEndEpochUnknownType CatSnapshotsRecordEndEpochType = iota
	CatSnapshotsRecordEndEpochInt64Type
	CatSnapshotsRecordEndEpochStringType
)

// Type returns which union branch was populated during decoding.
// Returns CatSnapshotsRecordEndEpochUnknownType if the value has not been decoded.
func (u *CatSnapshotsRecordEndEpoch) Type() CatSnapshotsRecordEndEpochType { return u.typ }

// RawJSON returns the original JSON bytes for escape-hatch decoding.
func (u *CatSnapshotsRecordEndEpoch) RawJSON() json.RawMessage { return u.raw }

// Int64 returns the int64 branch value.
func (u *CatSnapshotsRecordEndEpoch) Int64() int64 { return u.int64Val }

// String returns the string branch value.
func (u *CatSnapshotsRecordEndEpoch) String() string { return u.stringVal }

func (u *CatSnapshotsRecordEndEpoch) UnmarshalJSON(data []byte) error {
	u.raw = append(u.raw[:0], data...)
	if len(data) == 0 || string(data) == "null" {
		return nil
	}
	switch {
	case data[0] >= '0' && data[0] <= '9' || data[0] == '-':
		u.typ = CatSnapshotsRecordEndEpochInt64Type
		return json.Unmarshal(data, &u.int64Val)
	case data[0] == '"':
		u.typ = CatSnapshotsRecordEndEpochStringType
		return json.Unmarshal(data, &u.stringVal)
	default:
		return fmt.Errorf("CatSnapshotsRecordEndEpoch: unexpected JSON token: %s", data[:1])
	}
	return nil
}

func (u CatSnapshotsRecordEndEpoch) MarshalJSON() ([]byte, error) {
	switch u.typ {
	case CatSnapshotsRecordEndEpochInt64Type:
		return json.Marshal(u.int64Val)
	case CatSnapshotsRecordEndEpochStringType:
		return json.Marshal(u.stringVal)
	default:
		if len(u.raw) > 0 {
			return u.raw, nil
		}
		return []byte("null"), nil
	}
}

// Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures
// this behavior while keeping the semantics of the field type.
//
// Depending on the target language, code generators can keep the union or remove it and leniently parse
// strings to the target type.
// Use Type() to determine which branch was decoded, then call
// the corresponding accessor.
type CatSnapshotsRecordStartEpoch struct {
	typ       CatSnapshotsRecordStartEpochType
	raw       json.RawMessage
	int64Val  int64
	stringVal string
}

// CatSnapshotsRecordStartEpochType discriminates the branches of CatSnapshotsRecordStartEpoch.
type CatSnapshotsRecordStartEpochType int

const (
	CatSnapshotsRecordStartEpochUnknownType CatSnapshotsRecordStartEpochType = iota
	CatSnapshotsRecordStartEpochInt64Type
	CatSnapshotsRecordStartEpochStringType
)

// Type returns which union branch was populated during decoding.
// Returns CatSnapshotsRecordStartEpochUnknownType if the value has not been decoded.
func (u *CatSnapshotsRecordStartEpoch) Type() CatSnapshotsRecordStartEpochType { return u.typ }

// RawJSON returns the original JSON bytes for escape-hatch decoding.
func (u *CatSnapshotsRecordStartEpoch) RawJSON() json.RawMessage { return u.raw }

// Int64 returns the int64 branch value.
func (u *CatSnapshotsRecordStartEpoch) Int64() int64 { return u.int64Val }

// String returns the string branch value.
func (u *CatSnapshotsRecordStartEpoch) String() string { return u.stringVal }

func (u *CatSnapshotsRecordStartEpoch) UnmarshalJSON(data []byte) error {
	u.raw = append(u.raw[:0], data...)
	if len(data) == 0 || string(data) == "null" {
		return nil
	}
	switch {
	case data[0] >= '0' && data[0] <= '9' || data[0] == '-':
		u.typ = CatSnapshotsRecordStartEpochInt64Type
		return json.Unmarshal(data, &u.int64Val)
	case data[0] == '"':
		u.typ = CatSnapshotsRecordStartEpochStringType
		return json.Unmarshal(data, &u.stringVal)
	default:
		return fmt.Errorf("CatSnapshotsRecordStartEpoch: unexpected JSON token: %s", data[:1])
	}
	return nil
}

func (u CatSnapshotsRecordStartEpoch) MarshalJSON() ([]byte, error) {
	switch u.typ {
	case CatSnapshotsRecordStartEpochInt64Type:
		return json.Marshal(u.int64Val)
	case CatSnapshotsRecordStartEpochStringType:
		return json.Marshal(u.stringVal)
	default:
		if len(u.raw) > 0 {
			return u.raw, nil
		}
		return []byte("null"), nil
	}
}
