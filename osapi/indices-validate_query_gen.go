// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IndicesValidateQueryReq represents the request for the indices.validate_query operation.
//
// Allows a user to validate a potentially expensive query without executing it.
//
// Path: /_validate/query
//
// Methods: GET, POST
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesValidateQueryReq struct {
	// Index specifies the list of path segments for the request URL.
	Index []string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params IndicesValidateQueryParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesValidateQueryReq) GetRequest() (*http.Request, error) {
	path, err := ospath.IndicesValidateQueryPath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}
	method := http.MethodGet
	if r.Body != nil {
		method = http.MethodPost
	}

	return opensearch.BuildRequest(
		method,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// IndicesValidateQueryParams represents query parameters for the IndicesValidateQueryReq.
type IndicesValidateQueryParams struct {
	// If `true`, the validation is executed on all shards instead of one
	// random shard per index.
	AllShards bool

	// If `false`, the request returns an error if any wildcard expression,
	// index alias, or `_all` value targets only missing or closed indexes.
	// This behavior applies even if the request targets other open indexes.
	AllowNoIndices bool

	// If `true`, wildcard and prefix queries are analyzed.
	//
	// Default: false.
	AnalyzeWildcard bool

	// Analyzer to use for the query string. This parameter can only be used
	// when the `q` query string parameter is specified.
	Analyzer string

	// The default operator for query string query: `AND` or `OR`.
	DefaultOperator string

	// Field to use as default where no field prefix is given in the query
	// string. This parameter can only be used when the `q` query string
	// parameter is specified.
	Df string

	// Type of index that wildcard patterns can match. If the request can
	// target data streams, this argument determines whether wildcard
	// expressions match hidden data streams. Supports comma-separated values,
	// such as `open,hidden`. Valid values are: `all`, `open`, `closed`,
	// `hidden`, `none`.
	ExpandWildcards []string

	// If `true`, the response returns detailed information if an error has
	// occurred.
	Explain bool

	// If `false`, the request returns an error if it targets a missing or
	// closed index.
	IgnoreUnavailable bool

	// If `true`, format-based query failures (such as providing text to a
	// numeric field) in the query string will be ignored.
	Lenient bool

	// Query in the Lucene query string syntax.
	Q string

	// If `true`, returns a more detailed explanation showing the actual Lucene
	// query that will be executed.
	Rewrite bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r IndicesValidateQueryParams) get() map[string]string {
	params := make(map[string]string, 12)

	if r.AllShards {
		params["all_shards"] = "true"
	}

	if r.AllowNoIndices {
		params["allow_no_indices"] = "true"
	}

	if r.AnalyzeWildcard {
		params["analyze_wildcard"] = "true"
	}

	if r.Analyzer != "" {
		params["analyzer"] = r.Analyzer
	}

	if r.DefaultOperator != "" {
		params["default_operator"] = r.DefaultOperator
	}

	if r.Df != "" {
		params["df"] = r.Df
	}

	if len(r.ExpandWildcards) > 0 {
		params["expand_wildcards"] = strings.Join(r.ExpandWildcards, ",")
	}

	if r.Explain {
		params["explain"] = "true"
	}

	if r.IgnoreUnavailable {
		params["ignore_unavailable"] = "true"
	}

	if r.Lenient {
		params["lenient"] = "true"
	}

	if r.Q != "" {
		params["q"] = r.Q
	}

	if r.Rewrite {
		params["rewrite"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// IndicesValidateQueryResp represents the response for the indices.validate_query operation.
//
// Allows a user to validate a potentially expensive query without executing it.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesValidateQueryResp struct {
	Shards       *ShardStatistics                                   `json:"_shards,omitempty"`
	Error        *string                                            `json:"error,omitempty"`
	Explanations []IndicesValidateQueryIndicesValidationExplanation `json:"explanations,omitempty"`
	Valid        bool                                               `json:"valid"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesValidateQueryResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// IndicesValidateQueryIndicesValidationExplanation is a typed component of the indices.validate_query response.
//
// The validation results for an index query.
type IndicesValidateQueryIndicesValidationExplanation struct {
	// The error message if validation failed.
	Error *string `json:"error,omitempty"`

	// The explanation of why the query is valid or invalid.
	Explanation *string `json:"explanation,omitempty"`

	Index string `json:"index"`

	// Whether the query is valid for the index.
	Valid bool `json:"valid"`
}

func (c indicesClient) ValidateQuery(ctx context.Context, req *IndicesValidateQueryReq) (*IndicesValidateQueryResp, error) {
	if req == nil {
		req = &IndicesValidateQueryReq{}
	}

	var (
		data IndicesValidateQueryResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
