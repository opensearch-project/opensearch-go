// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"encoding/json"
	"net/http"
	"strings"

	"fmt"
	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// CatCountReq represents the request for the cat.count operation.
//
// Provides quick access to the document count of the entire cluster or of an individual index.
//
// GET /_cat/count
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-count/
type CatCountReq struct {
	// Index specifies the list of path segments for the request URL.
	Index []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params CatCountParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r CatCountReq) GetRequest() (*http.Request, error) {
	path, err := ospath.CatCountPath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// CatCountParams represents query parameters for the CatCountReq.
type CatCountParams struct {
	// A short version of the `Accept` header, such as `json` or `yaml`.
	Format string

	// A comma-separated list of column names to display.
	H []string

	// Returns help information.
	//
	// Default: false.
	Help bool

	// A comma-separated list of column names or column aliases to sort by.
	S []string

	// Enables verbose mode, which displays column headers.
	//
	// Default: false.
	V bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r CatCountParams) get() map[string]string {
	params := make(map[string]string, 5)

	if r.Format != "" {
		params["format"] = r.Format
	}

	if len(r.H) > 0 {
		params["h"] = strings.Join(r.H, ",")
	}

	if r.Help {
		params["help"] = "true"
	}

	if len(r.S) > 0 {
		params["s"] = strings.Join(r.S, ",")
	}

	if r.V {
		params["v"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// CatCountResp represents the response for the cat.count operation.
//
// Provides quick access to the document count of the entire cluster or of an individual index.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-count/
type CatCountResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r CatCountResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// CatCountRecord is a typed component of the cat.count response.
type CatCountRecord struct {
	// the document count
	Count *string `json:"count,omitempty"`

	// Certain APIs may return values, including numbers such as epoch
	// timestamps, as strings. This setting captures this behavior while
	// keeping the semantics of the field type. Depending on the target
	// language, code generators can keep the union or remove it and leniently
	// parse strings to the target type.
	Epoch *CatCountRecordEpoch `json:"epoch,omitempty"`

	// Time of day, expressed as HH:MM:SS.
	Timestamp *string `json:"timestamp,omitempty"`
}

func (c catClient) Count(ctx context.Context, req *CatCountReq) (*CatCountResp, error) {
	if req == nil {
		req = &CatCountReq{}
	}

	var (
		data CatCountResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}

// Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures
// this behavior while keeping the semantics of the field type.
//
// Depending on the target language, code generators can keep the union or remove it and leniently parse
// strings to the target type.
// Use Type() to determine which branch was decoded, then call
// the corresponding accessor.
type CatCountRecordEpoch struct {
	typ       CatCountRecordEpochType
	raw       json.RawMessage
	int64Val  int64
	stringVal string
}

// CatCountRecordEpochType discriminates the branches of CatCountRecordEpoch.
type CatCountRecordEpochType int

const (
	CatCountRecordEpochUnknownType CatCountRecordEpochType = iota
	CatCountRecordEpochInt64Type
	CatCountRecordEpochStringType
)

// Type returns which union branch was populated during decoding.
// Returns CatCountRecordEpochUnknownType if the value has not been decoded.
func (u *CatCountRecordEpoch) Type() CatCountRecordEpochType { return u.typ }

// RawJSON returns the original JSON bytes for escape-hatch decoding.
func (u *CatCountRecordEpoch) RawJSON() json.RawMessage { return u.raw }

// Int64 returns the int64 branch value.
func (u *CatCountRecordEpoch) Int64() int64 { return u.int64Val }

// String returns the string branch value.
func (u *CatCountRecordEpoch) String() string { return u.stringVal }

func (u *CatCountRecordEpoch) UnmarshalJSON(data []byte) error {
	u.raw = append(u.raw[:0], data...)
	if len(data) == 0 || string(data) == "null" {
		return nil
	}
	switch {
	case data[0] >= '0' && data[0] <= '9' || data[0] == '-':
		u.typ = CatCountRecordEpochInt64Type
		return json.Unmarshal(data, &u.int64Val)
	case data[0] == '"':
		u.typ = CatCountRecordEpochStringType
		return json.Unmarshal(data, &u.stringVal)
	default:
		return fmt.Errorf("CatCountRecordEpoch: unexpected JSON token: %s", data[:1])
	}
	return nil
}

func (u CatCountRecordEpoch) MarshalJSON() ([]byte, error) {
	switch u.typ {
	case CatCountRecordEpochInt64Type:
		return json.Marshal(u.int64Val)
	case CatCountRecordEpochStringType:
		return json.Marshal(u.stringVal)
	default:
		if len(u.raw) > 0 {
			return u.raw, nil
		}
		return []byte("null"), nil
	}
}
