// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// CatIndicesReq represents the request for the cat.indices operation.
//
// Lists information related to indexes, that is, how much disk space they are using, how many shards they have, their health status, and so on.
//
// GET /_cat/indices
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-indices/
type CatIndicesReq struct {
	// Index specifies the list of path segments for the request URL.
	Index []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params CatIndicesParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r CatIndicesReq) GetRequest() (*http.Request, error) {
	path, err := ospath.CatIndicesPath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// CatIndicesParams represents query parameters for the CatIndicesReq.
type CatIndicesParams struct {
	// The units used to display byte values.
	Bytes string

	// The amount of time allowed to establish a connection to the cluster
	// manager node.
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	ExpandWildcards []string

	// A short version of the `Accept` header, such as `json` or `yaml`.
	Format string

	// A comma-separated list of column names to display.
	H []string

	// Limits indexes based on their health status. Supported values are
	// `green`, `yellow`, and `red`.
	Health string

	// Returns help information.
	//
	// Default: false.
	Help bool

	// Whether to include information from segments not loaded into memory.
	//
	// Default: false.
	IncludeUnloadedSegments bool

	// Returns local information but does not retrieve the state from the
	// cluster manager node.
	//
	// Default: false.
	Local bool

	// The amount of time allowed to establish a connection to the cluster
	// manager node.
	//
	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	// When `true`, returns information only from the primary shards.
	//
	// Default: false.
	Pri bool

	// A comma-separated list of column names or column aliases to sort by.
	S []string

	// Specifies the time units.
	Time string

	// Enables verbose mode, which displays column headers.
	//
	// Default: false.
	V bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r CatIndicesParams) get() map[string]string {
	params := make(map[string]string, 14)

	if r.Bytes != "" {
		params["bytes"] = r.Bytes
	}

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if len(r.ExpandWildcards) > 0 {
		params["expand_wildcards"] = strings.Join(r.ExpandWildcards, ",")
	}

	if r.Format != "" {
		params["format"] = r.Format
	}

	if len(r.H) > 0 {
		params["h"] = strings.Join(r.H, ",")
	}

	if r.Health != "" {
		params["health"] = r.Health
	}

	if r.Help {
		params["help"] = "true"
	}

	if r.IncludeUnloadedSegments {
		params["include_unloaded_segments"] = "true"
	}

	if r.Local {
		params["local"] = "true"
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if r.Pri {
		params["pri"] = "true"
	}

	if len(r.S) > 0 {
		params["s"] = strings.Join(r.S, ",")
	}

	if r.Time != "" {
		params["time"] = r.Time
	}

	if r.V {
		params["v"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// CatIndicesResp represents the response for the cat.indices operation.
//
// Lists information related to indexes, that is, how much disk space they are using, how many shards they have, their health status, and so on.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-indices/
type CatIndicesResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r CatIndicesResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c catClient) Indices(ctx context.Context, req *CatIndicesReq) (*CatIndicesResp, error) {
	if req == nil {
		req = &CatIndicesReq{}
	}

	var (
		data CatIndicesResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
