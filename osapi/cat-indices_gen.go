// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"bytes"
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strconv"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// CatIndicesReq represents the request for the cat.indices operation.
//
// Lists information related to indexes, that is, how much disk space they are using, how many shards they have, their health status, and so on.
//
// GET /_cat/indices
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-indices/
type CatIndicesReq struct {
	// Index specifies the list of path segments for the request URL.
	Index []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *CatIndicesParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r CatIndicesReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.CatIndicesPath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// CatIndicesParams represents query parameters for the CatIndicesReq.
type CatIndicesParams struct {
	TimeoutParams
	DebugParams
	// The units used to display byte values.
	Bytes string

	ExpandWildcards []string

	// Limits indexes based on their health status. Supported values are
	// `green`, `yellow`, and `red`.
	Health string

	// Whether to include information from segments not loaded into memory.
	//
	// Default: false.
	IncludeUnloadedSegments *bool

	// Returns local information but does not retrieve the state from the
	// cluster manager node.
	//
	// Default: false.
	Local *bool

	// When `true`, returns information only from the primary shards.
	//
	// Default: false.
	Pri *bool

	// Specifies the time units.
	Time string
}

func (r CatIndicesParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)
	if r.Format == "" {
		set("format", "json")
	}

	if r.Bytes != "" {
		set("bytes", r.Bytes)
	}

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	if r.Health != "" {
		set("health", r.Health)
	}

	if r.IncludeUnloadedSegments != nil {
		set("include_unloaded_segments", strconv.FormatBool(*r.IncludeUnloadedSegments))
	}

	if r.Local != nil {
		set("local", strconv.FormatBool(*r.Local))
	}

	if r.Pri != nil {
		set("pri", strconv.FormatBool(*r.Pri))
	}

	if r.Time != "" {
		set("time", r.Time)
	}

	return params
}

// CatIndicesResp represents the response for the CatIndices operation.
// The response body is a JSON array of records.
//
// Lists information related to indexes, that is, how much disk space they are using, how many shards they have, their health status, and so on.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-indices/
type CatIndicesResp struct {
	Records  []CatIndicesRecord `json:"-"`
	response *opensearch.Response
}

// UnmarshalJSON decodes the response body as an array of records.
func (r *CatIndicesResp) UnmarshalJSON(b []byte) error {
	return json.Unmarshal(b, &r.Records)
}

// MarshalJSON encodes the response body for comparison testing.
func (r CatIndicesResp) MarshalJSON() ([]byte, error) {
	return json.Marshal(r.Records)
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r CatIndicesResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r CatIndicesResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// Indices lists information related to indexes, that is, how much disk space they are using, how many shards they have, their health status, and so on.
//
// GET /_cat/indices
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-indices/
func (c catClient) Indices(ctx context.Context, req *CatIndicesReq) (*CatIndicesResp, error) {
	if req == nil {
		req = &CatIndicesReq{}
	}

	var (
		data CatIndicesResp
		err  error
	)
	if data.response, err = do(
		ctx,
		c.apiClient,
		http.MethodGet,
		req, &data,
	); err != nil {
		return &data, err
	}

	return &data, nil
}
