// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"io"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IndicesSimulateTemplateReq represents the request for the indices.simulate_template operation.
//
// Simulate resolving the given template name or body.
//
// POST /_index_template/_simulate
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesSimulateTemplateReq struct {
	// Name specifies the path segment for the request URL.
	Name string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params IndicesSimulateTemplateParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesSimulateTemplateReq) GetRequest() (*http.Request, error) {
	path, err := ospath.IndicesSimulateTemplatePath{
		Name: r.Name,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// IndicesSimulateTemplateParams represents query parameters for the IndicesSimulateTemplateReq.
type IndicesSimulateTemplateParams struct {
	// User defined reason for dry-run creating the new template for simulation
	// purposes.
	//
	// Default: false.
	Cause string

	// Operation timeout for connection to cluster-manager node.
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// If `true`, the template passed in the body is only used if no existing
	// templates match the same index patterns. If `false`, the simulation uses
	// the template with the highest priority. Note that the template is not
	// permanently added or updated in either case; it is only used for the
	// simulation.
	//
	// Default: false.
	Create bool

	// Period to wait for a connection to the cluster-manager node. If no
	// response is received before the timeout expires, the request fails and
	// returns an error.
	//
	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r IndicesSimulateTemplateParams) get() map[string]string {
	params := make(map[string]string, 4)

	if r.Cause != "" {
		params["cause"] = r.Cause
	}

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.Create {
		params["create"] = "true"
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// IndicesSimulateTemplateResp represents the response for the indices.simulate_template operation.
//
// Simulate resolving the given template name or body.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesSimulateTemplateResp struct {
	Overlapping []IndicesSimulateTemplateOverlapping `json:"overlapping,omitempty"`

	// The configuration for an index template simulation.
	Template IndicesSimulateTemplateTemplate `json:"template"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesSimulateTemplateResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c indicesClient) SimulateTemplate(ctx context.Context, req IndicesSimulateTemplateReq) (*IndicesSimulateTemplateResp, error) {
	var (
		data IndicesSimulateTemplateResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
