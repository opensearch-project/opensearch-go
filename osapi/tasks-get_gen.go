// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// TasksGetReq represents the request for the tasks.get operation.
//
// Returns information about a task.
//
// GET /_tasks/{task_id}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/tasks/
type TasksGetReq struct {
	// TaskID specifies the path segment for the request URL.
	TaskID string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params TasksGetParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r TasksGetReq) GetRequest() (*http.Request, error) {
	path, err := ospath.TasksGetPath{
		TaskID: r.TaskID,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// TasksGetParams represents query parameters for the TasksGetReq.
type TasksGetParams struct {
	// The amount of time to wait for a response.
	Timeout time.Duration

	// Waits for the matching task to complete. When `true`, the request is
	// blocked until the task has completed.
	//
	// Default: false.
	WaitForCompletion bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r TasksGetParams) get() map[string]string {
	params := make(map[string]string, 2)

	if r.Timeout != 0 {
		params["timeout"] = formatDuration(r.Timeout)
	}

	if r.WaitForCompletion {
		params["wait_for_completion"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// TasksGetResp represents the response for the tasks.get operation.
//
// Returns information about a task.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/tasks/
type TasksGetResp struct {
	Completed bool                      `json:"completed"`
	Error     *ErrorCause               `json:"error,omitempty"`
	Response  *BulkByScrollResponseBase `json:"response,omitempty"`
	Task      TasksTaskInfo             `json:"task"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r TasksGetResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c tasksClient) Get(ctx context.Context, req TasksGetReq) (*TasksGetResp, error) {
	var (
		data TasksGetResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
