// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// CatPluginsReq represents the request for the cat.plugins operation.
//
// Returns information about the names, components, and versions of the installed plugins.
//
// GET /_cat/plugins
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-plugins/
type CatPluginsReq struct {

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params CatPluginsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r CatPluginsReq) GetRequest() (*http.Request, error) {
	path, err := ospath.CatPluginsPath{}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodGet,
		path,
		nil,
		r.Params.get(),
		r.Header,
	)
}

// CatPluginsParams represents query parameters for the CatPluginsReq.
type CatPluginsParams struct {
	// The amount of time allowed to establish a connection to the cluster
	// manager node.
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// A short version of the `Accept` header, such as `json` or `yaml`.
	Format string

	// A comma-separated list of column names to display.
	H []string

	// Returns help information.
	//
	// Default: false.
	Help bool

	// Returns local information but does not retrieve the state from the
	// cluster manager node.
	//
	// Default: false.
	Local bool

	// The amount of time allowed to establish a connection to the cluster
	// manager node.
	//
	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	// A comma-separated list of column names or column aliases to sort by.
	S []string

	// Enables verbose mode, which displays column headers.
	//
	// Default: false.
	V bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r CatPluginsParams) get() map[string]string {
	params := make(map[string]string, 8)

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.Format != "" {
		params["format"] = r.Format
	}

	if len(r.H) > 0 {
		params["h"] = strings.Join(r.H, ",")
	}

	if r.Help {
		params["help"] = "true"
	}

	if r.Local {
		params["local"] = "true"
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if len(r.S) > 0 {
		params["s"] = strings.Join(r.S, ",")
	}

	if r.V {
		params["v"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// CatPluginsResp represents the response for the cat.plugins operation.
//
// Returns information about the names, components, and versions of the installed plugins.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cat/cat-plugins/
type CatPluginsResp struct {
	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r CatPluginsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// CatPluginsRecord is a typed component of the cat.plugins response.
type CatPluginsRecord struct {
	// The component name.
	Component *string `json:"component,omitempty"`

	// The plugin details.
	Description *string `json:"description,omitempty"`

	// The unique identifier of a node.
	ID *string `json:"id,omitempty"`

	// The name of a resource or configuration element.
	Name *string `json:"name,omitempty"`

	// The plugin type.
	Type *string `json:"type,omitempty"`

	Version *string `json:"version,omitempty"`
}

func (c catClient) Plugins(ctx context.Context, req *CatPluginsReq) (*CatPluginsResp, error) {
	if req == nil {
		req = &CatPluginsReq{}
	}

	var (
		data CatPluginsResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
