// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"io"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IndicesCloneReq represents the request for the indices.clone operation.
//
// Clones an index.
//
// Path: /{index}/_clone/{target}
//
// Methods: POST, PUT
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/index-apis/clone/
type IndicesCloneReq struct {
	// Index specifies the path segment for the request URL.
	Index string

	// Target specifies the path segment for the request URL.
	Target string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params IndicesCloneParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesCloneReq) GetRequest() (*http.Request, error) {
	path, err := ospath.IndicesClonePath{
		Index:  r.Index,
		Target: r.Target,
	}.Build()
	if err != nil {
		return nil, err
	}
	method := http.MethodPost
	if r.Body != nil {
		method = http.MethodPost
	}

	return opensearch.BuildRequest(
		method,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// IndicesCloneParams represents query parameters for the IndicesCloneReq.
type IndicesCloneParams struct {
	// Operation timeout for connection to cluster-manager node.
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// Period to wait for a connection to the cluster-manager node. If no
	// response is received before the timeout expires, the request fails and
	// returns an error.
	//
	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	// Explicit task execution timeout, only useful when `wait_for_completion`
	// is false, defaults to `1h`.
	TaskExecutionTimeout time.Duration

	// Period to wait for a response. If no response is received before the
	// timeout expires, the request fails and returns an error.
	Timeout time.Duration

	// The number of shard copies that must be active before proceeding with
	// the operation. Set to `all` or any positive integer up to the total
	// number of shards in the index (`number_of_replicas+1`).
	WaitForActiveShards string

	// Should this request wait until the operation has completed before
	// returning.
	//
	// Available: >= 2.7.0.
	//
	// Default: true.
	WaitForCompletion bool

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r IndicesCloneParams) get() map[string]string {
	params := make(map[string]string, 6)

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if r.TaskExecutionTimeout != 0 {
		params["task_execution_timeout"] = formatDuration(r.TaskExecutionTimeout)
	}

	if r.Timeout != 0 {
		params["timeout"] = formatDuration(r.Timeout)
	}

	if r.WaitForActiveShards != "" {
		params["wait_for_active_shards"] = r.WaitForActiveShards
	}

	if r.WaitForCompletion {
		params["wait_for_completion"] = "true"
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// IndicesCloneResp represents the response for the indices.clone operation.
//
// Clones an index.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/index-apis/clone/
type IndicesCloneResp struct {
	AcknowledgedResponseBase
	Index              string `json:"index"`
	ShardsAcknowledged bool   `json:"shards_acknowledged"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesCloneResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c indicesClient) Clone(ctx context.Context, req IndicesCloneReq) (*IndicesCloneResp, error) {
	var (
		data IndicesCloneResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
