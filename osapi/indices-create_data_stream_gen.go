// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"io"
	"net/http"
	"strings"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IndicesCreateDataStreamReq represents the request for the indices.create_data_stream operation.
//
// Creates or updates a data stream.
//
// PUT /_data_stream/{name}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/data-streams/
type IndicesCreateDataStreamReq struct {
	// Name specifies the path segment for the request URL.
	Name string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params IndicesCreateDataStreamParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesCreateDataStreamReq) GetRequest() (*http.Request, error) {
	path, err := ospath.IndicesCreateDataStreamPath{
		Name: r.Name,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPut,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// IndicesCreateDataStreamParams represents query parameters for the IndicesCreateDataStreamReq.
type IndicesCreateDataStreamParams struct {

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r IndicesCreateDataStreamParams) get() map[string]string {
	params := make(map[string]string, 0)

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// IndicesCreateDataStreamResp represents the response for the indices.create_data_stream operation.
//
// Creates or updates a data stream.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/im-plugin/data-streams/
type IndicesCreateDataStreamResp struct {
	// For a successful response, this value is always true. On failure, an
	// exception is returned instead.
	Acknowledged bool `json:"acknowledged"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesCreateDataStreamResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

func (c indicesClient) CreateDataStream(ctx context.Context, req IndicesCreateDataStreamReq) (*IndicesCreateDataStreamResp, error) {
	var (
		data IndicesCreateDataStreamResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
