// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"context"
	"io"
	"net/http"
	"strings"
	"time"

	"github.com/opensearch-project/opensearch-go/v4"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IndicesSimulateIndexTemplateReq represents the request for the indices.simulate_index_template operation.
//
// Simulate matching the given index name against the index templates in the system.
//
// POST /_index_template/_simulate_index/{name}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesSimulateIndexTemplateReq struct {
	// Name specifies the path segment for the request URL.
	Name string

	// Body is the request payload, typically JSON-encoded.
	Body io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params IndicesSimulateIndexTemplateParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesSimulateIndexTemplateReq) GetRequest() (*http.Request, error) {
	path, err := ospath.IndicesSimulateIndexTemplatePath{
		Name: r.Name,
	}.Build()
	if err != nil {
		return nil, err
	}

	return opensearch.BuildRequest(
		http.MethodPost,
		path,
		r.Body,
		r.Params.get(),
		r.Header,
	)
}

// IndicesSimulateIndexTemplateParams represents query parameters for the IndicesSimulateIndexTemplateReq.
type IndicesSimulateIndexTemplateParams struct {
	// Operation timeout for connection to cluster-manager node.
	//
	// Available: >= 2.0.0.
	ClusterManagerTimeout time.Duration

	// Period to wait for a connection to the cluster-manager node. If no
	// response is received before the timeout expires, the request fails and
	// returns an error.
	//
	// Deprecated: since 2.0.0. To promote inclusive language, use
	// `cluster_manager_timeout` instead.
	MasterTimeout time.Duration

	// Pretty enables indented JSON responses for debugging readability.
	Pretty bool
	// Human enables human-friendly values in responses (e.g. "1.5gb" instead of bytes).
	Human bool
	// ErrorTrace includes the Java stack trace of errors in the response.
	ErrorTrace bool
	// FilterPath restricts the response to the specified JSON paths.
	FilterPath []string
}

func (r IndicesSimulateIndexTemplateParams) get() map[string]string {
	params := make(map[string]string, 2)

	if r.ClusterManagerTimeout != 0 {
		params["cluster_manager_timeout"] = formatDuration(r.ClusterManagerTimeout)
	}

	if r.MasterTimeout != 0 {
		params["master_timeout"] = formatDuration(r.MasterTimeout)
	}

	if r.Pretty {
		params["pretty"] = "true"
	}

	if r.Human {
		params["human"] = "true"
	}

	if r.ErrorTrace {
		params["error_trace"] = "true"
	}

	if len(r.FilterPath) > 0 {
		params["filter_path"] = strings.Join(r.FilterPath, ",")
	}

	return params
}

// IndicesSimulateIndexTemplateResp represents the response for the indices.simulate_index_template operation.
//
// Simulate matching the given index name against the index templates in the system.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesSimulateIndexTemplateResp struct {
	Overlapping []IndicesSimulateTemplateOverlapping `json:"overlapping,omitempty"`

	// The configuration for an index template simulation.
	Template *IndicesSimulateTemplateTemplate `json:"template,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesSimulateIndexTemplateResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// IndicesSimulateTemplateOverlapping is a typed component of the indices.simulate_index_template response.
//
// The configuration for overlapping index templates.
type IndicesSimulateTemplateOverlapping struct {
	// The list of index patterns that may overlap with other templates.
	IndexPatterns []string `json:"index_patterns"`

	// The name of a resource or configuration element.
	Name string `json:"name"`
}

// IndicesSimulateTemplateTemplate is a typed component of the indices.simulate_index_template response.
//
// The configuration for an index template simulation.
type IndicesSimulateTemplateTemplate struct {
	// The map of alias names to their configurations.
	Aliases map[string]IndicesAlias `json:"aliases"`

	Mappings *CommonMappingType `json:"mappings,omitempty"`

	// The configuration settings for an index.
	Settings IndicesIndexSettings `json:"settings"`
}

func (c indicesClient) SimulateIndexTemplate(ctx context.Context, req IndicesSimulateIndexTemplateReq) (*IndicesSimulateIndexTemplateResp, error) {
	var (
		data IndicesSimulateIndexTemplateResp
		err  error
	)
	if data.response, err = do(ctx, c.apiClient, req, &data); err != nil {
		return &data, err
	}

	return &data, nil
}
