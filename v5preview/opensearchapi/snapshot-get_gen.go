// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"io"
	"net/http"
	"strconv"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// SnapshotGetReq represents the request for the snapshot.get operation.
//
// Returns information about a snapshot.
//
// GET /_snapshot/{repository}/{snapshot}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type SnapshotGetReq struct {
	// Repository specifies the path segment for the request URL.
	Repository string

	// Snapshot specifies the list of path segments for the request URL.
	Snapshot []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *SnapshotGetParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r SnapshotGetReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.SnapshotGetPath{
		Repository: r.Repository,
		Snapshot:   r.Snapshot,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// SnapshotGetParams represents query parameters for the SnapshotGetReq.
type SnapshotGetParams struct {
	TimeoutParams
	DebugParams
	// When `false`, the request returns an error for any snapshots that are
	// unavailable.
	//
	// Default: false.
	IgnoreUnavailable *bool

	// When `true`, returns additional information about each snapshot, such as
	// the version of OpenSearch which took the snapshot, the start and end
	// times of the snapshot, and the number of shards contained in the
	// snapshot. When `false`, returns only snapshot names and contained
	// indexes. This is useful when the snapshots belong to a cloud-based
	// repository, where each blob read is a cost or performance concern.
	Verbose *bool
}

func (r SnapshotGetParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.IgnoreUnavailable != nil {
		set("ignore_unavailable", strconv.FormatBool(*r.IgnoreUnavailable))
	}

	if r.Verbose != nil {
		set("verbose", strconv.FormatBool(*r.Verbose))
	}

	return params
}

// SnapshotGetResp represents the response for the snapshot.get operation.
//
// Returns information about a snapshot.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type SnapshotGetResp struct {
	Snapshots []SnapshotInfo `json:"snapshots"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r SnapshotGetResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r SnapshotGetResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// Get returns information about a snapshot.
//
// GET /_snapshot/{repository}/{snapshot}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
func (c snapshotClient) Get(ctx context.Context, req SnapshotGetReq) (*SnapshotGetResp, error) {
	var (
		data SnapshotGetResp
		err  error
	)
	if data.response, err = do(
		ctx,
		c.apiClient,
		http.MethodGet,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
