// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strconv"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// SearchShardsReq represents the request for the search_shards operation.
//
// Returns information about the indexes and shards that a search request would be executed against.
//
// Path: /_search_shards
//
// Methods: GET, POST
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type SearchShardsReq struct {
	// Index specifies the list of path segments for the request URL.
	Index []string

	// Body specifies the typed request body. When non-nil, it is
	// marshaled to JSON for the request payload.
	Body *SearchShardsBody

	// BodyReader provides an escape hatch for sending a raw request
	// body. It is used only when Body is nil.
	BodyReader io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *SearchShardsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r SearchShardsReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.SearchShardsPath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	var bodyReader io.Reader
	if r.Body != nil {
		bodyData, err := json.Marshal(r.Body)
		if err != nil {
			return nil, err
		}
		bodyReader = bytes.NewReader(bodyData)
	} else if r.BodyReader != nil {
		bodyReader = r.BodyReader
	}

	return build.Request(
		method,
		path,
		bodyReader,
		params,
		r.Header,
	)
}

// SearchShardsParams represents query parameters for the SearchShardsReq.
type SearchShardsParams struct {
	TimeoutParams
	DebugParams
	// If `false`, the request returns an error if any wildcard expression,
	// index alias, or `_all` value targets only missing or closed indexes.
	// This behavior applies even if the request targets other open indexes.
	// For example, a request targeting `foo*,bar*` returns an error if an
	// index starts with `foo` but no index starts with `bar`.
	AllowNoIndices *bool

	// Type of index that wildcard patterns can match. If the request can
	// target data streams, this argument determines whether wildcard
	// expressions match hidden data streams. Supports comma-separated values,
	// such as `open,hidden`. Valid values are: `all`, `open`, `closed`,
	// `hidden`, `none`.
	ExpandWildcards []string

	// If `false`, the request returns an error if it targets a missing or
	// closed index.
	IgnoreUnavailable *bool

	// If `true`, the request retrieves information from the local node only.
	//
	// Default: false.
	Local *bool

	// Specifies the node or shard the operation should be performed on. Random
	// by default.
	//
	// Default: random.
	Preference string

	// A custom value used to route operations to a specific shard.
	Routing []string
}

func (r SearchShardsParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.AllowNoIndices != nil {
		set("allow_no_indices", strconv.FormatBool(*r.AllowNoIndices))
	}

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	if r.IgnoreUnavailable != nil {
		set("ignore_unavailable", strconv.FormatBool(*r.IgnoreUnavailable))
	}

	if r.Local != nil {
		set("local", strconv.FormatBool(*r.Local))
	}

	if r.Preference != "" {
		set("preference", r.Preference)
	}

	if len(r.Routing) > 0 {
		set("routing", strings.Join(r.Routing, ","))
	}

	return params
}

// SearchShardsResp represents the response for the search_shards operation.
//
// Returns information about the indexes and shards that a search request would be executed against.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type SearchShardsResp struct {
	Indices map[string]SearchShardsShardStoreIndex `json:"indices"`
	Nodes   map[string]NodeAttributes              `json:"nodes"`
	Shards  [][]NodeShard                          `json:"shards"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r SearchShardsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r SearchShardsResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// SearchShardsShardStoreIndex is a typed component of the search_shards operation.
type SearchShardsShardStoreIndex struct {
	Aliases []string                      `json:"aliases,omitempty"`
	Filter  *CommonQueryDSLQueryContainer `json:"filter,omitempty"`
}

// SearchShardsBody is a typed component of the search_shards operation.
type SearchShardsBody struct {
	// The configuration for a sliced scroll request.
	Slice *SlicedScroll `json:"slice,omitempty"`
}

// SearchShards returns information about the indexes and shards that a search request would be executed against.
//
// Path: /_search_shards
//
// Methods: GET, POST
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
func (c Client) SearchShards(ctx context.Context, req *SearchShardsReq) (*SearchShardsResp, error) {
	if req == nil {
		req = &SearchShardsReq{}
	}

	var (
		data SearchShardsResp
		err  error
	)
	method := http.MethodGet
	if req.Body != nil || req.BodyReader != nil {
		method = http.MethodPost
	}
	if data.response, err = do(
		ctx,
		&c,
		method,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
