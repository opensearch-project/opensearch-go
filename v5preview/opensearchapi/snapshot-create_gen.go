// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strconv"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// SnapshotCreateReq represents the request for the snapshot.create operation.
//
// Creates a snapshot within an existing repository.
//
// Path: /_snapshot/{repository}/{snapshot}
//
// Methods: POST, PUT
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/snapshots/create-snapshot/
type SnapshotCreateReq struct {
	// Repository specifies the path segment for the request URL.
	Repository string

	// Snapshot specifies the path segment for the request URL.
	Snapshot string

	// Body specifies the typed request body. When non-nil, it is
	// marshaled to JSON for the request payload.
	Body *SnapshotCreateBody

	// BodyReader provides an escape hatch for sending a raw request
	// body. It is used only when Body is nil.
	BodyReader io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *SnapshotCreateParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r SnapshotCreateReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.SnapshotCreatePath{
		Repository: r.Repository,
		Snapshot:   r.Snapshot,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	var bodyReader io.Reader
	if r.Body != nil {
		bodyData, err := json.Marshal(r.Body)
		if err != nil {
			return nil, err
		}
		bodyReader = bytes.NewReader(bodyData)
	} else if r.BodyReader != nil {
		bodyReader = r.BodyReader
	}

	return build.Request(
		method,
		path,
		bodyReader,
		params,
		r.Header,
	)
}

// SnapshotCreateParams represents query parameters for the SnapshotCreateReq.
type SnapshotCreateParams struct {
	TimeoutParams
	DebugParams
	// When `true`, the request returns a response when the snapshot is
	// complete. When `false`, the request returns a response when the snapshot
	// initializes.
	//
	// Default: false.
	WaitForCompletion *bool
}

func (r SnapshotCreateParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.WaitForCompletion != nil {
		set("wait_for_completion", strconv.FormatBool(*r.WaitForCompletion))
	}

	return params
}

// SnapshotCreateResp represents the response for the snapshot.create operation.
//
// Creates a snapshot within an existing repository.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/snapshots/create-snapshot/
type SnapshotCreateResp struct {
	// Returns `true` if the snapshot was accepted. Present when the request
	// had `wait_for_completion` set to `false`.
	Accepted *bool `json:"accepted,omitempty"`

	Snapshot *SnapshotInfo `json:"snapshot,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r SnapshotCreateResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r SnapshotCreateResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// SnapshotCreateBody is a typed component of the snapshot.create operation.
//
// The snapshot definition.
type SnapshotCreateBody struct {
	// A list of feature states to include in the snapshot. Each feature state
	// includes one or more system indexes which contain related data. You can
	// view a list of features using the Get Feature API. If
	// `include_global_state` is `true`, all current feature states are
	// included by default. If `include_global_state` is `false`, no feature
	// states are included by default.
	FeatureStates []string `json:"feature_states,omitempty"`

	// When `true`, the request ignores any data streams and indexes that are
	// missing or closed. When `false`, the request returns an error for any
	// data streams or indexes that is missing or closed.
	IgnoreUnavailable *bool `json:"ignore_unavailable,omitempty"`

	// When `true`, includes the current cluster state in the snapshot. The
	// cluster state includes persistent cluster settings, composable index
	// templates, legacy index templates, ingest pipelines, and ILM policies.
	// The cluster state also includes data stored in system indexes, such as
	// watches and task records (configurable with `feature_states`).
	IncludeGlobalState *bool `json:"include_global_state,omitempty"`

	// A comma-separated list of data streams, indexes, and aliases used to
	// limit the request. Supports wildcards (`*`). To target all data streams
	// and indexes, omit this parameter or use `*` or `_all`.
	Indices []string `json:"indices,omitempty"`

	// The custom metadata attached to a resource.
	Metadata map[string]json.RawMessage `json:"metadata,omitempty"`

	// When `true`, enables the restoration of a partial snapshot from indexes
	// with unavailable shards. Only shards that were successfully included in
	// the snapshot will be restored. All missing shards will be recreated as
	// empty. When `false`, the entire restore operation will fail if one or
	// more indexes included in the snapshot do not have all primary shards
	// available.
	Partial *bool `json:"partial,omitempty"`
}

// Create creates a snapshot within an existing repository.
//
// Path: /_snapshot/{repository}/{snapshot}
//
// Methods: POST, PUT
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/snapshots/create-snapshot/
func (c snapshotClient) Create(ctx context.Context, req SnapshotCreateReq) (*SnapshotCreateResp, error) {
	var (
		data SnapshotCreateResp
		err  error
	)
	if data.response, err = do(
		ctx,
		c.apiClient,
		http.MethodPost,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
