// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package osapi

import (
	"bytes"
	"context"
	"io"
	"net/http"
	"strconv"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IndicesShardStoresReq represents the request for the indices.shard_stores operation.
//
// Provides store information for shard copies of indexes.
//
// GET /_shard_stores
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesShardStoresReq struct {
	// Index specifies the list of path segments for the request URL.
	Index []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *IndicesShardStoresParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesShardStoresReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.IndicesShardStoresPath{
		Index: r.Index,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// IndicesShardStoresParams represents query parameters for the IndicesShardStoresReq.
type IndicesShardStoresParams struct {
	TimeoutParams
	DebugParams
	// If `false`, the request returns an error if any wildcard expression,
	// index alias, or `_all` value targets only missing or closed indexes.
	// This behavior applies even if the request targets other open indexes.
	//
	// Default: false.
	AllowNoIndices *bool

	// Type of index that wildcard patterns can match. If the request can
	// target data streams, this argument determines whether wildcard
	// expressions match hidden data streams.
	//
	// Default: open.
	ExpandWildcards []string

	// If `true`, missing or closed indexes are not included in the response.
	//
	// Default: false.
	IgnoreUnavailable *bool

	// A list of shard health statuses used to limit the request.
	//
	// Default: [yellow red].
	Status []string
}

func (r IndicesShardStoresParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.AllowNoIndices != nil {
		set("allow_no_indices", strconv.FormatBool(*r.AllowNoIndices))
	}

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	if r.IgnoreUnavailable != nil {
		set("ignore_unavailable", strconv.FormatBool(*r.IgnoreUnavailable))
	}

	if len(r.Status) > 0 {
		set("status", strings.Join(r.Status, ","))
	}

	return params
}

// IndicesShardStoresResp represents the response for the indices.shard_stores operation.
//
// Provides store information for shard copies of indexes.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesShardStoresResp struct {
	Indices map[string]IndicesShardStoresIndices `json:"indices"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesShardStoresResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r IndicesShardStoresResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// IndicesShardStoresIndices is a typed component of the indices.shard_stores operation.
//
// The shard store information for indices.
type IndicesShardStoresIndices struct {
	// The store information for each shard.
	Shards map[string]IndicesShardStoresShardStoreWrapper `json:"shards"`
}

// IndicesShardStoresShardStoreWrapper is a typed component of the indices.shard_stores operation.
//
// The wrapper containing store information for a shard.
type IndicesShardStoresShardStoreWrapper struct {
	// The list of stores for the shard.
	Stores []IndicesShardStoresShardStore `json:"stores"`
}

// IndicesShardStoresShardStore is a typed component of the indices.shard_stores operation.
//
// The store information for a shard.
type IndicesShardStoresShardStore struct {
	// The allocation type of the shard store.
	Allocation string `json:"allocation"`

	// The unique identifier for a resource.
	AllocationID *string `json:"allocation_id,omitempty"`

	// The exception information for a shard store. Serialized by
	// `OpenSearchException.generateThrowableXContent`, so the shape is
	// identical to `ErrorCause` (includes `index`, `index_uuid`, `shard`,
	// `caused_by`, `stack_trace`, etc. when present).
	StoreException *IndicesShardStoresShardStoreException `json:"store_exception,omitempty"`
}

// IndicesShardStoresShardStoreException is a typed component of the indices.shard_stores operation.
//
// The exception information for a shard store.
// Serialized by `OpenSearchException.generateThrowableXContent`, so the
// shape is identical to `ErrorCause` (includes `index`, `index_uuid`,
// `shard`, `caused_by`, `stack_trace`, etc. when present).
type IndicesShardStoresShardStoreException struct {
	ErrorCause

	// The name of the index associated with the exception.
	Index *string `json:"index,omitempty"`

	// The UUID of the index associated with the exception.
	IndexUUID *string `json:"index_uuid,omitempty"`

	// The shard number associated with the exception.
	Shard *string `json:"shard,omitempty"`
}

// ShardStores provides store information for shard copies of indexes.
//
// GET /_shard_stores
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
func (c indicesClient) ShardStores(ctx context.Context, req *IndicesShardStoresReq) (*IndicesShardStoresResp, error) {
	if req == nil {
		req = &IndicesShardStoresReq{}
	}

	var (
		data IndicesShardStoresResp
		err  error
	)
	if data.response, err = do(
		ctx,
		c.apiClient,
		http.MethodGet,
		req, &data,
	); err != nil {
		return &data, err
	}

	return &data, nil
}
