// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"encoding/json"
	"io"
	"net/http"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// ClusterPutWeightedRoutingReq represents the request for the cluster.put_weighted_routing operation.
//
// Updates weighted shard routing weights.
//
// PUT /_cluster/routing/awareness/{attribute}/weights
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-awareness/#example-weighted-round-robin-search
type ClusterPutWeightedRoutingReq struct {
	// Attribute specifies the path segment for the request URL.
	Attribute string

	// Body specifies the typed request body. When non-nil, it is
	// marshaled to JSON for the request payload.
	Body *ClusterPutWeightedRoutingBody

	// BodyReader provides an escape hatch for sending a raw request
	// body. It is used only when Body is nil.
	BodyReader io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *ClusterPutWeightedRoutingParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r ClusterPutWeightedRoutingReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.ClusterPutWeightedRoutingPath{
		Attribute: r.Attribute,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	var bodyReader io.Reader
	if r.Body != nil {
		bodyData, err := json.Marshal(r.Body)
		if err != nil {
			return nil, err
		}
		bodyReader = bytes.NewReader(bodyData)
	} else if r.BodyReader != nil {
		bodyReader = r.BodyReader
	}

	return build.Request(
		method,
		path,
		bodyReader,
		params,
		r.Header,
	)
}

// ClusterPutWeightedRoutingParams represents query parameters for the ClusterPutWeightedRoutingReq.
type ClusterPutWeightedRoutingParams struct {
	TimeoutParams
	DebugParams
}

func (r ClusterPutWeightedRoutingParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	return params
}

// ClusterPutWeightedRoutingResp represents the response for the cluster.put_weighted_routing operation.
//
// Updates weighted shard routing weights.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-awareness/#example-weighted-round-robin-search
type ClusterPutWeightedRoutingResp struct {
	// For a successful response, this value is always true. On failure, an
	// exception is returned instead.
	Acknowledged bool `json:"acknowledged"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r ClusterPutWeightedRoutingResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r ClusterPutWeightedRoutingResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// ClusterPutWeightedRoutingBody is a typed component of the cluster.put_weighted_routing operation.
type ClusterPutWeightedRoutingBody struct {
	ClusterWeightedRoutingWeightsBase
}

// PutWeightedRouting updates weighted shard routing weights.
//
// PUT /_cluster/routing/awareness/{attribute}/weights
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-awareness/#example-weighted-round-robin-search
func (c clusterClient) PutWeightedRouting(ctx context.Context, req ClusterPutWeightedRoutingReq) (*ClusterPutWeightedRoutingResp, error) {
	var (
		data ClusterPutWeightedRoutingResp
		err  error
	)
	if data.response, err = do(
		ctx,
		c.apiClient,
		http.MethodPut,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
