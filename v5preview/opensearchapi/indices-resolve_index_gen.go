// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"io"
	"net/http"
	"strings"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// IndicesResolveIndexReq represents the request for the indices.resolve_index operation.
//
// Returns information about any matching indexes, aliases, and data streams.
//
// GET /_resolve/index/{name}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesResolveIndexReq struct {
	// Name specifies the list of path segments for the request URL.
	Name []string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *IndicesResolveIndexParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r IndicesResolveIndexReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.IndicesResolveIndexPath{
		Name: r.Name,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// IndicesResolveIndexParams represents query parameters for the IndicesResolveIndexReq.
type IndicesResolveIndexParams struct {
	TimeoutParams
	DebugParams
	// Type of index that wildcard patterns can match. If the request can
	// target data streams, this argument determines whether wildcard
	// expressions match hidden data streams. Supports comma-separated values,
	// such as `open,hidden`. Valid values are: `all`, `open`, `closed`,
	// `hidden`, `none`.
	ExpandWildcards []string
}

func (r IndicesResolveIndexParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if len(r.ExpandWildcards) > 0 {
		set("expand_wildcards", strings.Join(r.ExpandWildcards, ","))
	}

	return params
}

// IndicesResolveIndexResp represents the response for the indices.resolve_index operation.
//
// Returns information about any matching indexes, aliases, and data streams.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type IndicesResolveIndexResp struct {
	Aliases     []IndicesResolveIndexAliasItem       `json:"aliases"`
	DataStreams []IndicesResolveIndexDataStreamsItem `json:"data_streams"`
	Indices     []IndicesResolveIndexItem            `json:"indices"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r IndicesResolveIndexResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r IndicesResolveIndexResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// IndicesResolveIndexAliasItem is a typed component of the indices.resolve_index operation.
//
// The resolved information for an index alias.
type IndicesResolveIndexAliasItem struct {
	// A comma-separated list of data streams, indexes, and aliases used to
	// limit the request. Supports wildcards (`*`). To target all data streams
	// and indexes, omit this parameter or use `*` or `_all`.
	Indices []string `json:"indices"`

	// The name of a resource or configuration element.
	Name string `json:"name"`
}

// IndicesResolveIndexDataStreamsItem is a typed component of the indices.resolve_index operation.
//
// The resolved information for a data stream.
type IndicesResolveIndexDataStreamsItem struct {
	// A comma-separated list of data streams, indexes, and aliases used to
	// limit the request. Supports wildcards (`*`). To target all data streams
	// and indexes, omit this parameter or use `*` or `_all`.
	BackingIndices []string `json:"backing_indices"`

	Name string `json:"name"`

	// The path to a field or an array of paths. Some APIs support wildcards in
	// the path, which allows you to select multiple fields.
	TimestampField string `json:"timestamp_field"`
}

// IndicesResolveIndexItem is a typed component of the indices.resolve_index operation.
//
// The resolved information for an index.
type IndicesResolveIndexItem struct {
	// The list of aliases associated with the index.
	Aliases []string `json:"aliases,omitempty"`

	// The list of index attributes.
	Attributes []string `json:"attributes"`

	DataStream *string `json:"data_stream,omitempty"`

	// The name of a resource or configuration element.
	Name string `json:"name"`
}

// ResolveIndex returns information about any matching indexes, aliases, and data streams.
//
// GET /_resolve/index/{name}
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
func (c indicesClient) ResolveIndex(ctx context.Context, req *IndicesResolveIndexReq) (*IndicesResolveIndexResp, error) {
	if req == nil {
		req = &IndicesResolveIndexReq{}
	}

	var (
		data IndicesResolveIndexResp
		err  error
	)
	if data.response, err = do(
		ctx,
		c.apiClient,
		http.MethodGet,
		req, &data,
	); err != nil {
		return &data, err
	}

	return &data, nil
}
