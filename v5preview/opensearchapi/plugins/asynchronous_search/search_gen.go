// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package asynchronous_search

import (
	"bytes"
	"encoding/json"
	"io"
	"net/http"
	"strconv"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
	"github.com/opensearch-project/opensearch-go/v4/v5preview/opensearchapi"
)

// SearchReq represents the request for the asynchronous_search.search operation.
//
// Performs an asynchronous search.
//
// POST /_plugins/_asynchronous_search
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/search-plugins/async/index/#rest-api
type SearchReq struct {

	// Body specifies the typed request body. When non-nil, it is
	// marshaled to JSON for the request payload.
	Body *opensearchapi.AsynchronousSearchSearch

	// BodyReader provides an escape hatch for sending a raw request
	// body. It is used only when Body is nil.
	BodyReader io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *SearchParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r SearchReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.AsynchronousSearchSearchPath{}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	var bodyReader io.Reader
	if r.Body != nil {
		bodyData, err := json.Marshal(r.Body)
		if err != nil {
			return nil, err
		}
		bodyReader = bytes.NewReader(bodyData)
	} else if r.BodyReader != nil {
		bodyReader = r.BodyReader
	}

	return build.Request(
		method,
		path,
		bodyReader,
		params,
		r.Header,
	)
}

// SearchParams represents query parameters for the SearchReq.
type SearchParams struct {
	opensearchapi.TimeoutParams
	opensearchapi.DebugParams
	// The name of the index to be searched. Can be an individual name, a
	// comma-separated list of indexes, or a wildcard expression of index
	// names.
	Index string

	// The amount of time that the result is saved in the cluster. For example,
	// `2d` means that the results are stored in the cluster for 48 hours. The
	// saved search results are deleted after this period or if the search is
	// canceled. Note that this includes the query execution time. If the query
	// exceeds this amount of time, the process cancels this query
	// automatically.
	KeepAlive string

	// Whether to save the results in the cluster after the search is complete.
	// You can examine the stored results at a later time.
	KeepOnCompletion *bool

	// The amount of time to wait for the results. You can poll the remaining
	// results based on an ID. The maximum value is 300 seconds. Default is
	// `1s`.
	WaitForCompletionTimeout string
}

func (r SearchParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.Index != "" {
		set("index", r.Index)
	}

	if r.KeepAlive != "" {
		set("keep_alive", r.KeepAlive)
	}

	if r.KeepOnCompletion != nil {
		set("keep_on_completion", strconv.FormatBool(*r.KeepOnCompletion))
	}

	if r.WaitForCompletionTimeout != "" {
		set("wait_for_completion_timeout", r.WaitForCompletionTimeout)
	}

	return params
}

// SearchResp represents the response for the asynchronous_search.search operation.
//
// Performs an asynchronous search.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/search-plugins/async/index/#rest-api
type SearchResp struct {
	ExpirationTimeInMillis *float64                      `json:"expiration_time_in_millis,omitempty"`
	ID                     *string                       `json:"id,omitempty"`
	Response               *opensearchapi.SearchResponse `json:"response,omitempty"`
	StartTimeInMillis      *float64                      `json:"start_time_in_millis,omitempty"`
	State                  *string                       `json:"state,omitempty"`
	Took                   *float64                      `json:"took,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r SearchResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r SearchResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}
