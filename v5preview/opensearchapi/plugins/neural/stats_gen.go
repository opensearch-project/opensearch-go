// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package neural

import (
	"bytes"
	"encoding/json"
	"io"
	"net/http"
	"strconv"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
	"github.com/opensearch-project/opensearch-go/v4/osapi"
)

// StatsReq represents the request for the neural.stats operation.
//
// Provides information about the current status of the neural-search plugin.
//
// GET /_plugins/_neural/stats
//
// Available: >= 3.0.0.
type StatsReq struct {
	// Stat specifies the list of path segments for the request URL.
	Stat []string

	// NodeID specifies the path segment for the request URL.
	NodeID string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *StatsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r StatsReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.NeuralStatsPath{
		Stat:   r.Stat,
		NodeID: r.NodeID,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// StatsParams represents query parameters for the StatsReq.
type StatsParams struct {
	osapi.TimeoutParams
	osapi.DebugParams
	// Whether to return stats in the flat form, which can improve readability,
	// especially for heavily nested stats. For example, the flat form of
	// `"processors": { "ingest": { "text_embedding_executions": 20181212 } }`
	// is `"processors.ingest.text_embedding_executions": "20181212"`.
	//
	// Default: false.
	FlatStatPaths *bool

	// When `true` includes aggregated statistics across all nodes in the
	// `all_nodes` category. When `false`, excludes the `all_nodes` category
	// from the response.
	//
	// Available: >= 3.1.0.
	//
	// Default: true.
	IncludeAllNodes *bool

	// When `true` includes statistics for individual nodes in the `nodes`
	// category. When `false`, excludes the `nodes` category from the response.
	//
	// Available: >= 3.1.0.
	//
	// Default: true.
	IncludeIndividualNodes *bool

	// When `true` includes cluster-wide information in the `info` category.
	// When `false`, excludes the `info` category from the response.
	//
	// Available: >= 3.1.0.
	//
	// Default: true.
	IncludeInfo *bool

	// Whether to return stat metadata instead of the raw stat value, includes
	// additional information about the stat. These can include things like
	// type hints, time since last stats being recorded, or recent rolling
	// interval values
	//
	// Default: false.
	IncludeMetadata *bool
}

func (r StatsParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.FlatStatPaths != nil {
		set("flat_stat_paths", strconv.FormatBool(*r.FlatStatPaths))
	}

	if r.IncludeAllNodes != nil {
		set("include_all_nodes", strconv.FormatBool(*r.IncludeAllNodes))
	}

	if r.IncludeIndividualNodes != nil {
		set("include_individual_nodes", strconv.FormatBool(*r.IncludeIndividualNodes))
	}

	if r.IncludeInfo != nil {
		set("include_info", strconv.FormatBool(*r.IncludeInfo))
	}

	if r.IncludeMetadata != nil {
		set("include_metadata", strconv.FormatBool(*r.IncludeMetadata))
	}

	return params
}

// StatsResp represents the response for the Stats operation.
// The response body has a dynamic schema and is captured as raw JSON.
//
// Provides information about the current status of the neural-search plugin.
type StatsResp struct {
	Body     json.RawMessage `json:"-"`
	response *opensearch.Response
}

// UnmarshalJSON captures the raw response body.
//
//nolint:unparam // error return required by json.Unmarshaler; raw passthrough never fails
func (r *StatsResp) UnmarshalJSON(b []byte) error {
	r.Body = append(r.Body[:0], b...)
	return nil
}

// MarshalJSON returns the raw response body for comparison testing.
//
//nolint:unparam // error return required by json.Marshaler; raw passthrough never fails
func (r StatsResp) MarshalJSON() ([]byte, error) {
	if r.Body == nil {
		return build.NullJSON, nil
	}
	return r.Body, nil
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r StatsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r StatsResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}
