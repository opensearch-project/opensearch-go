// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package ppl

import (
	"bytes"
	"encoding/json"
	"io"
	"net/http"
	"strconv"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
	"github.com/opensearch-project/opensearch-go/v4/osapi"
)

// QueryReq represents the request for the ppl.query operation.
//
// Executes a PPL query against OpenSearch indexes.
//
// POST /_plugins/_ppl
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
type QueryReq struct {

	// Body specifies the typed request body. When non-nil, it is
	// marshaled to JSON for the request payload.
	Body *osapi.SQLQuery

	// BodyReader provides an escape hatch for sending a raw request
	// body. It is used only when Body is nil.
	BodyReader io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *QueryParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r QueryReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.PplQueryPath{}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	var bodyReader io.Reader
	if r.Body != nil {
		bodyData, err := json.Marshal(r.Body)
		if err != nil {
			return nil, err
		}
		bodyReader = bytes.NewReader(bodyData)
	} else if r.BodyReader != nil {
		bodyReader = r.BodyReader
	}

	return build.Request(
		method,
		path,
		bodyReader,
		params,
		r.Header,
	)
}

// QueryParams represents query parameters for the QueryReq.
type QueryParams struct {
	osapi.TimeoutParams
	osapi.DebugParams
	// Whether to sanitize special characters in the results.
	//
	// Default: true.
	Sanitize *bool
}

func (r QueryParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)
	if r.Format == "" {
		set("format", "jdbc")
	}

	if r.Sanitize != nil {
		set("sanitize", strconv.FormatBool(*r.Sanitize))
	}

	return params
}

// QueryResp represents the response for the ppl.query operation.
//
// Executes a PPL query against OpenSearch indexes.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
type QueryResp struct {
	// The cursor identifier for pagination.
	Cursor *string `json:"cursor,omitempty"`

	// The rows of data returned by the query.
	Datarows [][]json.RawMessage `json:"datarows,omitempty"`

	// The schema of the query result.
	Schema []json.RawMessage `json:"schema,omitempty"`

	// The number of rows in the current response.
	Size *int `json:"size,omitempty"`

	// The status code of the response.
	Status *int `json:"status,omitempty"`

	// The total number of hits.
	Total *int `json:"total,omitempty"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r QueryResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r QueryResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}
