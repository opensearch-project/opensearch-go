// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"encoding/json"
	"io"
	"net/http"
	"strconv"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// ClusterPutSettingsReq represents the request for the cluster.put_settings operation.
//
// Updates the cluster settings.
//
// PUT /_cluster/settings
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-settings/
type ClusterPutSettingsReq struct {

	// Body specifies the typed request body. When non-nil, it is
	// marshaled to JSON for the request payload.
	Body *ClusterPutSettingsBody

	// BodyReader provides an escape hatch for sending a raw request
	// body. It is used only when Body is nil.
	BodyReader io.Reader

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *ClusterPutSettingsParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r ClusterPutSettingsReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.ClusterPutSettingsPath{}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	var bodyReader io.Reader
	if r.Body != nil {
		bodyData, err := json.Marshal(r.Body)
		if err != nil {
			return nil, err
		}
		bodyReader = bytes.NewReader(bodyData)
	} else if r.BodyReader != nil {
		bodyReader = r.BodyReader
	}

	return build.Request(
		method,
		path,
		bodyReader,
		params,
		r.Header,
	)
}

// ClusterPutSettingsParams represents query parameters for the ClusterPutSettingsReq.
type ClusterPutSettingsParams struct {
	TimeoutParams
	DebugParams
	// Whether to return settings in the flat form, which can improve
	// readability, especially for heavily nested settings. For example, the
	// flat form of `"cluster": { "max_shards_per_node": 500 }` is
	// `"cluster.max_shards_per_node": "500"`.
	//
	// Default: false.
	FlatSettings *bool
}

func (r ClusterPutSettingsParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.FlatSettings != nil {
		set("flat_settings", strconv.FormatBool(*r.FlatSettings))
	}

	return params
}

// ClusterPutSettingsResp represents the response for the cluster.put_settings operation.
//
// Updates the cluster settings.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-settings/
type ClusterPutSettingsResp struct {
	Acknowledged bool                       `json:"acknowledged"`
	Persistent   map[string]json.RawMessage `json:"persistent"`
	Transient    map[string]json.RawMessage `json:"transient"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r ClusterPutSettingsResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r ClusterPutSettingsResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// ClusterPutSettingsBody is a typed component of the cluster.put_settings operation.
//
// The cluster settings to update.
type ClusterPutSettingsBody struct {
	Persistent map[string]json.RawMessage `json:"persistent,omitempty"`
	Transient  map[string]json.RawMessage `json:"transient,omitempty"`
}

// PutSettings updates the cluster settings.
//
// PUT /_cluster/settings
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest/api-reference/cluster-settings/
func (c clusterClient) PutSettings(ctx context.Context, req *ClusterPutSettingsReq) (*ClusterPutSettingsResp, error) {
	if req == nil {
		req = &ClusterPutSettingsReq{}
	}

	var (
		data ClusterPutSettingsResp
		err  error
	)
	if data.response, err = do(
		ctx,
		c.apiClient,
		http.MethodPut,
		req, &data,
	); err != nil {
		return &data, err
	}
	return &data, nil
}
