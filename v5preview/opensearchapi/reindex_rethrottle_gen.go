// SPDX-License-Identifier: Apache-2.0
//
// The OpenSearch Contributors require contributions made to
// this file be licensed under the Apache-2.0 license or a
// compatible open source license.

// Code generated by cmd/osgen; DO NOT EDIT.

package opensearchapi

import (
	"bytes"
	"context"
	"io"
	"net/http"
	"strconv"

	opensearch "github.com/opensearch-project/opensearch-go/v4"
	"github.com/opensearch-project/opensearch-go/v4/internal/build"
	osparams "github.com/opensearch-project/opensearch-go/v4/internal/params"
	ospath "github.com/opensearch-project/opensearch-go/v4/internal/path"
)

// ReindexRethrottleReq represents the request for the reindex_rethrottle operation.
//
// Changes the number of requests per second for a particular reindex operation.
//
// POST /_reindex/{task_id}/_rethrottle
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type ReindexRethrottleReq struct {
	// TaskID specifies the path segment for the request URL.
	TaskID string

	// Header provides additional HTTP headers for the request.
	Header http.Header

	// Params holds optional query parameters for the request.
	Params *ReindexRethrottleParams
}

// GetRequest builds the HTTP request from the structured fields.
func (r ReindexRethrottleReq) GetRequest(method string) (*http.Request, error) {
	path, err := ospath.ReindexRethrottlePath{
		TaskID: r.TaskID,
	}.Build()
	if err != nil {
		return nil, err
	}

	var params map[string]string
	if r.Params != nil {
		params = r.Params.get()
	}

	return build.Request(
		method,
		path,
		nil,
		params,
		r.Header,
	)
}

// ReindexRethrottleParams represents query parameters for the ReindexRethrottleReq.
type ReindexRethrottleParams struct {
	TimeoutParams
	DebugParams
	// The throttle for this request in sub-requests per second.
	RequestsPerSecond int
}

func (r ReindexRethrottleParams) get() map[string]string {
	var params map[string]string
	set := func(k, v string) {
		if params == nil {
			params = make(map[string]string)
		}
		params[k] = v
	}
	osparams.EncodeTimeout(r.TimeoutParams, set)
	osparams.EncodeDebug(r.DebugParams, set)

	if r.RequestsPerSecond != 0 {
		set("requests_per_second", strconv.Itoa(r.RequestsPerSecond))
	}

	return params
}

// ReindexRethrottleResp represents the response for the reindex_rethrottle operation.
//
// Changes the number of requests per second for a particular reindex operation.
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
type ReindexRethrottleResp struct {
	Nodes map[string]ReindexRethrottleReindexNode `json:"nodes"`

	response *opensearch.Response
}

// Inspect returns the raw OpenSearch response for debugging or advanced use.
func (r ReindexRethrottleResp) Inspect() Inspect {
	return Inspect{Response: r.response}
}

// RawBody returns a fresh reader over the original response bytes,
// useful when the typed response struct is incomplete for your use case.
func (r ReindexRethrottleResp) RawBody() io.Reader {
	if r.response == nil || len(r.response.RawBody()) == 0 {
		return nil
	}
	return bytes.NewReader(r.response.RawBody())
}

// ReindexRethrottleReindexNode is a typed component of the reindex_rethrottle operation.
type ReindexRethrottleReindexNode struct {
	BaseNode
	Tasks map[string]ReindexRethrottleReindexTask `json:"tasks"`
}

// ReindexRethrottleReindexTask is a typed component of the reindex_rethrottle operation.
type ReindexRethrottleReindexTask struct {
	Action      string `json:"action"`
	Cancellable bool   `json:"cancellable"`
	Cancelled   *bool  `json:"cancelled,omitempty"`
	Description string `json:"description"`

	// The HTTP headers.
	Headers map[string]HttpHeadersValue `json:"headers"`

	ID int64 `json:"id"`

	// The name of a resource or configuration element.
	Node string `json:"node"`

	// The statistics about resource usage.
	ResourceStats *ResourceStats `json:"resource_stats,omitempty"`

	// Time unit for nanoseconds.
	RunningTimeInNanos int64 `json:"running_time_in_nanos"`

	// The time unit for milliseconds.
	StartTimeInMillis int64 `json:"start_time_in_millis"`

	Status ReindexRethrottleReindexStatus `json:"status"`
	Type   string                         `json:"type"`
}

// ReindexRethrottleReindexStatus is a typed component of the reindex_rethrottle operation.
type ReindexRethrottleReindexStatus struct {
	// The number of scroll responses shown by the reindex.
	Batches int64 `json:"batches"`

	// The number of documents that were successfully created.
	Created int64 `json:"created"`

	// The number of documents that were successfully deleted.
	Deleted int64 `json:"deleted"`

	// The number of documents that were ignored because the script used for
	// the reindex operation returned a `noop` value for `ctx.op`.
	Noops int64 `json:"noops"`

	// The number of successful requests per second during the reindex
	// operation.
	RequestsPerSecond float32 `json:"requests_per_second"`

	// The retry statistics for bulk and search operations.
	Retries Retries `json:"retries"`

	// A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s`
	// (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0`
	// without a unit and `-1` to indicate an unspecified value.
	Throttled *string `json:"throttled,omitempty"`

	// The time unit for milliseconds.
	ThrottledMillis int64 `json:"throttled_millis"`

	// A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s`
	// (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0`
	// without a unit and `-1` to indicate an unspecified value.
	ThrottledUntil *string `json:"throttled_until,omitempty"`

	// The time unit for milliseconds.
	ThrottledUntilMillis int64 `json:"throttled_until_millis"`

	// The number of documents that were successfully processed.
	Total int64 `json:"total"`

	// The number of documents that were successfully updated.
	Updated int64 `json:"updated"`

	// The number of version conflicts that reindex hits.
	VersionConflicts int64 `json:"version_conflicts"`
}

// ReindexRethrottle changes the number of requests per second for a particular reindex operation.
//
// POST /_reindex/{task_id}/_rethrottle
//
// Available: >= 1.0.0.
//
// See: https://opensearch.org/docs/latest
func (c Client) ReindexRethrottle(ctx context.Context, req ReindexRethrottleReq) (*ReindexRethrottleResp, error) {
	var (
		data ReindexRethrottleResp
		err  error
	)
	if data.response, err = do(
		ctx,
		&c,
		http.MethodPost,
		req, &data,
	); err != nil {
		return &data, err
	}

	return &data, nil
}
